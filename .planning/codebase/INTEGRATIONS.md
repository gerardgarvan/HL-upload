# External Integrations

**Analysis Date:** 2026-03-17

## APIs & External Services

**Data Sources:**
- OneFlorida+ PCORnet CDM - Source of health records data
  - Format: CSV files (22 tables in Mailhot cohort)
  - Location: HPC orange filesystem at `/orange/erin.mobley-hl.bcu/Mailhot_V1_20250915`
  - Schema reference: `datastructure.txt` lists all 22 table filenames
  - Credentials: HPC account access (path in `config/paths.toml`)

## Data Storage

**Databases:**
- No external database connections detected
- Local file-based pipeline only

**File Storage:**
- HPC Orange Filesystem (read-only source)
  - Connection: Direct path at `config.paths.data_root`
  - Auth: HPC credentials
  - Contains: CSV files for 22 PCORnet CDM tables

- HPC Blue Filesystem (scratch and output)
  - Connection: Direct path at `config.paths.scratch_root`
  - Auth: HPC credentials
  - Output format: Parquet files (Apache Parquet with Snappy compression)
  - Subdirectories: `hpc-upload/parquet/`, `hpc-upload/logs/`

- Local Filesystem
  - Configuration: `config/paths.toml`
  - Project root: Directory containing `config/` subdirectory
  - Derived outputs: `derived/` directory (configurable in config)

**Data Format:**
- Input: CSV (OneFlorida+ PCORnet CDM)
- Processing intermediate: Polars DataFrames
- Storage output: Apache Parquet (snappy-compressed)
- Reporting: CSV exports with HIPAA suppression (counts 1-10 masked)

**Caching:**
- No external caching service detected
- No Redis/Memcached integration
- In-memory caching via Polars lazy evaluation

## Authentication & Identity

**Auth Provider:**
- Custom (HPC filesystem access)
  - Implementation: Direct filesystem paths resolved via `src/load/config.py`
  - No application-level authentication required (assumes HPC session context)
  - Credentials: Implicit from HPC login session

**Secrets Management:**
- `config/paths.toml` contains paths but not secrets
- All paths are relative to HPC user directories (e.g., `/blue/erin.mobley-hl.bcu`)
- No environment variables for secrets detected

## Monitoring & Observability

**Error Tracking:**
- None detected

**Logging:**
- Standard Python logging (import not detected in examined files)
- Report generation: Markdown files written to `reports/` directory
- Deduplication report: `reports/dedup_report.md` (generated by `scripts/clean_all.py`)
- Data inventory: `file_inventory.csv` (generated by `src/load/convert.py`)

**Reporting:**
- Quality report: `src/report/quality_report.py` - generates markdown summaries
- Site-level tables: `src/report/site_table.py` - summary statistics by site
- Encounter/payer summary: `src/report/encounter_payer_summary.py` - payer-focused patient summaries
- Insurance summary: `scripts/build_insurance_summary.py` - detailed payer analysis

## CI/CD & Deployment

**Hosting:**
- HPC environment (University of Florida HyperGator cluster assumed)
- Job submission: `submit_job.sh` for SLURM batch jobs
- Interactive sessions: `srun --pty bash` for development/testing

**CI Pipeline:**
- Pre-commit hooks: Ruff linting + Pytest (in `.pre-commit-config.yaml`)
- No GitHub Actions or external CI detected
- Local validation: `make ci` target runs lint + test

**Version Control:**
- Git repository detected (`.git/` present)
- Remote repository: Not detected in exploration (likely internal/HPC-hosted)

## Webhooks & Callbacks

**Incoming:**
- None detected

**Outgoing:**
- None detected

## Data Input/Output

**Input Tables (PCORnet CDM):**
All 22 tables from Mailhot cohort (listed in `datastructure.txt`):
- CONDITION, DEMOGRAPHIC, ENCOUNTER, LAB_RESULT, MED_ADMIN, PRESCRIBING, PROVIDER
- TUMOR_REGISTRY (3 files: TUMOR_REGISTRY1/2/3)
- DEATH_CAUSE, DIAGNOSIS, ENROLLMENT, LDS_ADDRESS_HISTORY, OBS_CLIN
- PROCEDURES, VITAL, DEATH, DISPENSING, IMMUNIZATION, OBS_GEN, PRO_CM

**Lookup/Reference Data:**
- `valuesets.csv` - Code value sets for validation
- `Outcomes.csv` - Modality codes and treatment indicator mappings (for HAD_CHEMO, HAD_RADIATION, HAD_SCT flags)
- `STAGE_ajcc_column_values2.csv` - AJCC staging code reference

**Output Artifacts:**
- Parquet files: Cleaned and deduplicated PCORnet CDM tables
- Patient-level summary: `patient_level.parquet` (one row per patient)
- Encounter payer summary: `encounter_payer_summary.parquet` (encounter-derived payer variables)
- Derived outputs: CSV/Parquet reports in `derived/` directory
- Quality reports: Markdown summaries (dedup findings, validation results)

## Environment Configuration

**Required Paths (in `config/paths.toml`):**
- `paths.data_root` - HPC orange filesystem path (read-only source)
- `paths.scratch_root` - HPC blue filesystem path (output)
- `paths.datastructure_path` - Relative path to manifest (default: `datastructure.txt`)
- `paths.valuesets_path` - Relative path to valuesets (default: `valuesets.csv`)

**Optional Output Config (in `config/paths.toml`):**
- `paths.output.parquet_dir` - Parquet output directory (default: `hl-clean/parquet` under scratch_root)
- `paths.output.logs_dir` - Log directory (default: `hl-clean/logs`)
- `paths.output.derived_dir` - Derived outputs directory (default: `derived/` at project root)

**No environment variables required:**
- All configuration via TOML file at `config/paths.toml`
- Paths absolute or relative to project root

## Data Pipeline Dependencies

**Conda Environment:**
- Defined in `environment.yml`
- Python 3.11
- Core: Polars, Pandas>=2.2, PyArrow>=18.0, DuckDB, Jupyter
- Viz: Matplotlib>=3.9, Seaborn>=0.13
- Dev: Ruff, Pre-commit, Pytest, Jinja2>=3.1, Tabulate

**HPC Setup:**
- Mamba for environment management
- Access to conda-forge and defaults channels
- Post-install: Run `mamba env export --no-builds > environment.yml` to capture resolved versions

---

*Integration audit: 2026-03-17*
