---
phase: 08-concerns-remediation
plan: 01
type: execute
wave: 1
depends_on: [07]
files_modified:
  - environment.yml
  - src/load/schema.py
  - src/load/convert.py
  - config/paths.toml
  - scripts/convert_all.py
  - scripts/validate_all.py
  - scripts/clean_all.py
  - scripts/validate_values.py
  - scripts/assemble_clean.py
  - scripts/add_modality_flags.py
  - scripts/build_site_table.py
  - src/report/site_table.py
  - src/report/quality_report.py
  - hpc-upload/config/paths.toml
  - .planning/docs/PATH_RESOLUTION.md
  - .planning/docs/HPC_UPLOAD_SYNC.md
autonomous: true
requirements: [REQ-04, REQ-05]
must_haves:
  truths:
    - "openpyxl is in environment.yml so pd.read_excel in outcomes_flags.py works"
    - "LAB_RESULT filename maps to LAB_RESULT_CM table name across convert, validate, clean, report"
    - "parquet_dir and scratch_root resolution are documented; config supports HPC and local dev"
    - "Every report path uses flag_small_cell or _suppress for counts 1-10"
    - "hpc-upload sync strategy documented"
  artifacts:
    - path: environment.yml
      provides: openpyxl in pip dependencies
      contains: "openpyxl"
    - path: src/load/schema.py
      provides: FILENAME_TO_TABLE_ALIAS for LAB_RESULT -> LAB_RESULT_CM
    - path: .planning/docs/PATH_RESOLUTION.md
      provides: Path resolution documentation
    - path: .planning/docs/HPC_UPLOAD_SYNC.md
      provides: hpc-upload sync strategy
  key_links:
    - from: convert.py
      to: schema.py
      via: filename_to_table_name
      pattern: "FILENAME_TO_TABLE_ALIAS|resolve_table_name"
    - from: _build_table_map (scripts)
      to: LAB_RESULT_CM
      via: resolve_table_name
      pattern: "LAB_RESULT_CM|LAB_RESULT"
---
<objective>
Address Tier 1 (Critical Path) and Tier 2 (Security) concerns from MILESTONE-CONCERNS.md:
- T1: Add openpyxl to environment.yml
- T2: Resolve LAB_RESULT vs LAB_RESULT_CM filename/table name mismatch
- T3: Verify and document path resolution (parquet_dir, scratch_root)
- T4: Audit all report paths for small-cell suppression
- T5: Document hpc-upload sync strategy

Output: Updated config, schema, scripts; documentation in .planning/docs/
</objective>

<execution_context>
Phase 8 follows Phase 7. Pipeline must run end-to-end; path config must be unambiguous; all reports must apply HIPAA small-cell suppression.
</execution_context>

<context>
@.planning/ROADMAP.md (Post-Phase 7 Milestones)
@.planning/milestones/MILESTONE-CONCERNS.md (T1-T5)
@.planning/codebase/CONCERNS.md
@src/load/config.py
@src/load/schema.py
@src/load/convert.py
@src/validate/structural.py (PATID_COL, flag_small_cell, SMALL_CELL_THRESHOLD)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add openpyxl to environment.yml</name>
  <files>environment.yml</files>
  <action>
Add openpyxl to the pip section of environment.yml. outcomes_flags.py uses pd.read_excel() for Outcomes.xlsx; pandas requires openpyxl for .xlsx support. Add under pip: after tabulate, add:
  - openpyxl
Verify: mamba env update (or create) and `python -c "import pandas as pd; pd.read_excel('Outcomes.xlsx', sheet_name='Outcomes', nrows=1)"` succeeds if Outcomes.xlsx exists.
</action>
  <verify>grep -q openpyxl environment.yml && (python -c "import openpyxl; print('OK')" 2>/dev/null || echo "openpyxl present in yml")</verify>
  <done>environment.yml contains openpyxl in pip dependencies; Outcomes.xlsx load works when file present</done>
</task>

<task type="auto">
  <name>Task 2: Resolve LAB_RESULT vs LAB_RESULT_CM</name>
  <files>src/load/schema.py, src/load/convert.py, scripts/convert_all.py, scripts/validate_all.py, scripts/validate_values.py, scripts/clean_all.py, scripts/assemble_clean.py, scripts/add_modality_flags.py, scripts/build_site_table.py</files>
  <action>
datastructure.txt lists LAB_RESULT_Mailhot_V1.csv (not LAB_RESULT_CM). Code expects table name LAB_RESULT_CM. Add alias mapping:

1. In src/load/schema.py:
   - Add FILENAME_TO_TABLE_ALIAS: dict = {"LAB_RESULT": "LAB_RESULT_CM"}
   - Add resolve_table_name(stem: str) -> str: returns FILENAME_TO_TABLE_ALIAS.get(table_part, table_part) where table_part = stem.split("_Mailhot_V1")[0]

2. In src/load/convert.py convert_table():
   - Import resolve_table_name from schema
   - Replace: table_name = stem.split("_Mailhot_V1")[0] with table_name = resolve_table_name(stem)

3. For parquet output filename: keep stem (LAB_RESULT_Mailhot_V1.parquet) so file matches CSV; table_map key becomes LAB_RESULT_CM. Ensure convert writes parquet with stem, but table_map and inventory use resolve_table_name.

4. scripts/convert_all.py: convert_table returns record with correct table_name. For display consistency, replace table_name = filename.split("_Mailhot_V1")[0] with table_name = resolve_table_name(Path(filename).stem).
   - scripts/validate_all.py, validate_values.py, clean_all.py, assemble_clean.py, add_modality_flags.py, build_site_table.py: each has _build_table_map with table_name = stem.split("_Mailhot_V1")[0]. Replace with resolve_table_name(stem).

5. Centralize: Add resolve_table_name to schema.py; all scripts import from src.load.schema.

6. If datastructure.txt lists LAB_RESULT_Mailhot_V1.csv, actual Parquet file will be LAB_RESULT_Mailhot_V1.parquet; table_map["LAB_RESULT_CM"] = parquet_dir / "LAB_RESULT_Mailhot_V1.parquet". Apply alias when building table_map: table_name = resolve_table_name(stem); table_map[table_name] = parquet_path.
</action>
  <verify>python -c "from src.load.schema import resolve_table_name; assert resolve_table_name('LAB_RESULT_Mailhot_V1') == 'LAB_RESULT_CM'; assert resolve_table_name('DEMOGRAPHIC_Mailhot_V1') == 'DEMOGRAPHIC'; print('OK')"</verify>
  <done>LAB_RESULT filename maps to LAB_RESULT_CM in table_map; outcomes_flags, dedup, structural, site_table all receive LAB_RESULT_CM</done>
</task>

<task type="auto">
  <name>Task 3: Verify and document path resolution</name>
  <files>config/paths.toml, hpc-upload/config/paths.toml, src/load/config.py, .planning/docs/PATH_RESOLUTION.md</files>
  <action>
1. Verify current resolution in config.py:
   - parquet_rel = output.get("parquet_dir", "hl-clean/parquet")  # default when absent
   - parquet_dir = scratch_root / parquet_rel  # So: /blue/erin.mobley-hl.bcu + hpc-upload/parquet = /blue/.../hpc-upload/parquet

2. Document in .planning/docs/PATH_RESOLUTION.md:
   - data_root: /orange/erin.mobley-hl.bcu/Mailhot_V1_20250915 (read-only source)
   - scratch_root: /blue/erin.mobley-hl.bcu
   - parquet_dir: scratch_root / output.parquet_dir (from paths.output)
   - Current config: parquet_dir = "hpc-upload/parquet" -> resolved = scratch_root / "hpc-upload/parquet"
   - ROADMAP mentions scratch_root / hl-clean/parquet as alternative; document that output.parquet_dir overrides default
   - For HPC: both configs use same structure; hpc-upload/config/paths.toml may point to project-local paths when run from hpc-upload/ directory

3. Add comments in config/paths.toml explaining: parquet_dir is relative to scratch_root (not project root). Result: /blue/erin.mobley-hl.bcu/hpc-upload/parquet
</action>
  <verify>python -c "from src.load.config import load_config; p = load_config(); print(p.parquet_dir)"</verify>
  <done>PATH_RESOLUTION.md documents data_root, scratch_root, parquet_dir; config comments clarify resolution</done>
</task>

<task type="auto">
  <name>Task 4: Audit report paths for small-cell suppression</name>
  <files>scripts/assemble_clean.py, scripts/clean_all.py, scripts/validate_all.py, scripts/validate_values.py, scripts/build_site_table.py, src/report/site_table.py, src/report/quality_report.py</files>
  <action>
Ensure every report output path uses flag_small_cell (markdown) or _suppress (CSV) for counts 1-10. Audit:

1. assemble_clean.py: Uses flag_small_cell from structural. Verify all count outputs pass through it (lines 134, 145, 154, 163, 177).

2. clean_all.py: Uses flag_small_cell and _suppress. Verify dedup_report, consistency_report, partner_harmonization_report all apply suppression to every count.

3. validate_all.py: Uses flag_small_cell. Grep all numeric outputs; ensure no raw count 1-10 is printed without flag_small_cell.

4. validate_values.py: Grep for counts in report generation. Ensure flag_small_cell applied to all aggregate counts in value_validation.md and related outputs.

5. build_site_table.py: Calls site_table.build_site_summary_table and build_site_summary_html. site_table.py must apply flag_small_cell to all counts in summary (lines 589, 716, 732). Verify build_site_table output (CSV, MD, HTML) contains suppressed counts.

6. quality_report.py: Uses _suppress for CSV; flag_small_cell for markdown. Verify aggregate_dq_metrics consumers apply suppression.

7. Add explicit checklist comment or docstring in each script: "Small-cell: all counts use flag_small_cell/_suppress per REQ-05."

8. If any path prints raw counts: wrap with flag_small_cell(n) or _suppress(n).
</action>
  <verify>rg "flag_small_cell|_suppress" scripts/ src/report/ --count</verify>
  <done>Every report script applies flag_small_cell or _suppress to counts 1-10; no raw small-cell counts in output</done>
</task>

<task type="auto">
  <name>Task 5: Document hpc-upload sync strategy</name>
  <files>.planning/docs/HPC_UPLOAD_SYNC.md</files>
  <action>
Create .planning/docs/HPC_UPLOAD_SYNC.md:

1. Treat hpc-upload/ as deploy copy: scripts/, src/, config/ mirror project root
2. Before HPC runs: sync from project root to hpc-upload (or copy specific dirs)
3. Sync command example: rsync -av --exclude parquet --exclude __pycache__ scripts/ src/ config/ hpc-upload/ (adjust paths for project layout)
4. Alternative: document manual copy of scripts/, src/, config/ from project root into hpc-upload/ before submit_job.sh
5. Note: hpc-upload/parquet and hpc-upload/logs are output dirs; do not overwrite during sync
6. Reference CONCERNS.md: "Duplicate code paths - hpc-upload mirrors project; sync before runs to reduce divergence"
</action>
  <verify>test -f .planning/docs/HPC_UPLOAD_SYNC.md && grep -q "sync\|copy" .planning/docs/HPC_UPLOAD_SYNC.md</verify>
  <done>HPC_UPLOAD_SYNC.md exists with sync steps and CONCERNS reference</done>
</task>

</tasks>

<verification>
- conda env with openpyxl loads Outcomes.xlsx
- resolve_table_name("LAB_RESULT_Mailhot_V1") == "LAB_RESULT_CM"
- PATH_RESOLUTION.md describes parquet_dir = scratch_root / parquet_rel
- All report scripts use flag_small_cell or _suppress
- HPC_UPLOAD_SYNC.md documents sync before HPC runs
</verification>

<success_criteria>
- Pipeline runs without openpyxl ImportError
- LAB_RESULT_CM table found in table_map when datastructure has LAB_RESULT_Mailhot_V1.csv
- Path resolution documented and consistent
- No raw counts 1-10 in any report output
- Developer knows how to sync hpc-upload before HPC submission
</success_criteria>
