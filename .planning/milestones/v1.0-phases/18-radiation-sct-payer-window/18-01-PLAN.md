---
phase: 18-radiation-sct-payer-window
plan: 01
type: execute
wave: 2
depends_on: [17]
files_modified:
  - src/report/encounter_payer_summary.py
  - scripts/build_insurance_summary.py
  - docs/CODEBOOK.md
  - docs/PAYER_VARIABLES_AND_CATEGORIES.md
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - "Payer-at-date window for first DX and first/last chemo is ±120 days (expanded by 30 from 90)"
    - "PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION = mode payer category among encounters in [first_rad, last_rad]"
    - "PAYER_CATEGORY_MOST_FREQUENT_AT_SCT = mode payer category among encounters in [first_sct, last_sct]"
  artifacts:
    - path: src/report/encounter_payer_summary.py
      provides: "PAYER_AT_TREATMENT_WINDOW_DAYS=120; _get_radiation_dates, _get_sct_dates; MOST_FREQUENT_AT_RADIATION, MOST_FREQUENT_AT_SCT"
    - path: derived/encounter_payer_summary.parquet
      provides: "Columns PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION, PAYER_CATEGORY_MOST_FREQUENT_AT_SCT"
  key_links:
    - from: encounter_payer_summary.py
      to: "TUMOR_REGISTRY (DT_RAD), PROCEDURES (PX, PX_DATE)"
      via: "Radiation and SCT date helpers; enc_radiation, enc_sct blocks"
---

<objective>
Add most common insurance at radiation encounters and at stem cell transplant (SCT) encounters; expand the first/last insurance-around-treatments window by 30 days (90 → 120). New variables: PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION, PAYER_CATEGORY_MOST_FREQUENT_AT_SCT. Treatment-related _payer_at_date calls use ±120 days.
</objective>

<context>
@.planning/phases/18-radiation-sct-payer-window/18-RESEARCH.md
@src/report/encounter_payer_summary.py
@src/report/quality_report.py
</context>

<prerequisites>
- Phase 17 complete. encounter_payer_summary has effective_payer, dual_eligible, "Dual eligible" category level.
- TUMOR_REGISTRY tables may have DT_RAD; PROCEDURES has PX, PX_DATE. Radiation CPTs (77401, 77402, 77407, 77412, 77427) and SCT CPTs (38240, 38241, 38242, 38230, 38232) per validate_values/quality_report.
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Expand payer-at-date window by 30 days (90 → 120)</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- Define constant PAYER_AT_TREATMENT_WINDOW_DAYS = 120 in encounter_payer_summary.py.
- For all _payer_at_date calls that are "around treatments" (first HL DX, first chemo, last chemo), pass window_days=PAYER_AT_TREATMENT_WINDOW_DAYS instead of default 90. So: _payer_at_date(enc_path, first_dx, "FIRST_HL_DX_DATE", PAYER_AT_TREATMENT_WINDOW_DAYS), and same for FIRST_CHEMO_DATE and LAST_CHEMO_DATE.
- Document in CODEBOOK/PAYER_VARIABLES that the window for payer at first DX and at first/last chemo is ±120 days.
  </action>
  <verify>Grep PAYER_AT_TREATMENT_WINDOW_DAYS and _payer_at_date call sites; confirm 120 is passed for first_dx, first_chemo, last_chemo.</verify>
  <done>Treatment-related payer-at-date uses ±120 days.</done>
</task>

<task type="auto">
  <name>Task 2: Add _get_radiation_dates and _get_sct_dates</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- Define RADIATION_CPTS = frozenset({"77401", "77402", "77407", "77412", "77427"}) and SCT_CPTS = frozenset({"38240", "38241", "38242", "38230", "38232"}) (or import SCT_CPTS from quality_report if it matches; ensure radiation subset is separate). Use same CPT sets for PROCEDURES filter.
- _get_radiation_dates(table_map, ids): (1) From TUMOR_REGISTRY tables, collect DT_RAD (parse dates like _get_chemo_dates). (2) From PROCEDURES, filter PX in RADIATION_CPTS, PX_DATE not null, PATID in ids; collect (PATID, PX_DATE as _rad_d). (3) Concatenate all date series, group_by PATID, agg min/max as FIRST_RADIATION_DATE, LAST_RADIATION_DATE. Return DataFrame or None if empty.
- _get_sct_dates(table_map, ids): From PROCEDURES, filter PX in SCT_CPTS, PX_DATE not null, PATID in ids; group_by PATID, agg min(PX_DATE), max(PX_DATE) as FIRST_SCT_DATE, LAST_SCT_DATE. Return DataFrame or None if empty.
  </action>
  <verify>Helpers return expected columns; handle missing TR or PROCEDURES gracefully.</verify>
  <done>Radiation and SCT date helpers implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION and PAYER_CATEGORY_MOST_FREQUENT_AT_SCT</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- After enc_chemo block (or in same chemo branch if we have ids): call _get_radiation_dates(table_map, ids). If not None, run enc_radiation block: same pattern as enc_chemo — scan ENCOUNTER with effective_payer and dual_eligible, filter to PATID in radiation, join radiation (FIRST_RADIATION_DATE, LAST_RADIATION_DATE), filter ADMIT_DATE in [first, last] and _valid, compute PAYER_CATEGORY per row via _payer_category_from_effective_and_dual, group_by PATID + PAYER_CATEGORY, count, sort by count desc, group_by PATID first → PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION. Join to base. Else add null column.
- Same for _get_sct_dates and enc_sct → PAYER_CATEGORY_MOST_FREQUENT_AT_SCT.
- Add both columns to empty_schema and to final .select().
  </action>
  <verify>Schema includes new columns; run build_encounter_payer_summary({}) and confirm no error.</verify>
  <done>MOST_FREQUENT_AT_RADIATION and MOST_FREQUENT_AT_SCT in output.</done>
</task>

<task type="auto">
  <name>Task 4: Docs and build_insurance_summary</name>
  <files>docs/CODEBOOK.md, docs/PAYER_VARIABLES_AND_CATEGORIES.md, scripts/build_insurance_summary.py</files>
  <action>
- CODEBOOK (Section 6a) and PAYER_VARIABLES_AND_CATEGORIES: Add PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION and PAYER_CATEGORY_MOST_FREQUENT_AT_SCT (definition: mode payer category among encounters with ADMIT_DATE in [first_rad, last_rad] or [first_sct, last_sct]; radiation dates from TR DT_RAD + PROCEDURES radiation CPTs; SCT from PROCEDURES SCT CPTs). Note that payer at first DX and at first/last chemo uses ±120 days.
- build_insurance_summary.py: Add "PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION" and "PAYER_CATEGORY_MOST_FREQUENT_AT_SCT" to INSURANCE_VARS so they appear in encounter_payer_summary.csv and in report tables.
  </action>
  <verify>Docs and report script list the new variables.</verify>
  <done>Documentation and report coverage updated.</done>
</task>

</tasks>

<verification>
- Run assemble_clean (or build_encounter_payer_summary with real table_map); confirm parquet has PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION and PAYER_CATEGORY_MOST_FREQUENT_AT_SCT.
- Run build_insurance_summary; confirm new variables in encounter_payer_summary.csv.
</verification>

<success_criteria>
- Payer at first DX, first chemo, last chemo use ±120-day window.
- PAYER_CATEGORY_MOST_FREQUENT_AT_RADIATION and PAYER_CATEGORY_MOST_FREQUENT_AT_SCT exist; logic mirrors MOST_FREQUENT_AT_CHEMO (encounters in treatment window, category with "Dual eligible" when dual_eligible, mode per patient).
- Docs and insurance summary report updated.
</success_criteria>

<output>
After completion, create .planning/phases/18-radiation-sct-payer-window/18-01-SUMMARY.md with what was implemented.
</output>
