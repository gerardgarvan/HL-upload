---
phase: 13-flag-code-audit-pipeline-verification
plan: 01
type: execute
wave: 1
depends_on: [12]
files_modified:
  - src/clean/flags_diagnosis_provider.py
  - tests/test_flags_diagnosis_provider.py
  - docs/FLAG_CODES.md
  - scripts/pipeline_smoke_test.py
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - "FLAG_HL_DX uses same 149-code set as cohort (ALL_HL_CODES/ALL_HL_NORMALIZED)"
    - "FLAG_HL_DX = 0 for excluded codes (201.3x, C81.5x, C81.6x)"
    - "Regression tests assert flag–cohort alignment"
    - "Survivorship code sources documented"
    - "Pipeline smoke test runs convert_all → validate_all → clean_all → assemble_clean"
  artifacts:
    - path: src/clean/flags_diagnosis_provider.py
      provides: FLAG_HL_DX using cohort ALL_HL_CODES
      min_lines: 100
    - path: docs/FLAG_CODES.md
      provides: survivorship and HL code documentation
      min_lines: 30
  key_links:
    - from: src/clean/flags_diagnosis_provider.py
      to: src/validate/cohort.py
      via: import ALL_HL_CODES, ICD9_HL_CODES, ICD10_HL_CODES, ICD9_HL_NORMALIZED, ICD10_HL_NORMALIZED
      pattern: "from src.validate.cohort import"
---

<objective>
Phase 13 verifies that Phase 12 flag variables use correct, consistent code logic aligned with cohort verification, and that the pipeline runs end-to-end.

**Critical fix:** FLAG_HL_DX currently uses prefix matching (201*, C81*) and flags 201.3x and C81.5x/C81.6x, which cohort excludes. Align FLAG_HL_DX with cohort's exact 149-code set (ALL_HL_CODES/ALL_HL_NORMALIZED).

**Output:** Aligned flags_diagnosis_provider.py, regression tests, FLAG_CODES.md, pipeline smoke test.
</objective>

<execution_context>
Phase 13 follows Phase 12. Cohort excludes ICD-9 201.3x and ICD-10 C81.5x/6x (149 codes total). FLAG_HL_DX must use the same set so flag and cohort verification agree.
</execution_context>

<context>
@.planning/phases/13-flag-code-audit-pipeline-verification/13-RESEARCH.md
@src/validate/cohort.py
@src/clean/flags_diagnosis_provider.py
@scripts/pipeline_smoke_test.py
</context>

<prerequisites>
- Phase 12 complete (flags_diagnosis_provider.py with FLAG_HL_DX, FLAG_SURVIVORSHIP_DX, FLAG_CANCER_PROVIDER)
- cohort.py exports ALL_HL_CODES, ICD9_HL_CODES, ICD10_HL_CODES, ICD9_HL_NORMALIZED, ICD10_HL_NORMALIZED
- convert_all, validate_all, clean_all, assemble_clean scripts exist
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Align FLAG_HL_DX with cohort code set</name>
  <files>src/clean/flags_diagnosis_provider.py</files>
  <action>
In src/clean/flags_diagnosis_provider.py:

1. **Import cohort constants:**
   from src.validate.cohort import ICD9_HL_CODES, ICD10_HL_CODES, ICD9_HL_NORMALIZED, ICD10_HL_NORMALIZED

2. **Replace prefix-based FLAG_HL_DX logic** with exact-set matching:
   - Keep dx_norm = pl.col("DX").fill_null("").str.to_uppercase().str.replace_all(r"\.", "")
   - For ICD-9: DX in ICD9_HL_CODES (dotted) OR dx_norm in ICD9_HL_NORMALIZED
   - For ICD-10: DX in ICD10_HL_CODES (dotted) OR dx_norm in ICD10_HL_NORMALIZED
   - Use pl.when().then().otherwise() with _is_icd9_type() / _is_icd10_type()

3. **Match both formats:** Data can be dotted (C81.10) or undotted (C8110). Check BOTH:
   pl.col("DX").is_in(ICD9_HL_CODES) | dx_norm.is_in(ICD9_HL_NORMALIZED) for ICD-9
   pl.col("DX").is_in(ICD10_HL_CODES) | dx_norm.is_in(ICD10_HL_NORMALIZED) for ICD-10

4. **Update docstring:** "FLAG_HL_DX: 1 when DX is in cohort HL set (149 codes, excludes 201.3x and C81.5x/C81.6x)."

Do NOT use starts_with("201") or starts_with("C81") — use the exact code sets from cohort.
</action>
  <verify>python -c "from src.clean.flags_diagnosis_provider import add_diagnosis_flags; df = __import__('polars').DataFrame({'ID':['P'], 'DX':['201.30','C81.50'], 'DX_TYPE':['09','10'], 'DX_DATE':[None,None]}); r = add_diagnosis_flags(df); assert r['FLAG_HL_DX'].sum() == 0, 'Excluded codes must not be flagged'"</verify>
  <done>FLAG_HL_DX uses ALL_HL_CODES/ALL_HL_NORMALIZED; excluded codes (201.3x, C81.5x/6x) yield FLAG_HL_DX=0</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for flag–cohort alignment</name>
  <files>tests/test_flags_diagnosis_provider.py</files>
  <action>
Add to tests/test_flags_diagnosis_provider.py:

1. **test_flag_hl_dx_matches_cohort_codes:**
   - Sample 3 ICD-9 codes from ICD9_HL_CODES and 3 ICD-10 codes from ICD10_HL_CODES
   - Build DF with DX (dotted or normalized), DX_TYPE 09/10, ID, DX_DATE=None
   - Call add_diagnosis_flags(df)
   - Assert FLAG_HL_DX.sum() == 6 (all flagged)

2. **test_flag_hl_dx_excluded_codes_not_flagged:**
   - DF with DX=["201.30", "201.35", "C81.50", "C81.60"], DX_TYPE=["09","09","10","10"]
   - Call add_diagnosis_flags(df)
   - Assert FLAG_HL_DX.sum() == 0

3. **Update test_add_diagnosis_flags_hl** if needed: 201.00 and C81.10 remain in cohort set; Z85.3 stays 0. Test should still pass after Task 1.

Import from src.validate.cohort: ICD9_HL_CODES, ICD10_HL_CODES (for sampling).
</action>
  <verify>python -m pytest tests/test_flags_diagnosis_provider.py -v -k "hl_dx"</verify>
  <done>test_flag_hl_dx_matches_cohort_codes and test_flag_hl_dx_excluded_codes_not_flagged pass</done>
</task>

<task type="auto">
  <name>Task 3: Document survivorship and HL flag code sources</name>
  <files>docs/FLAG_CODES.md</files>
  <action>
Create docs/FLAG_CODES.md:

1. **HL codes (FLAG_HL_DX):** Single source of truth: src/validate/cohort.py. 149 codes (77 ICD-10, 72 ICD-9). Excludes ICD-9 201.3x, ICD-10 C81.5x/6x.

2. **Survivorship codes (FLAG_SURVIVORSHIP_DX):** Source: flags_diagnosis_provider.py. Document:
   - Exact ICD-10: V87.41/42/43/46, Z92.21-25, Z92.3 (personal history chemotherapy/immunotherapy/etc.)
   - Exact ICD-9: V15.3 (personal history)
   - Prefix ICD-10: Z08 (follow-up after malignancy), Z85 (personal history malignancy)
   - Note: cite standard ICD references; V15.3 semantics vary — verify against study protocol if needed.

3. **Oncology keywords (FLAG_CANCER_PROVIDER):** Source: flags_diagnosis_provider.py. Document list (oncology, medical oncology, radiation oncology, hematology/oncology, pediatric oncology).

Keep doc concise (30–50 lines). Create docs/ dir if missing.
</action>
  <verify>test -f docs/FLAG_CODES.md && grep -q "FLAG_HL_DX" docs/FLAG_CODES.md</verify>
  <done>docs/FLAG_CODES.md exists and documents HL, survivorship, oncology code sources</done>
</task>

<task type="auto">
  <name>Task 4: Add pipeline smoke test (convert → validate → clean → assemble)</name>
  <files>scripts/smoke_test.py</files>
  <action>
Extend scripts/smoke_test.py to optionally run full pipeline verification:

**Option A (preferred):** Add a new script scripts/pipeline_smoke_test.py that:
1. Loads config (same pattern as smoke_test.py)
2. Runs in sequence: convert_all.main() → validate_all.main() → clean_all.main() → assemble_clean.main()
3. Uses subprocess.run or direct main() imports
4. After each step, checks: convert → parquet_dir has at least DEMOGRAPHIC.parquet; validate → reports/structural_validation.md exists; clean → parquet_dir has cleaned files; assemble → parquet_clean/ and derived/patient_level.parquet exist
5. Prints per-step OK/fail
6. Exit 0 if all pass; non-zero on first failure
7. Accepts [config/paths.toml] as optional arg

**Option B:** Add --pipeline flag to smoke_test.py that runs the four scripts after the existing DEMOGRAPHIC-only checks.

Choose Option A if smoke_test.py is already long; Option B if minimal. Either way: the test requires parquet and reports to exist (or will create them). Document that full run may take minutes on real data; for CI, config may point to minimal/sample data.

Implementation: Use subprocess.run(["python", "scripts/convert_all.py", str(config_path)]) with check=True, or import and call main() directly. Prefer subprocess for isolation.
</action>
  <verify>python scripts/pipeline_smoke_test.py config/paths.toml — exits 0 when config and data available</verify>
  <done>Pipeline smoke test runs convert_all → validate_all → clean_all → assemble_clean and verifies outputs exist</done>
</task>

</tasks>

<verification>
- python -c "from src.clean.flags_diagnosis_provider import add_diagnosis_flags; from src.validate.cohort import ICD9_HL_CODES; df = __import__('polars').DataFrame({'ID':['P']*2,'DX':list(ICD9_HL_CODES)[:2],'DX_TYPE':['09']*2,'DX_DATE':[None]*2}); assert add_diagnosis_flags(df)['FLAG_HL_DX'].sum()==2"
- python -m pytest tests/test_flags_diagnosis_provider.py -v
- test -f docs/FLAG_CODES.md
- python scripts/pipeline_smoke_test.py [config/paths.toml] (or python scripts/smoke_test.py --pipeline)
</verification>

<success_criteria>
- FLAG_HL_DX uses cohort ALL_HL_CODES (149 codes); excludes 201.3x, C81.5x/6x
- test_flag_hl_dx_matches_cohort_codes and test_flag_hl_dx_excluded_codes_not_flagged pass
- docs/FLAG_CODES.md documents survivorship, HL, and oncology code sources
- Pipeline smoke test runs convert_all → validate_all → clean_all → assemble_clean and asserts outputs
</success_criteria>

<traceability>
Research: 13-RESEARCH.md
- Critical inconsistency (prefix vs exact) addressed: FLAG_HL_DX aligned with cohort
- Single source of truth: import from cohort.py
- Regression tests enforce alignment
- Survivorship documentation added
- Pipeline verification extends smoke_test pattern
</traceability>

<effort_estimate>
~2.5 days (Claude execution: ~60–90 min)
</effort_estimate>

<output>
After completion, create .planning/phases/13-flag-code-audit-pipeline-verification/13-01-SUMMARY.md
</output>
