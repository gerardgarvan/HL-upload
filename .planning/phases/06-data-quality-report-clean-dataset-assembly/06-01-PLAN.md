---
phase: 06-data-quality-report-clean-dataset-assembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/report/quality_report.py
  - src/report/__init__.py
autonomous: true
requirements: [REQ-03, REQ-04, REQ-05, REQ-06]
must_haves:
  truths:
    - "Derived variables (AGE_AT_HL_DX, AGE_BAND, HL_SUBTYPE, FIRST_HL_DX_DATE, FIRST_HL_TX_DATE, DX_TO_TX_DAYS, PAYER_AT_DX, INSURANCE_CONTINUITY, REGION) can be computed from Parquet tables"
    - "DQ aggregation functions read Phase 3-5 outputs and produce metrics for completeness, conformance, plausibility, persistence"
    - "All report counts use small cell suppression (1-10 → '-' or 'N ⚠')"
  artifacts:
    - path: src/report/quality_report.py
      provides: Derived var computation, DQ aggregation
      min_lines: 150
    - path: src/report/__init__.py
      provides: Package init
  key_links:
    - from: src/report/quality_report.py
      to: scripts/validate_values.py
      via: _compute_hl_timeline pattern
      pattern: "ALL_HL_CODES|ALL_HL_NORMALIZED|detect_dx_format"
    - from: src/report/quality_report.py
      to: src/validate/structural.py
      via: flag_small_cell
      pattern: "flag_small_cell"
---

<objective>
Create the derived-variables module and DQ aggregation functions for Phase 6.

Purpose: Provide reusable functions for patient-level derived variables (AGE_AT_HL_DX, AGE_BAND, HL_SUBTYPE, FIRST_HL_DX_DATE, FIRST_HL_TX_DATE, DX_TO_TX_DAYS, PAYER_AT_DX, INSURANCE_CONTINUITY, REGION) and DQ report aggregation across Phases 3-5 outputs. This plan produces the core logic; Plan 02 uses it in the entry-point script.
Output: src/report/quality_report.py, src/report/__init__.py
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-quality-report-clean-dataset-assembly/06-RESEARCH.md
@scripts/validate_values.py
@src/validate/structural.py
@src/validate/cohort.py
@src/validate/values.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create derived-variables module</name>
  <files>src/report/__init__.py, src/report/quality_report.py</files>
  <action>
Create src/report/__init__.py (empty or minimal) and src/report/quality_report.py.

Implement build_patient_level_derived(table_map: dict[str, Path]) -> pl.DataFrame that:
1. **Reuse _compute_hl_timeline logic** from validate_values.py (lines 261-342): FIRST_HL_DX_DATE from DIAGNOSIS (C81*/201* via detect_dx_format, ALL_HL_CODES/ALL_HL_NORMALIZED); FIRST_HL_TX_DATE from PROCEDURES (SCT/radiation CPTs), PRESCRIBING (RX_ORDER_DATE), TUMOR_REGISTRY (DT_SURG, DT_RAD, DT_CHEMO with TR date parsing: %Y.%m.%d, %m/%d/%Y, %d%b%Y, %Y%m%d); DX_TO_TX_DAYS = FIRST_TX_DATE - FIRST_HL_DX_DATE.
2. **Add AGE_AT_HL_DX** from DEMOGRAPHIC.BIRTH_DATE + FIRST_HL_DX_DATE; masked BIRTH_DATE (MASKED_BIRTH_DATE=date(1900,1,1)) → fold into 65+ band per HL-EDA.
3. **Add AGE_BAND**: &lt;21, 21-39, 40-64, 65+ (masked → 65+).
4. **Add HL_SUBTYPE** from C81.x 4th character: C81.0 nodular LP, C81.1 nodular sclerosis, C81.2 mixed cellularity, etc. (use mapping from cohort/valuesets if available; otherwise concise label).
5. **Add PAYER_AT_DX**: ENCOUNTER.PAYER_TYPE_PRIMARY from encounter closest to FIRST_HL_DX_DATE (within ±90 days). BND/UCI/UMI have no payer — null is expected.
6. **Add INSURANCE_CONTINUITY**: flag=1 if gap &gt;30 days in ENROLLMENT covering treatment window (FIRST_HL_DX_DATE to min(FIRST_HL_TX_DATE + 365, last encounter)). Reuse enrollment coverage pattern from clean/harmonize. INSURANCE_CONTINUITY = gaps &gt;30 days in treatment window per RESEARCH.
7. **Add REGION**: LDS_ADDRESS_HISTORY.ADDRESS_STATE → Southeast (AL, AR, FL, GA, KY, LA, MS, NC, SC, TN, VA, WV) vs Other vs Unknown (null/NI/UN).
8. Import from src.validate.structural: PATID_COL, SMALL_CELL_THRESHOLD, TUMOR_REGISTRY_TABLES, flag_small_cell; from src.validate.cohort: detect_dx_format, ALL_HL_CODES, ALL_HL_NORMALIZED; from src.validate.values: MASKED_BIRTH_DATE.
9. Use PATID_COL="ID" throughout. Use .is_in(code_set) with implode() for HL patient filtering where applicable (HPC learning: is_in implode()).
10. Return one row per HL patient (ID, plus all derived cols). Patients without FIRST_HL_DX_DATE are excluded.
</action>
  <verify>python -c "from src.report.quality_report import build_patient_level_derived; print('OK')"</verify>
  <done>build_patient_level_derived() exists and returns DataFrame with ID and derived columns; imports resolve</done>
</task>

<task type="auto">
  <name>Task 2: Add DQ aggregation functions</name>
  <files>src/report/quality_report.py</files>
  <action>
Add to quality_report.py:

1. **aggregate_dq_metrics(table_map, reports_dir)** → dict with keys: completeness, conformance, plausibility, persistence.
   - completeness: Read reports/completeness_by_partner.csv if exists; otherwise compute per-column non-null rate by SOURCE from Parquet (group_by SOURCE, agg per column). Stratify by partner.
   - conformance: Aggregate invalid-code counts from Phase 4 value_validation or re-scan _val_* flag columns; summarize by table/check.
   - plausibility: Aggregate out-of-range counts, temporal violations from Phase 4 reports or _val_* flags.
   - persistence: Data volume over time (e.g., encounter counts by year/month, by SOURCE) from ENCOUNTER or DIAGNOSIS; detect drop-offs.

2. **generate_cleaning_decisions_content()** → list[str] (markdown lines) documenting:
   - Value set validation (valuesets.csv; NI/UN/OT handling)
   - Plausibility ranges (VITAL_RANGES, HL_LAB_RANGES from values.py)
   - Temporal rules (ICD10_TRANSITION 2015-10-01; DX_TO_TX grace)
   - Dedup keys (DEDUP_KEYS from clean/dedup)
   - Partner flags (ICD_MAPPED, CLAIMS_ONLY, DEATH_ONLY)
   - Masked values (BIRTH_DATE=1900-01-01, AGE_AT_DIAGNOSIS=200; 65+ band)
   - TUMOR_REGISTRY date formats (MM/DD/YYYY, YYYY.MM.DD fallback)
   - INSURANCE_CONTINUITY: gaps &gt;30 days in treatment window
   - SMALL_CELL_THRESHOLD=10

3. All counts in aggregation must pass through flag_small_cell or equivalent (1-10 → "N ⚠" or "-" for CSVs) per HIPAA.
4. Do NOT create markdown files here — return structured data; Plan 02 will write reports.
</action>
  <verify>python -c "from src.report.quality_report import aggregate_dq_metrics, generate_cleaning_decisions_content; print('OK')"</verify>
  <done>aggregate_dq_metrics and generate_cleaning_decisions_content exist and return expected structures</done>
</task>

</tasks>

<verification>
- src/report/quality_report.py imports from structural, cohort, values, clean
- build_patient_level_derived produces HL patient rows with all 9 derived vars
- aggregate_dq_metrics returns dict with 4 dimension keys
- generate_cleaning_decisions_content returns non-empty list of markdown lines
- No markdown/CSV files written by this module (Plan 02 writes)
</verification>

<success_criteria>
- build_patient_level_derived() produces patient-level DataFrame with derived variables
- aggregate_dq_metrics() produces DQ metrics for four dimensions
- generate_cleaning_decisions_content() produces CLEANING_DECISIONS content
- All exports usable by Plan 02 entry-point script
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-quality-report-clean-dataset-assembly/06-01-SUMMARY.md`
</output>
