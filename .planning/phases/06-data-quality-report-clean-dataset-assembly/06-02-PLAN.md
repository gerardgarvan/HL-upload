---
phase: 06-data-quality-report-clean-dataset-assembly
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/assemble_clean.py
  - reports/DATA_QUALITY_REPORT.md
  - reports/CLEANING_DECISIONS.md
  - reports/figures/
autonomous: true
requirements: [REQ-03, REQ-04, REQ-05, REQ-06]
must_haves:
  truths:
    - "User can run scripts/assemble_clean.py to produce parquet_clean/ and derived/patient_level.parquet"
    - "DATA_QUALITY_REPORT.md exists with completeness, conformance, plausibility, persistence sections stratified by partner"
    - "CLEANING_DECISIONS.md documents all rules, thresholds, rationale"
    - "Small cell suppression applied to all aggregate counts in reports"
  artifacts:
    - path: scripts/assemble_clean.py
      provides: Phase 6 entry point
      min_lines: 100
    - path: reports/DATA_QUALITY_REPORT.md
      provides: Comprehensive DQ report
    - path: reports/CLEANING_DECISIONS.md
      provides: Cleaning rules documentation
    - path: data/derived/patient_level.parquet
      provides: Patient-level derived variables
  key_links:
    - from: scripts/assemble_clean.py
      to: src/report/quality_report.py
      via: build_patient_level_derived, aggregate_dq_metrics, generate_cleaning_decisions_content
      pattern: "build_patient_level_derived|aggregate_dq_metrics"
    - from: scripts/assemble_clean.py
      to: parquet_clean
      via: write_parquet
      pattern: "write_parquet|compression.*snappy"
---

<objective>
Create the Phase 6 entry-point script, assemble clean Parquet files, and generate final reports.

Purpose: Orchestrate derived-variable assembly, copy Parquet tables to parquet_clean/, write patient_level.parquet, and produce DATA_QUALITY_REPORT.md and CLEANING_DECISIONS.md. All outputs HIPAA-compliant (small cell suppression).
Output: scripts/assemble_clean.py, reports/DATA_QUALITY_REPORT.md, reports/CLEANING_DECISIONS.md, reports/figures/, data/parquet_clean/*.parquet, data/derived/patient_level.parquet
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-quality-report-clean-dataset-assembly/06-RESEARCH.md
@.planning/phases/06-data-quality-report-clean-dataset-assembly/06-01-PLAN.md
@scripts/clean_all.py
@scripts/validate_values.py
@src/report/quality_report.py
@src/load/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assemble_clean entry point and Parquet assembly</name>
  <files>scripts/assemble_clean.py</files>
  <action>
Create scripts/assemble_clean.py as Phase 6 entry point.

1. **Config:** Derive parquet_clean_dir = paths.parquet_dir.parent / "parquet_clean" and derived_dir = paths.parquet_dir.parent / "derived" (do not modify config). RESEARCH: "derive as parquet_dir.parent / 'parquet_clean'".

2. **Main flow (mirror clean_all.py/validate_values.py):**
   - load_config, parse_datastructure, _build_table_map(table_filenames, paths.parquet_dir)
   - reports_dir = PROJECT_ROOT / "reports"
   - parquet_clean_dir = paths.parquet_dir.parent / "parquet_clean"
   - derived_dir = paths.parquet_dir.parent / "derived"
   - derived_dir.mkdir(parents=True, exist_ok=True)
   - parquet_clean_dir.mkdir(parents=True, exist_ok=True)

3. **Copy Parquet to parquet_clean:** For each table in table_map, read from parquet_dir, write to parquet_clean_dir with compression="snappy". Retain all Phase 4/5 flag columns. Do NOT modify schema. This preserves validated+flagged data as final analysis-ready output.

4. **Build patient_level.parquet:** Call build_patient_level_derived(table_map) from quality_report. Write to derived_dir / "patient_level.parquet" with df.write_parquet(path, compression="snappy").

5. **PATID_COL="ID"** throughout. Use snappy compression per HPC learnings.

6. **Usage:** python scripts/assemble_clean.py [config/paths.toml]

7. Print summary: tables copied, patient_level rows, output paths.
</action>
  <verify>python scripts/assemble_clean.py config/paths.toml (or equivalent) runs; parquet_clean/ and derived/patient_level.parquet exist</verify>
  <done>assemble_clean.py executes; Parquet files copied to parquet_clean; patient_level.parquet written</done>
</task>

<task type="auto">
  <name>Task 2: Generate DATA_QUALITY_REPORT and CLEANING_DECISIONS</name>
  <files>scripts/assemble_clean.py, reports/DATA_QUALITY_REPORT.md, reports/CLEANING_DECISIONS.md, reports/figures/</files>
  <action>
Extend assemble_clean.py to generate reports after assembly:

1. **DATA_QUALITY_REPORT.md:**
   - Call aggregate_dq_metrics(table_map, reports_dir) from quality_report.
   - Build markdown with sections: (1) Overview (timestamp, parquet_dir, tables), (2) Completeness (per-field non-null by partner; use flag_small_cell for all counts), (3) Conformance (invalid code summaries), (4) Plausibility (out-of-range, temporal violations), (5) Persistence (volume over time by partner).
   - Stratify by SOURCE (partner) throughout.
   - Use flag_small_cell from structural.py for every aggregate count (1-10 → "N ⚠").
   - Optional: completeness heatmap (tables × partners) — use matplotlib/seaborn if figures dir exists; or markdown table. Save to reports/figures/ as PNG if generating. RESEARCH: "Generate PNGs via matplotlib/seaborn; save to reports/figures/".
   - Write to reports_dir / "DATA_QUALITY_REPORT.md".

2. **CLEANING_DECISIONS.md:**
   - Call generate_cleaning_decisions_content() from quality_report.
   - Write lines to reports_dir / "CLEANING_DECISIONS.md".
   - Add header: title, generated timestamp, reference to Phase 3-5 reports.

3. **reports/figures/:** Create directory; save completeness heatmap and temporal coverage plot if feasible (matplotlib/seaborn). Reference in DATA_QUALITY_REPORT.md. If figures add significant complexity, a markdown table is acceptable per RESEARCH "embedded markdown tables" alternative.

4. Use _suppress() for CSV exports (1-10 → "-"); flag_small_cell for markdown per Phase 4/5 pattern.
</action>
  <verify>DATA_QUALITY_REPORT.md and CLEANING_DECISIONS.md exist; all counts use small cell suppression</verify>
  <done>Both reports generated; figures dir created; small cell suppression applied throughout</done>
</task>

</tasks>

<verification>
- assemble_clean.py runs end-to-end
- parquet_clean/ contains all 22 tables (or those present in parquet_dir)
- derived/patient_level.parquet has ID + derived columns
- DATA_QUALITY_REPORT.md has 4 DQ dimensions
- CLEANING_DECISIONS.md documents rules
- No counts 1-10 displayed without suppression
</verification>

<success_criteria>
- scripts/assemble_clean.py produces parquet_clean/, derived/patient_level.parquet, DATA_QUALITY_REPORT.md, CLEANING_DECISIONS.md
- Reports stratified by SOURCE (partner)
- HIPAA-compliant small cell suppression on all outputs
- Phase 6 complete: analysis-ready datasets and documentation
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-quality-report-clean-dataset-assembly/06-02-SUMMARY.md`
</output>
