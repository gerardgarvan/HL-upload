---
phase: 10-ci-precommit-hardening
plan: 01
type: execute
wave: 1
depends_on: [09]
files_modified:
  - .pre-commit-config.yaml
  - Makefile
  - .github/workflows/ci.yml
  - .planning/codebase/CONCERNS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-05, REQ-06]
must_haves:
  truths:
    - "Pre-commit runs ruff check, ruff format, and pytest before each commit"
    - "make test runs pytest; make lint runs ruff; make ci runs both"
    - "GitHub Actions or documented CI config exists to run tests and lint"
    - "CONCERNS.md marks openpyxl, LAB_RESULT, path resolution, small-cell audit, Outcomes/date docs, pytest, ruff, incremental convert as resolved"
    - "STATE.md reflects Phases 8-9 complete and Phase 10"
  artifacts:
    - path: .pre-commit-config.yaml
      provides: Pre-commit hooks for ruff and pytest
    - path: Makefile
      provides: make test, make lint, make ci targets
    - path: .github/workflows/ci.yml
      provides: CI pipeline (or documented alternative)
    - path: .planning/codebase/CONCERNS.md
      provides: Updated concerns with resolved markers
    - path: .planning/STATE.md
      provides: Current project state
  key_links:
    - from: .pre-commit-config.yaml
      to: ruff, pytest
      via: pre-commit hooks
    - from: Makefile
      to: pytest, ruff
      via: test and lint targets
---

<objective>
Add CI, pre-commit, and pipeline hardening to the HL Data Loading & Cleaning project:
- Pre-commit hooks run ruff (check + format) and pytest before commit
- CI placeholder: minimal GitHub Actions config (or Makefile target for CI)
- Makefile with make test and make lint for developers
- Update CONCERNS.md to mark Phase 8-9 addressed items as resolved
- Update STATE.md to reflect Phases 8-9 complete and Phase 10

Output: .pre-commit-config.yaml, Makefile, CI config, updated CONCERNS.md and STATE.md.
</objective>

<execution_context>
Phase 10 follows Phase 9 (Concerns Remediation complete). Phases 8-9 addressed all 11 MILESTONE-CONCERNS tasks. This phase adds developer workflow tooling and marks concerns as resolved. Do not duplicate Phase 8-9 work.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/milestones/MILESTONE-CONCERNS.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-commit hooks</name>
  <files>.pre-commit-config.yaml, environment.yml</files>
  <action>
1. Add pre-commit to environment.yml pip section (if not present).

2. Create .pre-commit-config.yaml at project root with:
   - default_language_version for python
   - repo: https://github.com/astral-sh/ruff-pre-commit
     - ruff hook (check, args: [--fix])
     - ruff-format hook
   - repo: local
     - pytest hook: entry "pytest", pass_filenames: false, language: system
     - Run tests in tests/ directory

3. Ensure pre-commit install works: `pre-commit install`

Reference: https://docs.astral.sh/ruff/integration/#pre-commit
Use ruff-pre-commit rev compatible with project's ruff version (check pyproject.toml or use latest stable).
</action>
  <verify>test -f .pre-commit-config.yaml && grep -q "ruff" .pre-commit-config.yaml && grep -q "pytest" .pre-commit-config.yaml</verify>
  <done>.pre-commit-config.yaml exists; ruff (check + format) and pytest run via pre-commit before commit</done>
</task>

<task type="auto">
  <name>Task 2: Add Makefile with test, lint, ci targets</name>
  <files>Makefile</files>
  <action>
Create Makefile at project root with:

1. **test** — Run pytest: `python -m pytest tests/ -v`
2. **lint** — Run ruff check and format: `ruff check .` and `ruff format --check .` (or `ruff check . && ruff format .` for fix)
3. **ci** — Run both: `$(MAKE) lint && $(MAKE) test`

Use .PHONY for test, lint, ci. Ensure commands work from project root. Use `python -m pytest` for consistency with conda/venv.
</action>
  <verify>cd "c:\cygwin64\home\Owner\Data loading and cleaing" && make test 2>/dev/null || true; make lint 2>/dev/null || true</verify>
  <done>make test runs pytest; make lint runs ruff; make ci runs both</done>
</task>

<task type="auto">
  <name>Task 3: Add CI placeholder</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create minimal GitHub Actions workflow at .github/workflows/ci.yml:

- name: CI
- on: push, pull_request (branches: main, master — pick one that exists, or both)
- jobs:
  - lint: runs ruff check . && ruff format --check .
  - test: runs python -m pytest tests/ -v
- Use matrix or single job; keep it minimal
- Python version: 3.11 (from pyproject.toml)
- Setup: checkout, setup-python, pip install -e . (or pip install -r requirements.txt if exists), or conda if preferred — use pip + pyproject.toml for simplicity; install pytest, ruff, polars, pyarrow, openpyxl, pandas, tomli for tests to pass

Alternative: If project uses GitLab, create .gitlab-ci.yml instead with equivalent stages. Default to GitHub Actions as most common.
</action>
  <verify>test -f .github/workflows/ci.yml && grep -q "pytest" .github/workflows/ci.yml && grep -q "ruff" .github/workflows/ci.yml</verify>
  <done>GitHub Actions ci.yml exists; runs lint and test on push/PR</done>
</task>

<task type="auto">
  <name>Task 4: Update CONCERNS.md — mark resolved items</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
Update CONCERNS.md to mark the following as resolved (add "**Resolved (Phase 8):**" or "**Resolved (Phase 9):**" and brief note):

1. **openpyxl** (Dependencies at Risk) — Resolved Phase 8: added to environment.yml
2. **LAB_RESULT vs LAB_RESULT_CM** (Tech Debt) — Resolved Phase 8: FILENAME_TO_TABLE_ALIAS in schema.py
3. **Path resolution / parquet_dir** (Tech Debt, Data Paths) — Resolved Phase 8: PATH_RESOLUTION.md
4. **Small-cell audit** (Security) — Resolved Phase 8: all report paths use flag_small_cell or _suppress
5. **Outcomes.xlsx schema** (Fragile Areas) — Resolved Phase 9: OUTCOMES_XLSX_SCHEMA.md
6. **Date parsing fallbacks** (Fragile Areas) — Resolved Phase 9: DATE_PARSING_FALLBACKS.md
7. **pytest** (Missing Critical Features, Test Coverage) — Resolved Phase 9: tests/ with unit and integration tests
8. **ruff / pyproject.toml** (Missing Critical Features) — Resolved Phase 9: ruff + pyproject.toml
9. **Incremental convert** (Missing Critical Features) — Resolved Phase 9: convert_all.py skip logic

Add a "Resolved" subsection or inline markers per section. Preserve original concern text for reference; add resolved note.
</action>
  <verify>grep -q "Resolved" .planning/codebase/CONCERNS.md && grep -q "Phase 8\|Phase 9" .planning/codebase/CONCERNS.md</verify>
  <done>CONCERNS.md marks openpyxl, LAB_RESULT, path resolution, small-cell audit, Outcomes/date docs, pytest, ruff, incremental convert as resolved</done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md and ROADMAP</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
1. Update .planning/STATE.md:
   - Current Phase: 10 (or 09 if Phase 10 not yet executed)
   - Add Phase 8 and Phase 9 to progress as complete (100%)
   - Update last session if applicable
   - Add Phase 10 to roadmap section if present

2. Update .planning/ROADMAP.md:
   - Add Phase 10 section after "Post-Phase 7 Milestones" (or after Concerns Remediation):
     - **Phase 10: CI, Pre-commit, and Pipeline Hardening**
     - Goal: Pre-commit hooks, CI placeholder, Makefile/run_tests, CONCERNS resolved markers, STATE current
     - Plans: 1 plan (10-01-PLAN.md)
     - Dependencies: Phase 9
</action>
  <verify>grep -q "Phase 8\|Phase 9\|Phase 10" .planning/STATE.md; grep -q "Phase 10" .planning/ROADMAP.md</verify>
  <done>STATE.md reflects Phases 8-9 complete; ROADMAP includes Phase 10</done>
</task>

</tasks>

<verification>
- pre-commit run --all-files (or manual ruff + pytest) passes
- make test and make lint run successfully
- .github/workflows/ci.yml exists and is valid YAML
- CONCERNS.md contains resolved markers for Phase 8-9 items
- STATE.md and ROADMAP.md updated
</verification>

<success_criteria>
- Developer can run make test or make lint; pre-commit blocks commits with lint/test failures
- CI config exists for GitHub (or documented alternative)
- CONCERNS.md accurately reflects addressed items
- STATE.md and ROADMAP.md are current
</success_criteria>

<output>
After completion, create .planning/phases/10-ci-precommit-hardening/10-01-SUMMARY.md
</output>
