---
phase: 03-structural-validation-hl-cohort-verification
verified: 2026-02-27T18:10:00Z
status: passed
score: 14/14 must-haves verified
human_verification:
  - test: "Run `python scripts/validate_all.py` on HPC and inspect reports/structural_validation.md"
    expected: "Heatmap uses Unicode block characters (█▓▒░·○), report has 5 sections, tables are aligned"
    why_human: "Report readability and visual quality require human eye on actual data output"
  - test: "Verify HL cohort union count against expected ~9,331"
    expected: "Console prints union count; mismatch investigation in report Section 5 if count differs"
    why_human: "Actual cohort count depends on real data; can only verify algorithm correctness statically"
  - test: "Check completeness_by_partner.csv and cohort_summary.csv open correctly in spreadsheet"
    expected: "CSV files have correct headers and parseable data"
    why_human: "Runtime output files only exist after execution against HPC data"
---

# Phase 3: Structural Validation & HL Cohort Verification — Verification Report

**Phase Goal:** Validate table structure against PCORnet CDM v6.1, verify key integrity, and confirm the HL cohort definition.
**Verified:** 2026-02-27T18:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 22 table schemas compared against DatasetCoverPage expected columns with extra/missing columns documented | ✓ VERIFIED | `validate_table_schema()` compares actual vs expected via `pl.read_parquet_schema()`. `parse_cover_page()` handles BOM. TUMOR_REGISTRY gets column-count-only validation. Report Section 1 generated by `_section_schema()` |
| 2 | PATID (ID) uniqueness verified in DEMOGRAPHIC | ✓ VERIFIED | `check_patid_uniqueness()` uses `pl.scan_parquet()` to count total vs unique IDs. Returns `is_unique` bool. Report Section 2 shows counts |
| 3 | Orphan patient IDs flagged in every clinical table (not dropped) | ✓ VERIFIED | `check_patid_integrity()` uses anti-join with String-cast keys across all 20 PATID_LINKED_TABLES. Returns orphan count/pct, no records dropped |
| 4 | Orphan ENCOUNTERIDs flagged in event tables, with CHP LAB_RESULT_CM skipped | ✓ VERIFIED | `check_encounterid_integrity()` with `skip_partner="CHP"` for LAB_RESULT_CM (validate_all.py:655). Skips tables without ENCOUNTERID column. 10 ENCOUNTER_LINKED_TABLES checked |
| 5 | Per-column completeness rates computed for every table, stratified by partner (SOURCE) | ✓ VERIFIED | `completeness_by_partner()` groups by SOURCE (fallback SITE), computes `1 - null_count/len`, unpivots to long form. CSV output at `reports/completeness_by_partner.csv` |
| 6 | Missing values classified as NI/UN/OT/empty/null with counts per column | ✓ VERIFIED | `classify_missing_values()` counts all 5 categories per string column. Report Section 4 includes handling rules |
| 7 | structural_validation.md report readable with heatmap-style completeness display | ✓ VERIFIED | `completeness_heatmap_symbol()` maps 6 thresholds to Unicode blocks (█▓▒░·○). Report uses `{symbol} {pct:.0%}` format. Per-table heatmap capped at 20 columns |
| 8 | HL cohort identified using exact set of 149 ICD codes (77 ICD-10 + 72 ICD-9) — not prefix matching | ✓ VERIFIED | `ICD10_HL_CODES` = 7 subtypes × 11 sites = 77. `ICD9_HL_CODES` = 8 subtypes × 9 sites = 72. Total 149. Uses `is_in(code_set)` exact matching, not prefix. Both dotted and normalized forms |
| 9 | Both Method A (2+ distinct DX_DATEs) and Method B (2+ distinct ADMIT_DATEs) computed and differences reported | ✓ VERIFIED | Stage 2 groups by ID, filters `n_unique(DX_DATE) >= 2`. Stage 3 joins ENCOUNTER on ENCOUNTERID, filters `n_unique(ADMIT_DATE) >= 2`. Stage 4 computes set operations (a_only, b_only, intersection, union) |
| 10 | Cohort count compared against expected ~9,331 with per-partner and per-ICD-version mismatch investigation | ✓ VERIFIED | `_section_cohort()` triggers "Count Mismatch Investigation" when `union != 9331`. Includes per-partner breakdown and per-year DX_DATE breakdown |
| 11 | ICD version flag assigned per patient: ICD9_ONLY, ICD10_ONLY, or BOTH — with AMS/UMI mapping caveat documented | ✓ VERIFIED | Stage 5 classifies each record as ICD10/ICD9, groups by patient. Caveat string: "AMS and UMI mapped ICD-9→ICD-10..." in return dict and report note |
| 12 | DX_TYPE mismatches reported but not used to exclude records | ✓ VERIFIED | `check_dx_type_mismatches()` returns mismatch DataFrame for reporting. No filter applied to exclude. Report note: "Per locked decision: DX_TYPE mismatches reported but not used for exclusion" |
| 13 | Enrollment cross-check completed: coverage rates, uncovered patients by partner, coverage period gaps | ✓ VERIFIED | `enrollment_crosscheck()` inner-joins and anti-joins cohort IDs against ENROLLMENT. Uncovered by partner via DEMOGRAPHIC join. Coverage periods: earliest_start, latest_end, median_duration per partner |
| 14 | cohort_summary.csv and structural_validation.md Section 5 (Cohort) produced | ✓ VERIFIED | `build_cohort_summary_df()` creates per-patient DataFrame (ID, in_method_a, in_method_b, icd_flag, has_enrollment, partner, dates). `_section_cohort()` generates Section 5. Deviation from plan: Section 5 not 4 (documented in SUMMARY) |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/validate/__init__.py` | Package marker for validate module | ✓ VERIFIED | Exists, empty file (correct for package marker) |
| `src/validate/structural.py` | Schema comparison, key integrity, completeness profiling functions | ✓ VERIFIED | 540 lines, 9 functions (parse_cover_page, validate_table_schema, check_patid_uniqueness, check_patid_integrity, check_encounterid_integrity, completeness_by_partner, classify_missing_values, completeness_heatmap_symbol, flag_small_cell), 7+ constants |
| `src/validate/cohort.py` | HL cohort verification with exact code matching, dual-date methods, ICD flags, enrollment cross-check | ✓ VERIFIED | 563 lines, 6 functions (normalize_dx, detect_dx_format, check_dx_type_mismatches, verify_hl_cohort, enrollment_crosscheck, build_cohort_summary_df), 149 ICD codes with dotted + normalized forms |
| `scripts/validate_all.py` | Entry point orchestrating structural validation and report generation | ✓ VERIFIED | 847 lines, imports from both structural and cohort modules, 5 report section builders, main() orchestrates all checks, `__main__` block with error handling |
| `reports/structural_validation.md` | Comprehensive validation report (5 sections) | ✓ VERIFIED (code) | Generated at runtime by `_section_schema()`, `_section_integrity()`, `_section_completeness()`, `_section_missing()`, `_section_cohort()`. File created on HPC execution |
| `reports/completeness_by_partner.csv` | Granular completeness data per table+column+partner | ✓ VERIFIED (code) | `comp_df.write_csv(csv_path)` at line 711. Long-form: [SOURCE, row_count, column, completeness, table] |
| `reports/cohort_summary.csv` | HL cohort breakdown by ICD version, partner, date range | ✓ VERIFIED (code) | `cohort_summary_df.write_csv(cohort_csv_path)` at line 764. Per-patient: [ID, in_method_a, in_method_b, icd_flag, has_enrollment, partner, earliest_dx_date, latest_dx_date, n_hl_dx_records] |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/validate/structural.py` | Parquet files on disk | `pl.read_parquet_schema()` / `pl.scan_parquet()` | ✓ WIRED | 9 occurrences across 6 functions. No eager `pl.read_parquet()` used |
| `scripts/validate_all.py` | `src/load/config.py` | `from src.load.config import load_config` | ✓ WIRED | Line 23. Called in `main()` at line 561 to resolve paths |
| `scripts/validate_all.py` | `src/validate/structural.py` | Imports all validation functions | ✓ WIRED | Lines 25-40. All 9 functions + 5 constants imported and used in main() |
| `scripts/validate_all.py` | `src/validate/cohort.py` | Imports cohort verification functions | ✓ WIRED | Lines 41-46. ALL_HL_CODES, verify_hl_cohort, enrollment_crosscheck, build_cohort_summary_df imported and used |
| `src/validate/cohort.py` | DIAGNOSIS Parquet | `pl.scan_parquet().filter(is_in(ALL_HL_CODES))` | ✓ WIRED | Lines 165-168. Lazy scan, format-adaptive match via _DX_MATCH column, then filter with is_in(code_set) |
| `src/validate/cohort.py` | ENCOUNTER Parquet | Join on ENCOUNTERID for ADMIT_DATE | ✓ WIRED | Line 214. Inner join on ENCOUNTERID (cast to String) to get ADMIT_DATE for Method B |
| `src/validate/cohort.py` | ENROLLMENT Parquet | Anti-join on ID for uncovered patients | ✓ WIRED | Line 386. Anti-join to find HL patients without enrollment records |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REQ-01 | 03-01, 03-02 | Load 22 large CSV files as fast as possible (Parquet reads) | ✓ SATISFIED | All validation uses `pl.scan_parquet()` (lazy) and `pl.read_parquet_schema()` (schema-only). Zero CSV reads. Zero eager `pl.read_parquet()` calls in validation modules |
| REQ-03 | 03-01, 03-02 | Clean data for HL insurance inequities analysis | ✓ SATISFIED | HL cohort verified with 149 exact ICD codes. PAYER_TYPE_PRIMARY completeness highlighted in report Section 3. Per-partner stratification throughout. Enrollment cross-check identifies coverage gaps |
| REQ-04 | 03-01, 03-02 | Run on HiPerGator HPC | ✓ SATISFIED | Config-driven paths via `load_config()` (paths.toml). No hardcoded paths. Script docstring: "Designed for HPC interactive sessions". PROJECT_ROOT-relative imports |
| REQ-05 | 03-01, 03-02 | HIPAA-compliant data handling | ✓ SATISFIED | `flag_small_cell()` marks counts 1-10 with ⚠ (SMALL_CELL_THRESHOLD=10). Used in all integrity tables and cohort counts. No data exported off HPC. Reports contain flagged counts, not raw PHI |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/validate/structural.py` | 119 | `return {}` | ℹ️ Info | Legitimate empty-dict return in `parse_cover_page()` error handler — not a stub |

No TODO, FIXME, PLACEHOLDER, or HACK comments found across any Phase 3 files. No stub implementations detected. No eager Parquet reads where lazy was required.

### Human Verification Required

### 1. Report Visual Quality

**Test:** Run `python scripts/validate_all.py` on HPC and open `reports/structural_validation.md`
**Expected:** Unicode heatmap symbols render correctly (█▓▒░·○), all 5 sections present, tables properly aligned, per-partner completeness readable
**Why human:** Report readability and visual quality of Unicode characters require human inspection of actual rendered output

### 2. Cohort Count Validation

**Test:** After running on HPC, verify union cohort count in console output and report Section 5
**Expected:** Union count ≈ 9,331 patients; if different, mismatch investigation section shows per-partner and per-year breakdown
**Why human:** Actual cohort count depends on real data; static analysis verifies algorithm correctness but not numerical result

### 3. CSV Output Integrity

**Test:** Open `reports/completeness_by_partner.csv` and `reports/cohort_summary.csv` in a spreadsheet tool
**Expected:** Correct headers, parseable numeric values, no truncation
**Why human:** Runtime-generated files only exist after HPC execution

### Gaps Summary

No gaps found. All 14 observable truths verified through static code analysis. All 7 code artifacts exist, are substantive (not stubs), and are properly wired. All 7 key links confirmed via import/usage checks. All 4 requirements (REQ-01, REQ-03, REQ-04, REQ-05) satisfied with evidence.

One minor deviation documented: HL Cohort Verification is Section 5 (not Section 4 as originally planned) because Plan 01 created 4 sections instead of 3. This was auto-fixed during execution and documented in SUMMARY.md.

---

_Verified: 2026-02-27T18:10:00Z_
_Verifier: Claude (gsd-verifier)_
