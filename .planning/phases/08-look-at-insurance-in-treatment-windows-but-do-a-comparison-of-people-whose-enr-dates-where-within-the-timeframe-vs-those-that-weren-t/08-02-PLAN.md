---
phase: 08-look-at-insurance-in-treatment-windows-but-do-a-comparison-of-people-whose-enr-dates-where-within-the-timeframe-vs-those-that-weren-t
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - scripts/build_insurance_presentation.py
autonomous: false
requirements:
  - D-22
  - D-18

must_haves:
  truths:
    - "PowerPoint presentation includes all Phase 8 comparison tables as new slides after existing Phase 5/6 slides"
    - "Unknown post-treatment encounter breakdown appears as a slide in the PowerPoint"
    - "New slides use same UF Health branding (blue #003087, orange #FA4616) and native tables as existing slides"
    - "Presentation remains re-runnable and reads from reports/ CSV files"
  artifacts:
    - path: "scripts/build_insurance_presentation.py"
      provides: "Extended PowerPoint generation with Phase 8 slides added"
      contains: "_add_table_slide"
    - path: "reports/insurance_tables_*.pptx"
      provides: "Updated PowerPoint with Phase 5+6+8 slides"
  key_links:
    - from: "scripts/build_insurance_presentation.py"
      to: "reports/insurance_enr_comparison/*.csv"
      via: "_read_csv_safe for Phase 8 CSV data"
      pattern: "insurance_enr_comparison"
---

<objective>
Extend the Phase 7 PowerPoint presentation script to include Phase 8 enrollment comparison tables and Unknown diagnostic breakdown as new slides.

Purpose: All insurance analysis tables (Phase 5 treatment, Phase 6 post-treatment, Phase 8 enrollment comparison) are assembled in one professional PowerPoint deck for stakeholder review.

Output: Updated `scripts/build_insurance_presentation.py` that adds 5 new slides (DX comparison, Chemo comparison, Radiation comparison, SCT comparison, Unknown breakdown) to the existing 9-slide deck, producing a 14-slide presentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-look-at-insurance-in-treatment-windows-but-do-a-comparison-of-people-whose-enr-dates-where-within-the-timeframe-vs-those-that-weren-t/08-CONTEXT.md
@.planning/phases/08-look-at-insurance-in-treatment-windows-but-do-a-comparison-of-people-whose-enr-dates-where-within-the-timeframe-vs-those-that-weren-t/08-01-SUMMARY.md

@scripts/build_insurance_presentation.py
@scripts/build_insurance_enr_comparison.py

<interfaces>
<!-- Key contracts from existing PowerPoint script -->

From scripts/build_insurance_presentation.py:
```python
# UF Health brand colors
UF_BLUE = RGBColor.from_string("003087")
UF_ORANGE = RGBColor.from_string("FA4616")
WHITE = RGBColor(255, 255, 255)
LIGHT_BLUE = RGBColor(0xCC, 0xD5, 0xEA)
LIGHT_ORANGE = RGBColor(0xFD, 0xD9, 0xCC)
DARK_TEXT = RGBColor(0x33, 0x33, 0x33)

def _add_table_slide(prs, title, subtitle, df, n_pct_columns, payer_col="Payer Category"):
    """Add a table slide to the presentation. Uses blank layout (index 6)."""

def _read_csv_safe(path: Path) -> pl.DataFrame | None:
    """Read CSV file with error handling."""

def _extract_cohort_size(df: pl.DataFrame, column_name: str) -> int:
    """Extract cohort size by summing the N column."""

def _create_title_slide(prs, total_n, chemo_n, radiation_n, sct_n):
    """Create title slide with cohort sizes."""
```

Phase 8 CSV column names (from Plan 01):
```
# DX comparison CSV columns ending in (N_Pct):
"ENR Covers Window (N_Pct)", "ENR Does Not Cover (N_Pct)"

# Chemo comparison CSV columns ending in (N_Pct):
"First Chemo ENR Covers (N_Pct)", "First Chemo ENR Gap (N_Pct)",
"Last Chemo ENR Covers (N_Pct)", "Last Chemo ENR Gap (N_Pct)"

# Unknown breakdown CSV columns ending in (N_Pct):
"N Patients (N_Pct)"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 slides to PowerPoint presentation script</name>
  <files>scripts/build_insurance_presentation.py</files>
  <read_first>
    - scripts/build_insurance_presentation.py (current 532-line script: _add_table_slide signature, _read_csv_safe, _create_title_slide, slide ordering, output path pattern)
    - scripts/build_insurance_enr_comparison.py (output CSV file names, column naming conventions for N_Pct columns)
    - reports/insurance_enr_comparison/dx_enr_comparison.csv (verify actual column names to use as n_pct_columns)
    - reports/insurance_enr_comparison/chemo_enr_comparison.csv (verify actual column names for 4-column tables)
    - reports/insurance_enr_comparison/unknown_post_tx_encounter_breakdown.csv (verify actual column names)
    - .planning/phases/08-look-at-insurance-in-treatment-windows-but-do-a-comparison-of-people-whose-enr-dates-where-within-the-timeframe-vs-those-that-weren-t/08-CONTEXT.md (D-22: add slides to existing presentation)
  </read_first>
  <action>
Modify `scripts/build_insurance_presentation.py` to add Phase 8 slides after the existing Phase 5/6 slides. Changes required:

**1. Update module docstring** (line 1-21):
- Change slide count from 9 to 14 in the description
- Add slide descriptions for Phase 8:
  - Slide 10: DX enrollment comparison (Phase 8)
  - Slide 11: Chemotherapy enrollment comparison (Phase 8)
  - Slide 12: Radiation enrollment comparison (Phase 8)
  - Slide 13: SCT enrollment comparison (Phase 8)
  - Slide 14: Unknown post-treatment encounter breakdown (Phase 8)

**2. Add Phase 8 CSV paths** in `main()` after the Phase 6 CSV path definitions (after line 328):
```python
# Phase 8 CSV files (5 tables)
phase8_dir = PROJECT_ROOT / "reports" / "insurance_enr_comparison"
dx_enr_path = phase8_dir / "dx_enr_comparison.csv"
chemo_enr_path = phase8_dir / "chemo_enr_comparison.csv"
radiation_enr_path = phase8_dir / "radiation_enr_comparison.csv"
sct_enr_path = phase8_dir / "sct_enr_comparison.csv"
unknown_breakdown_path = phase8_dir / "unknown_post_tx_encounter_breakdown.csv"
```

**3. Read Phase 8 CSVs** after the Phase 6 CSV reads (after line 344):
```python
# Read Phase 8 CSVs
dx_enr_df = _read_csv_safe(dx_enr_path)
chemo_enr_df = _read_csv_safe(chemo_enr_path)
radiation_enr_df = _read_csv_safe(radiation_enr_path)
sct_enr_df = _read_csv_safe(sct_enr_path)
unknown_breakdown_df = _read_csv_safe(unknown_breakdown_path)
```

**4. Update available_count** to include Phase 8 DataFrames in the count (update the `all_dfs` list to add the 5 new ones).

**5. Update Phase 8 report directory print** in the print section:
```python
print(f"  Phase 8 dir: {phase8_dir}")
```

**6. Add Phase 8 slides** after Slide 9 (SCT Post-Treatment, after line ~510). For each Phase 8 table:

**Slide 10: DX Enrollment Comparison** (after SCT post-treatment slide):
```python
# Slide 10: DX Enrollment Comparison (Phase 8)
if dx_enr_df is not None:
    n_pct_cols = [c for c in dx_enr_df.columns if c.endswith(" (N_Pct)")]
    if n_pct_cols:
        _add_table_slide(
            prs,
            "Diagnosis — Insurance by Enrollment Coverage",
            "Payer at first HL diagnosis: patients with vs without enrollment covering +/-30 day window",
            dx_enr_df,
            n_pct_cols
        )
        slide_count += 1
        print(f"  Slide {slide_count}: DX enrollment comparison")
```

**Slide 11: Chemo Enrollment Comparison:**
```python
if chemo_enr_df is not None:
    n_pct_cols = [c for c in chemo_enr_df.columns if c.endswith(" (N_Pct)")]
    if n_pct_cols:
        _add_table_slide(
            prs,
            "Chemotherapy — Insurance by Enrollment Coverage",
            "Payer at first/last chemo: patients with vs without enrollment covering +/-30 day window",
            chemo_enr_df,
            n_pct_cols
        )
        slide_count += 1
        print(f"  Slide {slide_count}: Chemo enrollment comparison")
```

**Slide 12: Radiation Enrollment Comparison:**
```python
if radiation_enr_df is not None:
    n_pct_cols = [c for c in radiation_enr_df.columns if c.endswith(" (N_Pct)")]
    if n_pct_cols:
        _add_table_slide(
            prs,
            "Radiation — Insurance by Enrollment Coverage",
            "Payer at first/last radiation: patients with vs without enrollment covering +/-30 day window",
            radiation_enr_df,
            n_pct_cols
        )
        slide_count += 1
        print(f"  Slide {slide_count}: Radiation enrollment comparison")
```

**Slide 13: SCT Enrollment Comparison:**
```python
if sct_enr_df is not None:
    n_pct_cols = [c for c in sct_enr_df.columns if c.endswith(" (N_Pct)")]
    if n_pct_cols:
        _add_table_slide(
            prs,
            "SCT — Insurance by Enrollment Coverage",
            "Payer at first/last SCT: patients with vs without enrollment covering +/-30 day window",
            sct_enr_df,
            n_pct_cols
        )
        slide_count += 1
        print(f"  Slide {slide_count}: SCT enrollment comparison")
```

**Slide 14: Unknown Post-Treatment Encounter Breakdown (per D-18):**
```python
if unknown_breakdown_df is not None:
    n_pct_cols = [c for c in unknown_breakdown_df.columns if c.endswith(" (N_Pct)")]
    if n_pct_cols:
        # Use "Encounter Count Bin" as payer_col since it's the row label
        _add_table_slide(
            prs,
            "Unknown Post-Treatment Payer — Encounter Breakdown",
            "Patients with Unknown post-treatment payer: how many encounters exist after last treatment?",
            unknown_breakdown_df,
            n_pct_cols,
            payer_col="Encounter Count Bin"
        )
        slide_count += 1
        print(f"  Slide {slide_count}: Unknown post-treatment breakdown")
```

**7. Adjust column widths for wider Phase 8 tables:**
The existing `_add_table_slide` uses `Inches(2.2)` for the first column and distributes the remaining `9.0 - 2.2 = 6.8` inches across data columns. For Phase 8's 4-column treatment tables (5 total columns: payer + 4 data), this gives ~1.7 inches per data column, which is adequate. The existing function handles this correctly via `data_col_width = remaining_width / len(n_pct_columns)`. No change needed to `_add_table_slide`.

No other changes needed to existing functions. The `_add_table_slide` function already handles variable numbers of columns.
  </action>
  <verify>
    <automated>python scripts/build_insurance_presentation.py && python -c "from pptx import Presentation; prs = Presentation(sorted(__import__('pathlib').Path('reports').glob('insurance_tables_*.pptx'))[-1]); n = len(prs.slides); print(f'Slides: {n}'); assert n >= 10, f'Expected >=10 slides, got {n}'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build_insurance_presentation.py contains `phase8_dir = PROJECT_ROOT / "reports" / "insurance_enr_comparison"`
    - scripts/build_insurance_presentation.py contains `dx_enr_path`
    - scripts/build_insurance_presentation.py contains `chemo_enr_path`
    - scripts/build_insurance_presentation.py contains `unknown_breakdown_path`
    - scripts/build_insurance_presentation.py contains string "Insurance by Enrollment Coverage"
    - scripts/build_insurance_presentation.py contains string "Unknown Post-Treatment Payer"
    - scripts/build_insurance_presentation.py contains `payer_col="Encounter Count Bin"` (for Unknown breakdown slide)
    - `python scripts/build_insurance_presentation.py` exits with code 0
    - Output .pptx has at least 10 slides (9 original + at least 1 Phase 8)
  </acceptance_criteria>
  <done>
    PowerPoint presentation includes Phase 8 enrollment comparison slides and Unknown breakdown slide, using same UF Health branding and native table format as existing slides. Script remains re-runnable and reads from reports/ CSVs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of PowerPoint with Phase 8 slides</name>
  <files>reports/insurance_tables_*.pptx</files>
  <read_first>
    - scripts/build_insurance_presentation.py
  </read_first>
  <action>
    Present the generated PowerPoint for human review. The user will open the latest reports/insurance_tables_YYYY-MM-DD.pptx and verify:
    1. Slides 10-14 exist (the new Phase 8 slides)
    2. DX slide has 2 data columns with enrollment coverage split
    3. Chemo/Radiation/SCT slides have 4 data columns (First Covers/Gap + Last Covers/Gap)
    4. Unknown breakdown slide has encounter count bins
    5. All new slides use UF Health branding (blue headers, alternating blue/orange row tints)
    6. Column headers are readable, N values match data
  </action>
  <verify>User confirms output quality by typing "approved"</verify>
  <done>User has visually verified that PowerPoint slides are correct, complete, and match UF Health branding</done>
</task>

</tasks>

<verification>
1. `python scripts/build_insurance_presentation.py` runs without errors
2. Output .pptx contains Phase 8 slides with enrollment comparison data
3. Unknown breakdown slide uses "Encounter Count Bin" as row labels (not "Payer Category")
4. All slides readable with correct UF branding
</verification>

<success_criteria>
- PowerPoint has at least 10 slides (original 9 + Phase 8 additions)
- Phase 8 slides use same _add_table_slide function with UF branding
- Unknown breakdown slide correctly uses "Encounter Count Bin" column
- Script remains re-runnable from CSV files
</success_criteria>

<output>
After completion, create `.planning/phases/08-look-at-insurance-in-treatment-windows-but-do-a-comparison-of-people-whose-enr-dates-where-within-the-timeframe-vs-those-that-weren-t/08-02-SUMMARY.md`
</output>
