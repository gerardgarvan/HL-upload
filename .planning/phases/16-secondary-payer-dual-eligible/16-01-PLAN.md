---
phase: 16-secondary-payer-dual-eligible
plan: 01
type: execute
wave: 2
depends_on: [15]
files_modified:
  - src/report/encounter_payer_summary.py
  - docs/CODEBOOK.md
  - docs/PAYER_VARIABLES_AND_CATEGORIES.md
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - "Effective payer per encounter: primary if valid, else secondary if valid, else null (sentinel = null, '', NI, UN, OT; 99/9999 choice documented)"
    - "All encounter-payer summary variables (N_ENCOUNTERS_WITH_PAYER, categories, at first DX/chemo, transition) use effective payer"
    - "Encounter-level dual_eligible = 1 when (Medicare+Medicaid) or (Medicaid+Medicare) or primary/secondary in {14, 141, 142}; else 0"
    - "Patient-level DUAL_ELIGIBLE column in encounter_payer_summary.parquet (1 if any encounter dual-eligible)"
    - "If PAYER_TYPE_SECONDARY missing from ENCOUNTER schema: effective_payer = primary only; dual_eligible = 0 when cannot compute"
  artifacts:
    - path: src/report/encounter_payer_summary.py
      provides: "effective_payer expression, dual_eligible logic, all aggregations on effective_payer"
    - path: derived/encounter_payer_summary.parquet
      provides: "Output with DUAL_ELIGIBLE column; all payer vars from effective payer"
    - path: docs/CODEBOOK.md
      provides: "Effective payer and DUAL_ELIGIBLE documentation"
    - path: docs/PAYER_VARIABLES_AND_CATEGORIES.md
      provides: "Variable definitions using effective payer; sentinel and 99/9999 choice"
  key_links:
    - from: src/report/encounter_payer_summary.py
      to: "ENCOUNTER parquet"
      via: "Schema check for PAYER_TYPE_SECONDARY; effective_payer and dual_eligible per row"
    - from: src/report/encounter_payer_summary.py
      to: "_payer_at_date and enc_chemo block"
      via: "Use effective_payer (not PAYER_TYPE_PRIMARY) for _raw_payer and category"
---

<objective>
Incorporate secondary payer and dual-eligible into the encounter-payer summary: (1) effective payer per encounter with sentinel fallback (primary → secondary → null); (2) encounter-level and patient-level dual-eligible flags; (3) all existing payer variables computed from effective payer; (4) documentation updates. When PAYER_TYPE_SECONDARY is missing from ENCOUNTER schema, effective_payer = primary only and dual_eligible = 0 when not computable.

Purpose: Support insurance inequities analysis with correct payer attribution and dual-eligibility stratification.
Output: Updated src/report/encounter_payer_summary.py, derived/encounter_payer_summary.parquet (with DUAL_ELIGIBLE), updated docs/CODEBOOK.md and docs/PAYER_VARIABLES_AND_CATEGORIES.md.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-secondary-payer-dual-eligible/16-RESEARCH.md
@src/report/encounter_payer_summary.py
@docs/CODEBOOK.md
@docs/PAYER_VARIABLES_AND_CATEGORIES.md
</context>

<prerequisites>
- Phase 15 complete (insurance summary tables/figures); encounter_payer_summary.parquet produced by Phase 14.
- ENCOUNTER parquet has PAYER_TYPE_PRIMARY; PAYER_TYPE_SECONDARY may or may not be present (check schema at runtime).
- valuesets.csv (or project equivalent) documents PAYER_TYPE codes; dual-eligible codes 14, 141, 142; code 41 = Corrections Federal (not dual-eligible).
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Add effective_payer and sentinel fallback in encounter_payer_summary.py</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- Define sentinel set: null, "", NI, UN, OT (reuse/extend INVALID_PAYER). Optionally include 99/9999 — implement as configurable constant (e.g. INCLUDE_99_AS_SENTINEL = False) and document the choice in CODEBOOK/PAYER_VARIABLES_AND_CATEGORIES.
- Add helper or expressions for "valid" primary and "valid" secondary: non-null, non-empty, not in sentinel set. If PAYER_TYPE_SECONDARY is not in ENCOUNTER schema (check via pl.read_parquet_schema(enc_path).names()), treat as no secondary: effective_payer = primary with current valid check only.
- In the first ENCOUNTER scan in build_encounter_payer_summary: select PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY (if present). Compute effective_payer per row: pl.when(valid_primary).then(pl.col("PAYER_TYPE_PRIMARY")).when(valid_secondary).then(pl.col("PAYER_TYPE_SECONDARY")).otherwise(pl.lit(None)). alias("effective_payer"). Add _valid = effective_payer is non-null and not in sentinel (or reuse valid check on effective_payer).
- Do not duplicate sentinel logic elsewhere; all downstream logic will use the single effective_payer column from this scan. Reference: 16-RESEARCH.md "Effective Payer", "Order of Operations", "Code References".
  </action>
  <verify>Grep for effective_payer and sentinel/INVALID_PAYER in encounter_payer_summary.py; confirm one place computes effective_payer; run script that builds encounter_payer_summary and ensure no schema/name errors.</verify>
  <done>effective_payer column exists per encounter row; sentinel list (and optional 99/9999) documented; when SECONDARY missing, effective_payer = primary only.</done>
</task>

<task type="auto">
  <name>Task 2: Add encounter-level dual_eligible and patient-level DUAL_ELIGIBLE</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- Define dual-eligible codes set: {14, 141, 142} only (not 41 — document 41 in codebook as Corrections Federal).
- Encounter-level dual_eligible: 1 when (a) primary is Medicare and secondary is Medicaid, or (b) primary is Medicaid and secondary is Medicare, or (c) primary or secondary is in {14, 141, 142}. Use _collapse_payer_category for Medicare/Medicaid (prefix 1 / prefix 2). Compute on raw PAYER_TYPE_PRIMARY and PAYER_TYPE_SECONDARY when both are present; if PAYER_TYPE_SECONDARY missing, set encounter-level dual_eligible = 0 (cannot compute (a)/(b); (c) can still use primary only — per goal, "dual_eligible = 0 when cannot compute" is acceptable for missing secondary).
- In the same first ENCOUNTER scan (after effective_payer), add dual_eligible (0/1) per row. Then when aggregating to patient level, add DUAL_ELIGIBLE = pl.col("dual_eligible").max() (or .any()) in the group_by(PATID_COL) that produces the final summary.
- Add "DUAL_ELIGIBLE" (Int8) to empty_schema and to the final .select() so derived/encounter_payer_summary.parquet includes the column. Reference: 16-RESEARCH.md "Dual-Eligible Definition", "Order of Operations" step 8.
  </action>
  <verify>Schema of output includes DUAL_ELIGIBLE; run build_encounter_payer_summary and check column exists; spot-check: patient with one encounter (Medicare primary, Medicaid secondary) has DUAL_ELIGIBLE=1.</verify>
  <done>Encounter-level dual_eligible computed per row; patient-level DUAL_ELIGIBLE in output schema and written to parquet.</done>
</task>

<task type="auto">
  <name>Task 3: Switch all payer logic to use effective_payer</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- Base counts (N_ENCOUNTERS_WITH_PAYER): already using _valid from effective_payer after Task 1. Ensure valid_enc and all group_by/agg use effective_payer for category and counts (replace PAYER_TYPE_PRIMARY with effective_payer in .select and .filter where the intent is "payer for this encounter").
- _payer_at_date: Must return effective_payer as _raw_payer, not PAYER_TYPE_PRIMARY. Either (1) compute effective_payer inside _payer_at_date using the same sentinel/valid logic and schema check for SECONDARY, or (2) pass in an encounter DataFrame that already has effective_payer. Prefer (1) for single source of truth: in _payer_at_date, read ENCOUNTER with PRIMARY and SECONDARY (if present), compute effective_payer in that scan, select PATID_COL, ADMIT_DATE, effective_payer; use effective_payer for join/closest and alias to _raw_payer.
- enc_chemo block: Select ADMIT_DATE and effective_payer (not PAYER_TYPE_PRIMARY). Filter on effective_payer valid (non-null, not sentinel). Use effective_payer for PAYER_CATEGORY via _collapse_payer_category. Ensure enc_chemo gets effective_payer from a scan that has it (e.g. same scan pattern as first enc scan: add effective_payer, then filter to chemo window and valid effective_payer).
- Grep entire module for PAYER_TYPE_PRIMARY; every use that means "payer for this encounter" must use effective_payer instead. Reference: 16-RESEARCH.md "Pitfall 1", "Code References" table.
  </action>
  <verify>No remaining uses of PAYER_TYPE_PRIMARY for encounter-level payer logic (only for reading raw columns when computing effective_payer or dual_eligible). Run pipeline that produces encounter_payer_summary.parquet and optionally compare N_ENCOUNTERS_WITH_PAYER / PAYER_CATEGORY_PRIMARY before and after on a small sample to confirm behavior.</verify>
  <done>N_ENCOUNTERS_WITH_PAYER, PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, PAYER_CATEGORY_AT_FIRST_CHEMO, PAYER_CATEGORY_AT_LAST_CHEMO, PAYER_CATEGORY_MOST_FREQUENT_AT_CHEMO, PAYER_TRANSITION all derived from effective_payer.</done>
</task>

<task type="auto">
  <name>Task 4: Update CODEBOOK and PAYER_VARIABLES_AND_CATEGORIES.md</name>
  <files>docs/CODEBOOK.md, docs/PAYER_VARIABLES_AND_CATEGORIES.md</files>
  <action>
- CODEBOOK.md (Section 6a): State that all encounter-payer summary variables use **effective payer**: per encounter, primary if valid (non-sentinel), else secondary if valid, else null. Sentinel = null, "", NI, UN, OT; document whether 99/9999 are included as sentinel (e.g. "Optional: 99/9999 configurable; default not treated as sentinel"). Add DUAL_ELIGIBLE: Int8, 0/1 at patient level; 1 if any encounter is dual-eligible (Medicare+Medicaid or code 14/141/142). Note: when PAYER_TYPE_SECONDARY is missing, effective_payer = primary only; dual_eligible = 0 when cannot compute.
- PAYER_VARIABLES_AND_CATEGORIES.md: Update "How the payer variables are calculated" to say all variables use **effective payer** (primary with sentinel fallback to secondary). Add subsection on effective payer definition and sentinel list (and 99/9999 choice). Add DUAL_ELIGIBLE to the variable table (patient-level, 1 if any encounter dual-eligible). Define dual-eligible encounter: (primary Medicare and secondary Medicaid) or (primary Medicaid and secondary Medicare) or primary or secondary in {14, 141, 142}. Document code 41 as Corrections Federal (Other government), not dual-eligible.
  </action>
  <verify>Read CODEBOOK.md and PAYER_VARIABLES_AND_CATEGORIES.md; confirm effective payer and DUAL_ELIGIBLE are described and sentinel/99/9999 choice is documented.</verify>
  <done>CODEBOOK and PAYER_VARIABLES_AND_CATEGORIES state that all payer variables use effective payer; DUAL_ELIGIBLE and dual-eligible definition documented; sentinel and 99/9999 choice documented.</done>
</task>

</tasks>

<verification>
- Run the script or pipeline that builds derived/encounter_payer_summary.parquet (e.g. assemble_clean or direct call to build_encounter_payer_summary) and confirm: (1) DUAL_ELIGIBLE column present; (2) no Python errors when PAYER_TYPE_SECONDARY is missing (test with schema check); (3) N_ENCOUNTERS_WITH_PAYER and other payer columns present and non-null where expected.
- Optional: On a dataset where ENCOUNTER has both PRIMARY and SECONDARY, spot-check one patient with primary=NI and secondary=Medicaid: effective payer should be Medicaid and N_ENCOUNTERS_WITH_PAYER should count that encounter.
- Optional: Patient with one encounter (primary Medicare, secondary Medicaid): DUAL_ELIGIBLE should be 1.
</verification>

<success_criteria>
- Effective payer is computed once per encounter with sentinel fallback; all summary variables use it.
- Encounter-level and patient-level DUAL_ELIGIBLE implemented; output parquet includes DUAL_ELIGIBLE.
- Missing PAYER_TYPE_SECONDARY handled: effective_payer = primary only; dual_eligible = 0 when cannot compute.
- CODEBOOK and PAYER_VARIABLES_AND_CATEGORIES updated with effective payer, DUAL_ELIGIBLE, and sentinel/99/9999 documentation.
</success_criteria>

<output>
After completion, create .planning/phases/16-secondary-payer-dual-eligible/16-01-SUMMARY.md per execute-plan workflow.
</output>
