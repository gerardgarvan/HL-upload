---
phase: 15-insurance-summary-tables-figures
plan: 01
type: execute
wave: 1
depends_on: [14]
files_modified:
  - scripts/build_insurance_summary.py
  - reports/insurance_summary.md
  - reports/payer_at_first_dx.csv
  - reports/payer_at_first_chemo.csv
  - reports/payer_crosstab_first_dx_first_chemo.csv
  - reports/payer_transition_prevalence.csv
  - reports/figures/insurance_payer_at_first_dx.png
  - reports/figures/insurance_payer_at_first_chemo.png
autonomous: true
requirements: [REQ-05, REQ-06]
must_haves:
  truths:
    - "Counts by PAYER_CATEGORY_AT_FIRST_DX appear in markdown and CSV with suppression"
    - "Counts by PAYER_CATEGORY_AT_FIRST_CHEMO appear in markdown and CSV with suppression"
    - "Cross-tab first DX vs first chemo and PAYER_TRANSITION prevalence in report and CSV"
    - "Bar charts for payer at first DX and first chemo exist with 1–10 suppressed"
    - "Empty or missing encounter_payer_summary.parquet exits gracefully without traceback"
  artifacts:
    - path: scripts/build_insurance_summary.py
      provides: "Insurance summary tables and figures script"
    - path: reports/insurance_summary.md
      provides: "Markdown report with all four tables using flag_small_cell"
    - path: reports/figures/insurance_payer_at_first_dx.png
      provides: "Bar chart payer at first DX"
    - path: reports/figures/insurance_payer_at_first_chemo.png
      provides: "Bar chart payer at first chemo"
  key_links:
    - from: scripts/build_insurance_summary.py
      to: derived/encounter_payer_summary.parquet
      via: "read from paths.derived_dir"
    - from: scripts/build_insurance_summary.py
      to: src/validate/structural.py
      via: "flag_small_cell for markdown"
    - from: scripts/build_insurance_summary.py
      to: src/report/quality_report.py
      via: "_suppress for CSV"
---

<objective>
Implement a single script that reads Phase 14 encounter-payer summary, builds four summary tables (counts by payer at first DX, counts by payer at first chemo, cross-tab first DX vs first chemo, PAYER_TRANSITION prevalence), writes markdown with flag_small_cell and CSV with _suppress, and generates two bar-chart figures with counts 1–10 suppressed.

Purpose: Provide HIPAA-compliant insurance-stratified summary tables and figures for the HL insurance inequities study.
Output: scripts/build_insurance_summary.py, reports/insurance_summary.md, reports/*.csv (4 tables), reports/figures/insurance_payer_at_first_dx.png, insurance_payer_at_first_chemo.png.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-insurance-summary-tables-figures/15-RESEARCH.md
@docs/CODEBOOK.md (§ 6a encounter-payer schema)
@src/validate/structural.py (flag_small_cell, SMALL_CELL_THRESHOLD)
@src/report/quality_report.py (_suppress)
@scripts/build_site_table.py (config load, table_map pattern, reports_dir)
@scripts/assemble_clean.py (derived_dir, figures_dir, encounter_payer_summary path)
@src/report/encounter_payer_summary.py (build_encounter_payer_summary schema)
</context>

<prerequisites>
- Phase 14 complete: derived/encounter_payer_summary.parquet produced by assemble_clean (build_encounter_payer_summary). Columns: ID, N_ENCOUNTERS, N_ENCOUNTERS_WITH_PAYER, N_DISTINCT_PAYER_CATEGORIES, PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, PAYER_CATEGORY_AT_FIRST_CHEMO, PAYER_CATEGORY_AT_LAST_CHEMO, PAYER_CATEGORY_MOST_FREQUENT_AT_CHEMO, PAYER_TRANSITION.
- Config: load_config() provides derived_dir; use PROJECT_ROOT / "reports" for reports_dir and reports/figures for figures (match assemble_clean).
- Suppression: flag_small_cell(n) for markdown (displays "N ⚠" for 1≤n≤10); _suppress(n) for CSV (displays "-" for 1≤n≤10). Apply to every count including totals and cross-tab cells.
- Category order (CODEBOOK and research): Medicare, Medicaid, Private, Other government, No payment / Self-pay, Other, Unavailable, Unknown. Normalize null/empty to "Unknown" so totals match.
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Script scaffold, input handling, and table 1–2 (counts by first DX and first chemo)</name>
  <files>scripts/build_insurance_summary.py</files>
  <action>
- Create scripts/build_insurance_summary.py. Add PROJECT_ROOT and sys.path like build_site_table.py. Use load_config(config_path) to get paths; set derived_dir = paths.derived_dir, reports_dir = PROJECT_ROOT / "reports", figures_dir = reports_dir / "figures".
- Read derived/encounter_payer_summary.parquet with pl.read_parquet(derived_dir / "encounter_payer_summary.parquet"). If file missing or DataFrame empty, print a short message (e.g. "encounter_payer_summary.parquet missing or empty; skipping.") and exit with return/sys.exit(0). No traceback.
- Define PAYER_CATEGORY_ORDER = ["Medicare", "Medicaid", "Private", "Other government", "No payment / Self-pay", "Other", "Unavailable", "Unknown"]. Use this order for row ordering in all tables; treat null/empty as "Unknown".
- Build table 1: counts by PAYER_CATEGORY_AT_FIRST_DX. Polars: fill_null("Unknown") on the column, then group_by(column).len(). Sort rows by PAYER_CATEGORY_ORDER (categories present first, then any others). Apply flag_small_cell to each count for markdown; _suppress for CSV. Write section "## Counts by payer at first HL diagnosis" and a markdown table (column headers: Payer category, N). Write CSV reports/payer_at_first_dx.csv with headers and _suppress for the count column.
- Build table 2: counts by PAYER_CATEGORY_AT_FIRST_CHEMO the same way. Section "## Counts by payer at first chemotherapy", markdown table, CSV reports/payer_at_first_chemo.csv.
- Ensure reports_dir and figures_dir exist (mkdir parents=True, exist_ok=True). Write reports/insurance_summary.md starting with a title and the two sections/tables above. Import flag_small_cell from src.validate.structural and _suppress from src.report.quality_report.
  </action>
  <verify>Run: python scripts/build_insurance_summary.py [config/paths.toml]. With valid derived/encounter_payer_summary.parquet, reports/insurance_summary.md contains both sections and two markdown tables; reports/payer_at_first_dx.csv and reports/payer_at_first_chemo.csv exist and use "-" for any count in 1–10. With missing or empty parquet, script exits without traceback.</verify>
  <done>Script exists; empty/missing input handled; tables 1 and 2 in insurance_summary.md and two CSVs written with correct suppression.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-tab and PAYER_TRANSITION prevalence tables</name>
  <files>scripts/build_insurance_summary.py, reports/insurance_summary.md</files>
  <action>
- In build_insurance_summary.py, add table 3: cross-tabulation of PAYER_CATEGORY_AT_FIRST_DX (rows) vs PAYER_CATEGORY_AT_FIRST_CHEMO (columns). Use Polars group_by with both columns, len(), then pivot or manual construction so rows = first DX categories (ordered by PAYER_CATEGORY_ORDER), columns = first chemo categories (same order). Include row and column totals; apply flag_small_cell to every cell and total for markdown, _suppress for CSV. Append to insurance_summary.md section "## Cross-tab: Payer at first diagnosis vs payer at first chemotherapy" and a markdown table. Write reports/payer_crosstab_first_dx_first_chemo.csv with _suppress for all numeric cells.
- Add table 4: PAYER_TRANSITION prevalence. Count rows where PAYER_TRANSITION == 1; total N; percentage. Apply flag_small_cell to the transition count and total in markdown (if total or transition count in 1–10, still show with ⚠); _suppress in CSV. Append section "## Payer transition prevalence" and table (columns e.g. Metric, Value). Write reports/payer_transition_prevalence.csv with _suppress.
- Ensure category order and "Unknown" handling are consistent: fill_null("Unknown") for both payer columns in cross-tab.
  </action>
  <verify>Run script again. insurance_summary.md contains all four sections; reports/payer_crosstab_first_dx_first_chemo.csv and reports/payer_transition_prevalence.csv exist; cross-tab has row/column totals; all counts in 1–10 show "N ⚠" in markdown and "-" in CSV.</verify>
  <done>Cross-tab and PAYER_TRANSITION prevalence in report and CSVs with per-cell suppression.</done>
</task>

<task type="auto">
  <name>Task 3: Bar chart figures with suppression</name>
  <files>scripts/build_insurance_summary.py, reports/figures/insurance_payer_at_first_dx.png, reports/figures/insurance_payer_at_first_chemo.png</files>
  <action>
- Add matplotlib (or seaborn) figure generation in build_insurance_summary.py. Use the same aggregated counts as for tables 1 and 2 (counts by PAYER_CATEGORY_AT_FIRST_DX and by PAYER_CATEGORY_AT_FIRST_CHEMO).
- For each chart: exclude categories with count in 1–10 from the bar chart (do not draw bars for those categories), or show bar but do not label with exact value (label as "≤10" or leave unlabeled). Research recommends excluding for HIPAA. Order bars by PAYER_CATEGORY_ORDER for categories present. Save as reports/figures/insurance_payer_at_first_dx.png and reports/figures/insurance_payer_at_first_chemo.png (e.g. fig.savefig(figures_dir / "insurance_payer_at_first_dx.png", dpi=150, bbox_inches="tight")).
- Ensure figures_dir exists before saving. If all categories are suppressed (all counts 1–10), write an empty or placeholder figure or skip and log; do not error.
  </action>
  <verify>Run script. reports/figures/insurance_payer_at_first_dx.png and insurance_payer_at_first_chemo.png exist; no bar shows exact count 1–10 (either category omitted or label masked).</verify>
  <done>Two PNG bar charts produced with 1–10 suppression applied.</done>
</task>

</tasks>

<verification>
- Run python scripts/build_insurance_summary.py with config that points to a derived dir containing encounter_payer_summary.parquet from Phase 14: all four table sections in insurance_summary.md, four CSVs under reports/, two PNGs under reports/figures/.
- Run with derived dir missing encounter_payer_summary.parquet or empty file: script exits cleanly with message, no exception.
- Grep reports/insurance_summary.md for "⚠" to confirm small-cell marker used; grep CSV files for "-" where counts would be 1–10.
</verification>

<success_criteria>
- scripts/build_insurance_summary.py runs after Phase 14 and produces reports/insurance_summary.md (four tables, flag_small_cell), four CSV files with _suppress, and two bar chart PNGs with 1–10 suppressed.
- Missing or empty encounter_payer_summary.parquet is handled without traceback.
- Category order matches CODEBOOK: Medicare, Medicaid, Private, Other government, No payment / Self-pay, Other, Unavailable, Unknown.
</success_criteria>

<output>
After completion, create .planning/phases/15-insurance-summary-tables-figures/15-01-SUMMARY.md with what was implemented (script entry points, output paths, optional site stratification note).
</output>
