---
phase: 01-documentation-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/load/__init__.py
  - src/load/config.py
  - src/load/schema.py
  - src/load/convert.py
  - src/validate/__init__.py
  - src/validate/structural.py
  - src/validate/cohort.py
  - src/validate/values.py
  - src/clean/validate/__init__.py
  - src/clean/validate/structural.py
  - src/clean/validate/cohort.py
  - src/clean/validate/values.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "Every function in src/load/, src/validate/, and src/clean/validate/ has a Google-style docstring with Args, Returns, and clinical rationale"
    - "Every module in src/load/, src/validate/, and src/clean/validate/ has a module-level docstring explaining purpose and pipeline position"
    - "Side effects (file writes, prints, DataFrame mutations) are mentioned naturally in description paragraphs"
    - "TODO(audit) comments mark unknowns discovered during documentation"
  artifacts:
    - path: "src/load/config.py"
      provides: "Docstrings for Paths dataclass and load_config()"
      contains: "Args:"
    - path: "src/load/convert.py"
      provides: "Docstrings for date detection and CSV-to-Parquet conversion"
      contains: "Args:"
    - path: "src/validate/structural.py"
      provides: "Docstrings for key integrity, schema validation, small-cell flagging"
      contains: "Args:"
    - path: "src/validate/cohort.py"
      provides: "Docstrings for HL cohort verification with 149-code set"
      contains: "Args:"
    - path: "src/validate/values.py"
      provides: "Docstrings for vital/lab range and date bound validation"
      contains: "Args:"
    - path: "src/clean/validate/structural.py"
      provides: "Docstrings mirroring src/validate/structural.py with clean-layer context"
      contains: "Args:"
  key_links:
    - from: "src/load/config.py"
      to: "config/paths.toml"
      via: "load_config() reads TOML"
      pattern: "load_config"
    - from: "src/validate/cohort.py"
      to: "src/clean/flags_diagnosis_provider.py"
      via: "ICD10_HL_CODES constant shared"
      pattern: "ICD10_HL_CODES"
---

<objective>
Add Google-style docstrings to all functions and module-level docstrings to all modules in `src/load/`, `src/validate/`, and `src/clean/validate/`.

Purpose: Establish documented understanding of the pipeline's loading, validation, and clean-validation layers. These modules handle CSV-to-Parquet conversion (3 date formats), structural integrity checks (PATID/ENCOUNTERID), HL cohort verification (149 ICD codes), and value validation (vital/lab ranges, date bounds). Documentation makes correctness verifiable by collaborators.

Output: 12 Python files with complete docstrings; TODO(audit) comments on unknowns discovered during documentation.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-baseline/01-CONTEXT.md
@.planning/phases/01-documentation-baseline/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docstrings to src/load/ and src/validate/ modules</name>
  <files>
    src/load/__init__.py
    src/load/config.py
    src/load/schema.py
    src/load/convert.py
    src/validate/__init__.py
    src/validate/structural.py
    src/validate/cohort.py
    src/validate/values.py
  </files>
  <action>
    Read every function in src/load/ (config.py, schema.py, convert.py) and src/validate/ (structural.py, cohort.py, values.py). For EACH function (public, private, helper — no exceptions):

    1. Add a Google-style docstring with:
       - One-line summary (imperative mood)
       - Description paragraph explaining WHAT the code does (document actual behavior, not intended)
       - One sentence of clinical rationale ("why" this exists in the pipeline context)
       - Side effects mentioned naturally in description (file writes, prints, DataFrame mutations)
       - Args: section with parameter name, type, and description
       - Returns: section with type and description
       - Do NOT add a separate "Side Effects:" section

    2. For module-level docstrings (__init__.py and each .py file):
       - Explain what the module does
       - State which pipeline phase it belongs to
       - List Input/Output/Orchestrated-by
       - List key functions

    3. For constants (ICD10_HL_CODES, DEDUP_KEYS, SMALL_CELL_THRESHOLD, etc.):
       - Add inline comments explaining clinical/data rationale
       - For magic numbers: research and explain with best-guess, flag confidence level

    4. When encountering unknowns (unclear logic, magic numbers, suspected bugs):
       - Add TODO(audit) inline comment: `# TODO(audit): <description>`
       - Note the file, line, and severity (HIGH/MEDIUM/LOW based on data correctness impact)
       - Keep a running list to provide in the SUMMARY for docs/AUDIT_LOG.md later

    Key modules and what to document:
    - config.py (3 functions): Paths dataclass, load_config(), _project_root(). Config is foundational — document TOML schema expectations.
    - schema.py (3 functions): parse_datastructure(), resolve_table_name(), _normalize_table_name(). Document the 22-table manifest format and LAB_RESULT alias issue.
    - convert.py (5 functions): convert_table(), detect_date_columns(), write_inventory(). Document the 3 date format detection chain (DATE9_RE, DATETIME_RE, YYYYMMDD_RE) and fallback behavior. This is a known fragile area.
    - structural.py (9 functions): check_patid_integrity(), check_encounterid_integrity(), validate_table_schema(), flag_small_cell(). Document HIPAA small-cell threshold (10) and key integrity checks.
    - cohort.py (6 functions): verify_hl_cohort(), detect_dx_format(). Document the 149 HL ICD code set (77 ICD-10 C81.xx + 72 ICD-9 201.xx) and dual-format matching.
    - values.py (13 functions): Vital ranges, lab ranges, date bounds, masked value handling. Document clinical ranges and why specific thresholds exist.

    Preserve all existing code behavior. Only add/modify docstrings, comments, and module docstrings. Do NOT change any logic.

    Run `ruff check src/load/ src/validate/` and `ruff format --check src/load/ src/validate/` after changes to ensure style compliance.
  </action>
  <verify>
    1. `ruff check src/load/ src/validate/` passes with no errors
    2. `ruff format --check src/load/ src/validate/` passes with no formatting issues
    3. `python -c "from src.load.config import load_config; from src.validate.structural import check_patid_integrity; from src.validate.cohort import verify_hl_cohort"` — imports succeed
    4. `pytest tests/ -x --tb=short` — existing tests still pass (no behavior changes)
    5. Every `def` in these files has a docstring (grep -c pattern matches function count)
  </verify>
  <done>
    All 39 functions in src/load/ (11) and src/validate/ (28) have Google-style docstrings with Args, Returns, clinical rationale, and side effects. All 6 modules + 2 __init__.py have module-level docstrings. TODO(audit) comments mark any unknowns found. Ruff and tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add docstrings to src/clean/validate/ modules (near-copies of src/validate/)</name>
  <files>
    src/clean/validate/__init__.py
    src/clean/validate/structural.py
    src/clean/validate/cohort.py
    src/clean/validate/values.py
  </files>
  <action>
    The src/clean/validate/ modules are near-copies of src/validate/ with minor differences (e.g., detect_dx_format() has no code_col parameter in the clean version). Reuse the docstring patterns from Task 1 with adjustments:

    1. For each module: Add module-level docstring that distinguishes this from src/validate/ — explain that these are clean-layer validation helpers used by scripts/clean_all.py (Phase 5), as opposed to src/validate/ used by scripts/validate_all.py (Phase 3-4).

    2. For each function: Copy the docstring structure from the corresponding src/validate/ function, but:
       - Update the description to reflect any parameter differences (e.g., removed code_col parameter)
       - Update pipeline phase references (Phase 5 clean context vs Phase 3-4 validate context)
       - Note any behavioral differences discovered

    3. Add a TODO(audit) comment at the top of each module noting the near-duplication with src/validate/ — this is a refactoring opportunity for Phase 2/3.

    4. For __init__.py: Add module docstring explaining this is the clean-layer validation subpackage.

    Preserve all existing code behavior. Only add/modify docstrings and comments.

    Run `ruff check src/clean/validate/` and `ruff format --check src/clean/validate/` after changes.
  </action>
  <verify>
    1. `ruff check src/clean/validate/` passes with no errors
    2. `ruff format --check src/clean/validate/` passes
    3. `pytest tests/ -x --tb=short` — all tests still pass
    4. Every `def` in src/clean/validate/ has a docstring
    5. Each module has a module-level docstring
  </verify>
  <done>
    All 28 functions in src/clean/validate/ have Google-style docstrings adapted from src/validate/ with clean-layer context. All 3 modules + __init__.py have module-level docstrings distinguishing them from src/validate/. TODO(audit) comments flag the near-duplication as refactoring opportunity. Ruff and tests pass.
  </done>
</task>

</tasks>

<verification>
1. All functions in src/load/, src/validate/, src/clean/validate/ have Google-style docstrings (grep for "def " and verify each is followed by triple-quoted docstring)
2. All modules have module-level docstrings (first non-comment string in each .py file)
3. `ruff check src/load/ src/validate/ src/clean/validate/` — clean
4. `ruff format --check src/load/ src/validate/ src/clean/validate/` — clean
5. `pytest tests/ -x` — all tests pass (no regressions)
6. TODO(audit) comments present for any unknowns discovered
</verification>

<success_criteria>
- 67 functions across 12 files have complete Google-style docstrings with Args, Returns, clinical rationale
- 12 module-level docstrings explain purpose, pipeline position, inputs/outputs
- Side effects documented naturally in description paragraphs
- Known fragile areas (date parsing, cohort matching) have detailed documentation
- All existing tests pass unchanged
- Ruff lint and format checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-baseline/01-01-SUMMARY.md`
</output>
