---
phase: 01-documentation-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clean/__init__.py
  - src/clean/dedup.py
  - src/clean/harmonize.py
  - src/clean/flags_diagnosis_provider.py
  - src/clean/outcomes_flags.py
  - src/report/__init__.py
  - src/report/quality_report.py
  - src/report/encounter_payer_summary.py
  - src/report/site_table.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "Every function in src/clean/ (core) and src/report/ has a Google-style docstring with Args, Returns, and clinical rationale"
    - "Every module in src/clean/ and src/report/ has a module-level docstring explaining purpose and pipeline position"
    - "Complex payer logic in encounter_payer_summary.py is documented with clinical rationale for fallback chains, dual-eligible detection, and sentinel value handling"
    - "Deduplication composite key logic and cross-table consistency checks are documented with rationale for key selection"
    - "TODO(audit) comments mark unknowns discovered during documentation, especially in payer logic and flag derivation"
  artifacts:
    - path: "src/clean/dedup.py"
      provides: "Docstrings for composite-key dedup and cross-table consistency"
      contains: "Args:"
    - path: "src/clean/harmonize.py"
      provides: "Docstrings for partner flags and enrollment coverage checks"
      contains: "Args:"
    - path: "src/report/encounter_payer_summary.py"
      provides: "Docstrings for effective payer logic, dual-eligible detection, payer-at-treatment"
      contains: "Args:"
    - path: "src/report/quality_report.py"
      provides: "Docstrings for patient-level derived variables and DQ metrics"
      contains: "Args:"
    - path: "src/report/site_table.py"
      provides: "Docstrings for per-site HL summary tables"
      contains: "Args:"
  key_links:
    - from: "src/clean/dedup.py"
      to: "src/validate/structural.py"
      via: "Shared PATID_LINKED_TABLES, ENCOUNTER_LINKED_TABLES constants"
      pattern: "PATID_LINKED_TABLES|ENCOUNTER_LINKED_TABLES"
    - from: "src/report/encounter_payer_summary.py"
      to: "src/report/quality_report.py"
      via: "Both build patient-level variables used in derived/"
      pattern: "build_patient_level|build_encounter_payer"
    - from: "src/clean/outcomes_flags.py"
      to: "Outcomes.csv"
      via: "load_outcomes_code_lookup() reads treatment codes"
      pattern: "load_outcomes_code_lookup"
---

<objective>
Add Google-style docstrings to all functions and module-level docstrings to all modules in `src/clean/` (core modules: dedup, harmonize, flags, outcomes) and `src/report/` (quality_report, encounter_payer_summary, site_table).

Purpose: Document the pipeline's most complex logic — deduplication composite keys, partner harmonization, HL/survivorship/provider flags, treatment modality matching, effective payer derivation with dual-eligible detection, and patient-level variable assembly. These are known fragile areas where correctness is hardest to verify. Documentation makes the clinical reasoning behind each decision explicit and auditable.

Output: 9 Python files with complete docstrings; TODO(audit) comments on unknowns, especially in payer logic and flag derivation.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-baseline/01-CONTEXT.md
@.planning/phases/01-documentation-baseline/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@docs/PAYER_VARIABLES_AND_CATEGORIES.md
@docs/FLAG_CODES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docstrings to src/clean/ core modules (dedup, harmonize, flags, outcomes)</name>
  <files>
    src/clean/__init__.py
    src/clean/dedup.py
    src/clean/harmonize.py
    src/clean/flags_diagnosis_provider.py
    src/clean/outcomes_flags.py
  </files>
  <action>
    Read every function in src/clean/ core modules (dedup.py: 6 functions, harmonize.py: 3 functions, flags_diagnosis_provider.py: 5 functions, outcomes_flags.py: 3 functions). For EACH function:

    1. Add Google-style docstring per locked decision:
       - One-line summary
       - Description paragraph: document ACTUAL behavior (what code DOES), not intended behavior
       - One sentence clinical rationale
       - Side effects mentioned naturally in description
       - Args: and Returns: sections with types

    2. Module-level docstrings for each .py file:
       - Purpose and pipeline phase (Phase 5: Cleaning)
       - Input/Output/Orchestrated-by
       - Key functions listed

    3. Constants documentation:
       - DEDUP_KEYS dict: document composite key selection rationale per table (why these columns uniquely identify a record)
       - PARTNER_FLAGS: document what each partner flag means clinically (ICD_MAPPED = retrospective ICD coding by AMS/UMI)
       - ONCOLOGY_KEYWORDS: document how provider oncology classification works
       - Outcomes modality codes: document code system mapping (CPT, HCPCS, ICD-10-PCS)

    4. Known fragile areas requiring extra attention:
       - dedup.py: Null key behavior in is_duplicated() — document whether nulls match each other (Polars behavior: they do NOT, which is correct). Add TODO(audit) if unclear.
       - harmonize.py: Many-to-many join explosion in enrollment coverage checks — document lazy evaluation strategy and memory implications
       - outcomes_flags.py: Pandas dependency (pd.read_csv) in an otherwise Polars codebase — document why and add TODO(audit) for migration
       - flags_diagnosis_provider.py: Survivorship code list (specifics + prefix matching) — document clinical definition

    5. When encountering unknowns: Add TODO(audit) comments with severity rating. Keep running list for AUDIT_LOG.md.

    Preserve all existing code behavior. Only add/modify docstrings and comments.

    Run `ruff check src/clean/` and `ruff format --check src/clean/` after changes (this will also check src/clean/validate/ — ensure no issues there either; if src/clean/validate/ not yet documented by Plan 01, that's fine, just ensure no regressions).
  </action>
  <verify>
    1. `ruff check src/clean/` passes with no errors
    2. `ruff format --check src/clean/` passes
    3. `pytest tests/ -x --tb=short` — all tests still pass
    4. Every `def` in src/clean/dedup.py, harmonize.py, flags_diagnosis_provider.py, outcomes_flags.py has a docstring
    5. Each module has a module-level docstring referencing Phase 5 pipeline context
  </verify>
  <done>
    All 17 functions in src/clean/ core modules have Google-style docstrings with Args, Returns, clinical rationale. All 4 modules + __init__.py have module-level docstrings. DEDUP_KEYS, PARTNER_FLAGS, ONCOLOGY_KEYWORDS constants documented. Fragile areas (null dedup behavior, pandas dependency, many-to-many joins) flagged with TODO(audit). Ruff and tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add docstrings to src/report/ modules (quality_report, encounter_payer_summary, site_table)</name>
  <files>
    src/report/__init__.py
    src/report/quality_report.py
    src/report/encounter_payer_summary.py
    src/report/site_table.py
  </files>
  <action>
    Read every function in src/report/ (quality_report.py: 11 functions, encounter_payer_summary.py: 12 functions, site_table.py: 18 functions). For EACH function:

    1. Add Google-style docstring per locked decision. These modules are the most complex in the codebase — take extra care with clinical rationale.

    2. Module-level docstrings explaining:
       - quality_report.py: Phase 6 (Assembly) — builds patient-level derived variables, DQ metrics, cleaning decisions
       - encounter_payer_summary.py: Phase 6 (Assembly) — one-row-per-patient payer summary with effective payer derivation, dual-eligible detection, payer-at-treatment windows
       - site_table.py: Phase 7+ (Reporting) — per-site HL summary tables with outcomes cross-tabs

    3. CRITICAL — encounter_payer_summary.py (known complex area, 788 lines):
       - Document the effective payer logic: primary → secondary fallback chain
       - Document dual-eligible detection: codes 14/141/142 and Medicare+Medicaid combinations
       - Document payer-at-treatment: 30-day window, mode of valid payers, clinical justification
       - Document PAYER_CATEGORY_ORDER and why certain categories exist
       - Document INCLUDE_99_AS_SENTINEL flag: what it does, why it's disabled, what happens in each mode
       - Document _collapse_payer_category(): PCORnet code prefix → reporting category mapping
       - For every magic number (30 days, threshold values, sentinel codes): explain clinical/data rationale
       - Add TODO(audit) for any unclear logic in fallback chains

    4. quality_report.py:
       - Document build_patient_level_derived(): how demographics, treatment flags, HL subtype are assembled
       - Document _suppress() function: HIPAA small-cell suppression (counts 1-10 → "-")
       - Document aggregate_dq_metrics(): per-partner, per-table quality metrics

    5. site_table.py:
       - Document per-site summary generation and cross-tab logic
       - Document small-cell handling in site-level tables
       - Document how outcomes (CHEMO, RADIATION, SCT) are cross-tabulated

    6. Reference docs/PAYER_VARIABLES_AND_CATEGORIES.md and docs/FLAG_CODES.md for clinical context when writing rationale.

    Preserve all existing code behavior. Only add/modify docstrings and comments.

    Run `ruff check src/report/` and `ruff format --check src/report/` after changes.
  </action>
  <verify>
    1. `ruff check src/report/` passes with no errors
    2. `ruff format --check src/report/` passes
    3. `pytest tests/ -x --tb=short` — all tests still pass
    4. Every `def` in src/report/ has a docstring
    5. encounter_payer_summary.py docstrings include clinical rationale for payer logic decisions
    6. _suppress() and _collapse_payer_category() have complete docstrings explaining HIPAA and PCORnet mapping
  </verify>
  <done>
    All 41 functions in src/report/ have Google-style docstrings with Args, Returns, clinical rationale. All 3 modules + __init__.py have module-level docstrings. Complex payer logic fully documented: effective payer fallback, dual-eligible detection, 30-day treatment window, sentinel value handling (INCLUDE_99_AS_SENTINEL). Small-cell suppression documented with HIPAA context. TODO(audit) comments mark unclear payer logic decisions. Ruff and tests pass.
  </done>
</task>

</tasks>

<verification>
1. All functions in src/clean/ (core) and src/report/ have Google-style docstrings
2. All modules have module-level docstrings with pipeline phase context
3. `ruff check src/clean/ src/report/` — clean
4. `ruff format --check src/clean/ src/report/` — clean
5. `pytest tests/ -x` — all tests pass
6. Payer logic (encounter_payer_summary.py) has detailed clinical rationale in docstrings
7. TODO(audit) comments present for unknowns, especially payer sentinel values and pandas dependency
</verification>

<success_criteria>
- 58 functions across 9 files have complete Google-style docstrings
- 9 module-level docstrings with pipeline position and I/O context
- Complex payer logic has line-by-line clinical rationale where needed
- DEDUP_KEYS, PARTNER_FLAGS, payer constants documented
- Known fragile areas (payer fallback, null dedup, pandas dep) flagged
- All existing tests pass unchanged
- Ruff lint and format checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-baseline/01-02-SUMMARY.md`
</output>
