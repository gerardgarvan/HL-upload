---
phase: 01-documentation-baseline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture_golden.py
  - .golden/manifest.json
  - .gitignore
autonomous: true
requirements:
  - BASE-01

must_haves:
  truths:
    - "scripts/capture_golden.py exists and can capture SHA256 checksums, schemas, and row counts for pipeline outputs"
    - ".golden/manifest.json is committed to git with file metadata but NO actual patient data"
    - ".gitignore prevents actual pipeline output files (Parquet, CSV) from being committed"
    - "Capture script is rerunnable to update baseline after intentional changes"
  artifacts:
    - path: "scripts/capture_golden.py"
      provides: "Golden baseline capture script"
      contains: "hashlib"
    - path: ".golden/manifest.json"
      provides: "Golden manifest with checksums and schemas"
      contains: "sha256"
    - path: ".gitignore"
      provides: "Gitignore entries preventing PHI data from being committed"
      contains: "parquet_clean"
  key_links:
    - from: "scripts/capture_golden.py"
      to: "src/load/config.py"
      via: "Uses load_config() for path resolution"
      pattern: "load_config"
    - from: "scripts/capture_golden.py"
      to: ".golden/manifest.json"
      via: "Writes manifest file"
      pattern: "manifest.json"
---

<objective>
Create the golden baseline capture system: a script that records SHA256 checksums, schemas (columns + dtypes), and row counts for pipeline outputs into a committed manifest, with actual data files gitignored.

Purpose: Establish regression detection baselines for all pipeline outputs before any code changes. The manifest enables comparison after changes to detect unintended output modifications. Actual patient data stays local (HIPAA compliance) while metadata is safe to commit.

Output: scripts/capture_golden.py, .golden/manifest.json (populated if pipeline outputs exist locally), updated .gitignore.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-baseline/01-CONTEXT.md
@.planning/phases/01-documentation-baseline/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@src/load/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/capture_golden.py and update .gitignore</name>
  <files>
    scripts/capture_golden.py
    .gitignore
  </files>
  <action>
    Create scripts/capture_golden.py following the pattern from 01-RESEARCH.md Example 4, with these specifications:

    1. Script structure:
       - Module-level docstring explaining purpose, usage, and HIPAA rationale
       - Import from src.load.config for path resolution
       - Functions:
         - compute_file_sha256(file_path: Path) -> str: SHA256 using hashlib.file_digest() (Python 3.11+)
         - capture_parquet_metadata(file_path: Path) -> dict: Schema + row count using pl.read_parquet_schema() and pl.scan_parquet().select(pl.len())
         - capture_csv_metadata(file_path: Path) -> dict: Row count and column names for CSV files
         - capture_golden_manifest(output_dirs: list[Path], manifest_path: Path) -> dict: Main capture function
         - main(): Entry point with config loading

    2. Output directories to capture (tiered by regression detection value):
       HIGH priority:
       - parquet_clean/ (cleaned CDM tables — core pipeline output)
       - derived/ (patient_level.parquet, encounter_payer_summary.parquet)
       MEDIUM priority:
       - reports/*.csv (summary tables)
       LOW priority (include but mark as optional):
       - reports/figures/*.png (binary, harder to diff)

    3. Manifest structure (.golden/manifest.json):
       ```json
       {
         "manifest_version": "1.0",
         "captured": "ISO-8601 timestamp",
         "pipeline_commit": "git HEAD SHA (if available)",
         "files": {
           "relative/path.parquet": {
             "sha256": "hex digest",
             "schema": {"col_name": "dtype_string", ...},
             "row_count": 12345,
             "size_bytes": 67890,
             "priority": "HIGH|MEDIUM|LOW",
             "captured": "ISO-8601 timestamp"
           },
           "relative/path.csv": {
             "sha256": "hex digest",
             "columns": ["col1", "col2"],
             "row_count": 100,
             "size_bytes": 2048,
             "priority": "MEDIUM",
             "captured": "ISO-8601 timestamp"
           }
         }
       }
       ```

    4. Features:
       - Graceful handling: skip missing directories/files (partial pipeline runs OK)
       - Progress output: print each file being captured
       - Git commit SHA: include current HEAD if in a git repo (subprocess.run)
       - Comparison mode: if manifest already exists, report changes (added/removed/changed files)
       - All functions have Google-style docstrings per locked decision

    5. Update .gitignore:
       - Read existing .gitignore first
       - Add entries for pipeline output files that should never be committed:
         ```
         # Pipeline outputs (contain PHI — never commit)
         parquet_clean/
         derived/
         reports/*.csv
         reports/figures/*.png

         # Golden manifest is safe to commit (metadata only, no PHI)
         !.golden/manifest.json
         ```
       - Do NOT remove existing .gitignore entries
       - Be careful: check if entries already exist before adding duplicates

    6. The script should work even if pipeline hasn't been run (empty manifest is valid):
       - If no output files found, create manifest with empty files dict
       - Print message suggesting user run pipeline first

    Run `ruff check scripts/capture_golden.py` and `ruff format scripts/capture_golden.py` after creating.
  </action>
  <verify>
    1. scripts/capture_golden.py exists and is syntactically valid: `python -c "import ast; ast.parse(open('scripts/capture_golden.py').read())"`
    2. `ruff check scripts/capture_golden.py` passes
    3. `ruff format --check scripts/capture_golden.py` passes
    4. All functions have docstrings: grep for "def " and verify docstrings follow
    5. .gitignore contains entries for parquet_clean/, derived/, reports/*.csv, reports/figures/*.png
    6. .gitignore contains !.golden/manifest.json exception
    7. `pytest tests/ -x --tb=short` — existing tests still pass
    8. Script can run (may produce empty manifest if pipeline outputs don't exist locally):
       `python scripts/capture_golden.py` or verify syntax only if config not available
  </verify>
  <done>
    scripts/capture_golden.py created with compute_file_sha256(), capture_parquet_metadata(), capture_csv_metadata(), capture_golden_manifest(), and main(). Captures SHA256 checksums, schemas, and row counts for pipeline outputs at 3 priority tiers. Manifest is JSON with no PHI. .gitignore updated to prevent pipeline output commits while allowing .golden/manifest.json. Script is rerunnable and handles partial pipeline runs gracefully. All functions have Google-style docstrings. Ruff passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run capture script and generate initial manifest</name>
  <files>
    .golden/manifest.json
  </files>
  <action>
    Attempt to run the capture script to generate the initial golden manifest:

    1. Run: `python scripts/capture_golden.py`

    2. If pipeline outputs exist locally (parquet_clean/, derived/, reports/):
       - Manifest will be populated with file metadata
       - Review manifest to verify no PHI (only checksums, schemas, row counts)
       - Verify manifest JSON is valid and well-structured

    3. If pipeline outputs do NOT exist locally (likely on dev machine without HPC data):
       - Script should create an empty/minimal manifest gracefully
       - Create a placeholder .golden/manifest.json with:
         ```json
         {
           "manifest_version": "1.0",
           "captured": "ISO-8601 timestamp",
           "pipeline_commit": "current git HEAD",
           "note": "Placeholder — run on HPC after pipeline execution to capture actual baselines",
           "files": {}
         }
         ```
       - This is expected behavior — the script is designed to be rerun on HPC

    4. Verify .golden/manifest.json:
       - Is valid JSON
       - Contains manifest_version field
       - Does NOT contain any actual patient data values
       - Contains only metadata (checksums, column names, dtypes, counts)

    5. Verify .gitignore works:
       - `git status` should NOT show any .parquet or pipeline .csv files as untracked
       - .golden/manifest.json SHOULD be trackable (not gitignored)
  </action>
  <verify>
    1. .golden/manifest.json exists and is valid JSON: `python -c "import json; json.load(open('.golden/manifest.json'))"`
    2. Manifest contains manifest_version field
    3. Manifest does NOT contain actual data values (only metadata)
    4. `git status` does not show pipeline output files (.parquet, .csv from derived/reports)
    5. .golden/manifest.json is not gitignored: appears in `git status` or can be `git add`ed
  </verify>
  <done>
    .golden/manifest.json generated (either populated with pipeline output metadata or placeholder if outputs not available locally). Manifest is valid JSON with no PHI. .gitignore correctly prevents pipeline outputs from being committed while allowing manifest. Golden baseline system is ready for use — rerun scripts/capture_golden.py on HPC after pipeline execution to capture actual baselines.
  </done>
</task>

</tasks>

<verification>
1. scripts/capture_golden.py exists with all required functions and docstrings
2. .golden/manifest.json exists and is valid JSON with no PHI
3. .gitignore prevents pipeline output commits
4. Script is rerunnable (idempotent)
5. Ruff checks pass on capture script
6. Existing tests still pass
</verification>

<success_criteria>
- scripts/capture_golden.py captures SHA256, schemas, row counts for pipeline outputs
- .golden/manifest.json committed to git with file metadata only (no PHI)
- .gitignore updated to prevent actual data files from being committed
- Capture script works with both full pipeline outputs (HPC) and empty state (dev machine)
- Script follows project conventions (Google-style docstrings, ruff compliance)
- Golden baseline system ready for regression detection
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-baseline/01-05-SUMMARY.md`
</output>
