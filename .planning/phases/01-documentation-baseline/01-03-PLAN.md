---
phase: 01-documentation-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert_all.py
  - scripts/validate_all.py
  - scripts/clean_all.py
  - scripts/assemble_clean.py
  - scripts/build_insurance_summary.py
  - scripts/build_site_table.py
  - scripts/add_modality_flags.py
  - scripts/check_insurance_outputs.py
  - scripts/pipeline_smoke_test.py
  - scripts/smoke_test.py
  - scripts/validate_values.py
  - scripts/inspect_tr_stage.py
  - docs/AUDIT_LOG.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "Every function in scripts/ has a Google-style docstring with Args, Returns, and clinical rationale"
    - "Every script has a module-level docstring explaining what pipeline phase it executes and how to run it"
    - "docs/AUDIT_LOG.md exists with all TODO(audit) items collected from this plan AND Plans 01-02 (aggregated from SUMMARY files)"
    - "Audit log entries are categorized by severity (HIGH/MEDIUM/LOW) with location, issue, clinical context"
  artifacts:
    - path: "scripts/convert_all.py"
      provides: "Docstring for Phase 1 CSV-to-Parquet entry point"
      contains: "Args:"
    - path: "scripts/validate_all.py"
      provides: "Docstrings for Phase 2 structural validation entry point"
      contains: "Args:"
    - path: "scripts/clean_all.py"
      provides: "Docstrings for Phase 3 dedup and flagging entry point"
      contains: "Args:"
    - path: "scripts/build_insurance_summary.py"
      provides: "Docstrings for Phase 5 payer analysis entry point"
      contains: "Args:"
    - path: "docs/AUDIT_LOG.md"
      provides: "Central audit log of all unknowns discovered during documentation"
      contains: "AUDIT-"
  key_links:
    - from: "scripts/convert_all.py"
      to: "src/load/convert.py"
      via: "Calls convert_table() for each CSV"
      pattern: "convert_table"
    - from: "scripts/clean_all.py"
      to: "src/clean/dedup.py"
      via: "Calls flag_duplicates() per table"
      pattern: "flag_duplicates"
    - from: "docs/AUDIT_LOG.md"
      to: "src/"
      via: "References source locations of unknowns"
      pattern: "AUDIT-"
---

<objective>
Add Google-style docstrings to all functions and module-level docstrings to all scripts in `scripts/`, then create `docs/AUDIT_LOG.md` collecting all TODO(audit) items discovered across Plans 01, 02, and 03.

Purpose: Document the pipeline's entry points (the scripts users actually run) and create a centralized audit log of unknowns for Phase 2/3 prioritization. Scripts are the user-facing layer — clear documentation of what each script does, requires, and produces enables collaborators to run the pipeline.

Output: 12 script files with complete docstrings; docs/AUDIT_LOG.md with severity-categorized unknowns.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-documentation-baseline/01-CONTEXT.md
@.planning/phases/01-documentation-baseline/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md

# Read Plan 01 and Plan 02 SUMMARYs for TODO(audit) items to aggregate
@.planning/phases/01-documentation-baseline/01-01-SUMMARY.md
@.planning/phases/01-documentation-baseline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docstrings to all scripts/ files</name>
  <files>
    scripts/convert_all.py
    scripts/validate_all.py
    scripts/clean_all.py
    scripts/assemble_clean.py
    scripts/build_insurance_summary.py
    scripts/build_site_table.py
    scripts/add_modality_flags.py
    scripts/check_insurance_outputs.py
    scripts/pipeline_smoke_test.py
    scripts/smoke_test.py
    scripts/validate_values.py
    scripts/inspect_tr_stage.py
  </files>
  <action>
    Read every function in scripts/ (46 functions across 12 files). For EACH function:

    1. Add Google-style docstring per locked decision:
       - One-line summary
       - Description paragraph documenting actual behavior
       - Clinical rationale (one sentence)
       - Side effects (prints, file writes) mentioned naturally
       - Args: and Returns: sections

    2. Module-level docstrings for each script:
       - What pipeline phase this script executes
       - Usage: `python scripts/scriptname.py [config/paths.toml]`
       - Prerequisites: what must have run before this script
       - Outputs: files created/modified
       - Key operations performed

    3. Priority scripts (largest, most complex):
       - validate_all.py (802 lines, 7 functions): Document all validation checks, report generation, cohort verification flow
       - clean_all.py (576 lines, 6 functions): Document table-by-table cleaning loop, flag addition order, report generation
       - build_insurance_summary.py (640 lines, 4 functions): Document payer summary construction, cross-tab generation, figure creation, small-cell suppression in reports
       - validate_values.py (1199 lines, 18 functions): Document deep value validation — ICD code validation, date bounds, vital/lab ranges, masked values

    4. Utility/debugging scripts (simpler):
       - add_modality_flags.py (65 lines): Document standalone treatment flag addition
       - check_insurance_outputs.py (104 lines): Document payer output verification
       - inspect_tr_stage.py (63 lines): Document tumor registry debugging helper
       - pipeline_smoke_test.py (101 lines): Document minimal integration test
       - smoke_test.py (134 lines): Document unit test runner
       - build_site_table.py (88 lines): Document per-site summary generation

    5. When encountering unknowns: Add TODO(audit) comments with severity. Track all items for AUDIT_LOG.md in Task 2.

    Preserve all existing code behavior. Only add/modify docstrings and comments.

    Run `ruff check scripts/` and `ruff format --check scripts/` after changes.
  </action>
  <verify>
    1. `ruff check scripts/` passes with no errors
    2. `ruff format --check scripts/` passes
    3. `pytest tests/ -x --tb=short` — all tests still pass
    4. Every `def` in scripts/ has a docstring
    5. Each script has a module-level docstring with usage, prerequisites, and outputs
  </verify>
  <done>
    All 46 functions in 12 scripts have Google-style docstrings. All scripts have module-level docstrings with pipeline phase, usage instructions, prerequisites, and outputs. Priority scripts (validate_all, clean_all, build_insurance_summary, validate_values) have detailed documentation. TODO(audit) items tracked. Ruff and tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/AUDIT_LOG.md aggregating all TODO(audit) items</name>
  <files>
    docs/AUDIT_LOG.md
  </files>
  <action>
    Create docs/AUDIT_LOG.md collecting ALL TODO(audit) items discovered during Plans 01, 02, and 03. This is the central audit log per user decision (both inline TODO(audit) in source AND collected in AUDIT_LOG.md).

    1. Read the SUMMARY files from Plans 01 and 02 (if available) to collect their TODO(audit) items. Also collect items from Task 1 of this plan.

    2. If SUMMARY files are not yet available (Plans 01/02 running in parallel), grep the codebase for all TODO(audit) comments:
       `grep -rn "TODO(audit)" src/ scripts/`

    3. Structure the audit log:
       ```
       # Audit Log: Unknowns & Technical Debt

       **Created:** 2026-03-17 (Phase 1 documentation)
       **Purpose:** Track unknowns discovered during documentation for validation/testing in Phase 2/3
       **How to use:** Each entry maps to a Phase 2/3 requirement for follow-up

       ## HIGH Severity (Data Correctness Impact)
       ### AUDIT-NNN: Title
       **Location:** `file:line`
       **Code context:** (relevant code snippet)
       **Issue:** What is unclear or potentially wrong
       **What code DOES (actual behavior):** Description of current behavior
       **Clinical context:** Why this matters for data correctness
       **Impact on data correctness:** How wrong this could be
       **Confidence level:** HIGH/MEDIUM/LOW — how sure we are this is actually a problem
       **Recommended action:** What to do about it
       **Phase 2/3 follow-up:** Which requirement ID to attach to

       ## MEDIUM Severity (Usability/Maintenance Impact)
       ...

       ## LOW Severity (Nice-to-Have)
       ...
       ```

    4. Known items to include (from CONCERNS.md and RESEARCH.md):
       HIGH severity candidates:
       - INCLUDE_99_AS_SENTINEL flag (encounter_payer_summary.py) — payer fallback behavior
       - Date parsing 30%/50% match thresholds (convert.py) — false positive/negative risk
       - LAB_RESULT vs LAB_RESULT_CM naming mismatch (schema.py, dedup.py) — silently skips data
       - Null key behavior in deduplication (dedup.py) — data integrity

       MEDIUM severity candidates:
       - Pandas dependency in outcomes_flags.py (Polars codebase using pandas)
       - Small-cell suppression inconsistency (flag_small_cell vs _suppress)
       - src/clean/validate/ near-duplication of src/validate/
       - Date parsing fallback: no parse failure rate reporting

       LOW severity candidates:
       - No logging framework (print-only)
       - No incremental conversion
       - hpc-upload/ directory divergence

    5. Number entries sequentially: AUDIT-001, AUDIT-002, etc.

    6. At the end of the file, add a "Phase 2/3 Mapping" section summarizing which audit items map to which Phase 2/3 requirements (VAL-01 through VAL-04, TEST-01 through TEST-04).

    IMPORTANT: Document ACTUAL behavior in each entry, not speculated behavior. Read the code to confirm what it does before describing.
  </action>
  <verify>
    1. docs/AUDIT_LOG.md exists and is well-structured
    2. Every TODO(audit) comment in src/ and scripts/ has a corresponding entry in AUDIT_LOG.md
    3. Entries have severity categorization (HIGH/MEDIUM/LOW)
    4. Each entry has Location, Issue, What code DOES, Impact, Confidence
    5. Phase 2/3 mapping section exists at the end
    6. `grep -c "AUDIT-" docs/AUDIT_LOG.md` shows reasonable count (expect 10-20 entries)
  </verify>
  <done>
    docs/AUDIT_LOG.md created with all TODO(audit) items from Plans 01, 02, and 03 collected and categorized by severity. Each entry has location, code context, issue description, actual behavior, clinical context, confidence level, and Phase 2/3 mapping. HIGH severity items focus on data correctness risks (sentinel values, date parsing, null dedup, table name mismatches). Phase 2/3 mapping section connects audit items to specific requirement IDs.
  </done>
</task>

</tasks>

<verification>
1. All functions in scripts/ have Google-style docstrings
2. All scripts have module-level docstrings with usage and pipeline context
3. docs/AUDIT_LOG.md exists with severity-categorized entries
4. `ruff check scripts/` — clean
5. `ruff format --check scripts/` — clean
6. `pytest tests/ -x` — all tests pass
7. grep for TODO(audit) in src/ and scripts/ — all have corresponding AUDIT_LOG entries
</verification>

<success_criteria>
- 46 functions across 12 scripts have complete Google-style docstrings
- 12 module-level docstrings with pipeline phase, usage, prerequisites, outputs
- docs/AUDIT_LOG.md contains 10-20 categorized audit entries
- Each audit entry has location, actual behavior, severity, Phase 2/3 mapping
- All existing tests pass unchanged
- Ruff lint and format checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-baseline/01-03-SUMMARY.md`
</output>
