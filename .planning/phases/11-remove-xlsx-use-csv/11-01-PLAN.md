---
phase: 11-remove-xlsx-use-csv
plan: 01
type: execute
wave: 1
depends_on: [10]
files_modified:
  - src/clean/outcomes_flags.py
  - scripts/assemble_clean.py
  - scripts/add_modality_flags.py
  - tests/test_load_outcomes_code_lookup.py
  - tests/test_add_modality_flags.py
  - environment.yml
  - .github/workflows/ci.yml
  - .planning/docs/OUTCOMES_XLSX_SCHEMA.md
  - .planning/docs/HPC_UPLOAD_SYNC.md
  - reports/modality_flags.md
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - "load_outcomes_code_lookup reads Outcomes.csv via pd.read_csv; ffill logic unchanged"
    - "Path references use Outcomes.csv in assemble_clean.py and add_modality_flags.py"
    - "Tests use df.to_csv() for mock outcomes; no openpyxl in test fixtures"
    - "openpyxl removed from environment.yml and .github/workflows/ci.yml"
    - "pytest and CI pass without openpyxl installed"
  artifacts:
    - path: src/clean/outcomes_flags.py
      provides: CSV loader with pd.read_csv and ffill
    - path: .planning/docs/OUTCOMES_XLSX_SCHEMA.md
      provides: CSV schema documentation (updated or renamed)
  key_links:
    - from: src/clean/outcomes_flags.py
      to: Outcomes.csv
      via: pd.read_csv
      pattern: "pd\\.read_csv"
---

<objective>
Replace Outcomes.xlsx with Outcomes.csv to eliminate the openpyxl dependency:
- Change load_outcomes_code_lookup: pd.read_excel → pd.read_csv; keep ffill logic identical
- Update path references (assemble_clean.py, add_modality_flags.py)
- Update tests: mock .xlsx → .csv (df.to_csv)
- Remove openpyxl from environment.yml and CI
- Update schema and docs to describe CSV format

Output: CSV-based outcomes loader, openpyxl removed, all tests and CI passing.
</objective>

<execution_context>
Phase 11 follows Phase 10. Outcomes.csv already exists at project root (or run optional conversion script first). Clean cutover — no dual .xlsx/.csv support per research. Pitfall: do NOT use keep_default_na=False in read_csv (empty cells must become NaN for ffill).
</execution_context>

<context>
@.planning/phases/11-remove-xlsx-use-csv/11-RESEARCH.md
@src/clean/outcomes_flags.py
@scripts/assemble_clean.py
@scripts/add_modality_flags.py
@tests/test_load_outcomes_code_lookup.py
@tests/test_add_modality_flags.py
</context>

<prerequisites>
- Phase 10 complete (CI, pre-commit, pipeline hardening)
- Outcomes.csv available at project root:
  - Option A: Outcomes.csv already exists (research confirms identical columns)
  - Option B: Run scripts/convert_outcomes_to_csv.py once (create from Outcomes.xlsx with openpyxl still installed) before removing openpyxl
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Switch loader to CSV and update path references</name>
  <files>src/clean/outcomes_flags.py, scripts/assemble_clean.py, scripts/add_modality_flags.py</files>
  <action>
1. **src/clean/outcomes_flags.py:**
   - Replace `pd.read_excel(path, sheet_name="Outcomes")` with `pd.read_csv(path)`
   - Keep ffill logic identical: `df["Modality"] = df["Modality"].ffill()`; `df["Code system"] = df["Code system"].ffill()`
   - Do NOT use keep_default_na=False (empty cells must become NaN for ffill per research Pitfall 1)
   - Update module docstring and load_outcomes_code_lookup docstring: "Outcomes.xlsx" → "Outcomes.csv"
   - Update add_modality_flags docstring similarly

2. **scripts/assemble_clean.py:**
   - Change `PROJECT_ROOT / "Outcomes.xlsx"` → `PROJECT_ROOT / "Outcomes.csv"`
   - Update print messages: "Outcomes.xlsx" → "Outcomes.csv" (e.g. "SKIP modality flags (Outcomes.csv not found)")

3. **scripts/add_modality_flags.py:**
   - Change default: `PROJECT_ROOT / "Outcomes.xlsx"` → `PROJECT_ROOT / "Outcomes.csv"`
   - Update module docstring and comments

Reference: 11-RESEARCH.md Pattern 1 (read_csv + ffill), Pitfall 1 (no keep_default_na=False).
</action>
  <verify>grep -q "pd.read_csv" src/clean/outcomes_flags.py && grep -q "Outcomes.csv" scripts/assemble_clean.py scripts/add_modality_flags.py</verify>
  <done>Loader uses pd.read_csv; path references point to Outcomes.csv</done>
</task>

<task type="auto">
  <name>Task 2: Update tests to use CSV mocks</name>
  <files>tests/test_load_outcomes_code_lookup.py, tests/test_add_modality_flags.py</files>
  <action>
1. **tests/test_load_outcomes_code_lookup.py:**
   - Change excel_path = tmp_path / "outcomes_mock.xlsx" → csv_path = tmp_path / "outcomes_mock.csv"
   - Replace df.to_excel(excel_path, sheet_name="Outcomes", index=False) with df.to_csv(csv_path, index=False)
   - Update load_outcomes_code_lookup(csv_path)
   - Update docstring: "mock Excel" → "mock CSV"

2. **tests/test_add_modality_flags.py:**
   - Change outcomes_path = tmp_path / "outcomes.xlsx" → tmp_path / "outcomes.csv"
   - Replace .to_excel(outcomes_path, sheet_name="Outcomes", index=False) with .to_csv(outcomes_path, index=False)
   - Update comment: "Outcomes.xlsx" → "Outcomes.csv"

Both tests must pass without openpyxl. Ensure sparse rows (None in Modality) write correctly: df.to_csv produces empty string for NaN by default; pd.read_csv reads empty as NaN. Per research: default read_csv na handling is correct.
</action>
  <verify>python -m pytest tests/test_load_outcomes_code_lookup.py tests/test_add_modality_flags.py -v</verify>
  <done>Tests pass using CSV mocks; no openpyxl in fixtures</done>
</task>

<task type="auto">
  <name>Task 3: Remove openpyxl and update documentation</name>
  <files>environment.yml, .github/workflows/ci.yml, .planning/docs/OUTCOMES_XLSX_SCHEMA.md, .planning/docs/HPC_UPLOAD_SYNC.md, reports/modality_flags.md</files>
  <action>
1. **environment.yml:** Remove `openpyxl` from pip section.

2. **.github/workflows/ci.yml:** Remove `openpyxl` from "Install dependencies" pip install line (test job).

3. **.planning/docs/OUTCOMES_XLSX_SCHEMA.md:** Update to describe CSV:
   - Change title to "Outcomes CSV Schema" (or rename file to OUTCOMES_CSV_SCHEMA.md — in-place update preferred per research Open Question 2)
   - Remove "Sheet name: Outcomes" (CSV has no sheets)
   - Add note: Source file is Outcomes.csv (UTF-8, quoted fields for commas in Description)
   - Preserve columns, forward-fill rules, load_outcomes_code_lookup expectations

4. **.planning/docs/HPC_UPLOAD_SYNC.md:** Replace Outcomes.xlsx with Outcomes.csv in sync commands and table.

5. **reports/modality_flags.md:** Update header "Outcomes.xlsx" → "Outcomes.csv" if present.
</action>
  <verify>python -m pytest tests/ -v</verify>
  <done>openpyxl removed; docs reference Outcomes.csv; pytest passes without openpyxl</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_load_outcomes_code_lookup.py tests/test_add_modality_flags.py -v (passes)
- Uninstall openpyxl in a fresh env: pip uninstall openpyxl -y; python -m pytest tests/ -v (passes)
- make test (or python -m pytest tests/ -v) succeeds
- CI runs without openpyxl in install step
</verification>

<success_criteria>
- load_outcomes_code_lookup reads Outcomes.csv via pd.read_csv; ffill semantics unchanged
- All path references use Outcomes.csv
- Tests pass with CSV mocks; no openpyxl in test code
- openpyxl removed from environment.yml and CI
- Docs and reports reference Outcomes.csv
</success_criteria>

<traceability>
Research file: 11-RESEARCH.md
- Pitfalls addressed: Pitfall 1 (keep_default_na), Pitfall 2 (quoted commas — default read_csv handles), Pitfall 3 (encoding — UTF-8 default), Pitfall 4 (header whitespace — ensure CSV has clean headers)
- Files updated per research § Files to Update
- Pattern 1 (read_csv + ffill) applied
- Clean cutover (no dual xlsx/csv) per Migration Strategy
</traceability>

<effort_estimate>
0.5–1 day (Claude execution: ~30–45 min)
</effort_estimate>

<output>
After completion, create .planning/phases/11-remove-xlsx-use-csv/11-01-SUMMARY.md
</output>
