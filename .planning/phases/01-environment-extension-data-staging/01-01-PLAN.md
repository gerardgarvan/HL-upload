---
phase: 01-environment-extension-data-staging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/paths.toml
  - src/__init__.py
  - src/load/__init__.py
  - src/load/config.py
  - src/load/schema.py
  - datastructure.txt
  - valuesets.csv
  - environment.yml
  - submit_job.sh
  - scripts/smoke_test.py
autonomous: true
requirements:
  - REQ-04
  - REQ-05
must_haves:
  truths:
    - "Config file references /orange source data and /blue scratch paths with hl-clean output section"
    - "Shared HL-EDA assets (datastructure.txt, valuesets.csv) are present in project root"
    - "Config loader returns Paths dataclass with parquet_dir field"
    - "Schema parser can extract 22 table filenames from datastructure.txt"
    - "SLURM template uses erin.mobley-hl.bcu account with 64GB memory and 4 CPUs"
    - "Smoke test script implements full CSV→date-parse→Parquet→verify pipeline"
    - "Environment spec includes polars and duckdb alongside existing HL-EDA packages"
  artifacts:
    - path: "config/paths.toml"
      provides: "HPC path configuration with output section"
      contains: "parquet_dir"
    - path: "src/load/config.py"
      provides: "TOML config loader returning Paths dataclass with parquet_dir"
      exports: ["load_config", "Paths"]
    - path: "src/load/schema.py"
      provides: "datastructure.txt parser and file verifier"
      exports: ["parse_datastructure", "verify_files_exist"]
    - path: "datastructure.txt"
      provides: "Table file manifest (22 CSV filenames)"
      min_lines: 20
    - path: "environment.yml"
      provides: "Conda environment spec with polars and duckdb"
      contains: "polars"
    - path: "submit_job.sh"
      provides: "SLURM batch template for HiPerGator"
      contains: "erin.mobley-hl.bcu"
    - path: "scripts/smoke_test.py"
      provides: "End-to-end CSV→Parquet validation script"
      min_lines: 50
  key_links:
    - from: "scripts/smoke_test.py"
      to: "config/paths.toml"
      via: "src/load/config.py load_config()"
      pattern: "load_config"
    - from: "scripts/smoke_test.py"
      to: "datastructure.txt"
      via: "src/load/schema.py parse_datastructure()"
      pattern: "parse_datastructure"
    - from: "submit_job.sh"
      to: "scripts/smoke_test.py"
      via: "python scripts/smoke_test.py"
      pattern: "smoke_test"
    - from: "src/load/config.py"
      to: "config/paths.toml"
      via: "tomllib.load()"
      pattern: "paths\\.toml"
---

<objective>
Create the complete project scaffold for the HL data loading & cleaning pipeline: configuration files referencing HL-EDA shared assets, source modules for config loading and schema parsing, conda environment spec with Polars+DuckDB, SLURM batch template, and smoke test script.

Purpose: Establish all local artifacts so the HPC deployment (Plan 02) has everything it needs. This mirrors the proven HL-EDA project structure with extensions for Parquet output.
Output: Complete project directory with config, source modules, environment spec, SLURM template, and smoke test script.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-environment-extension-data-staging/01-RESEARCH.md

# HL-EDA reference files to adapt from
@c:/cygwin64/home/Owner/HL-EDA/config/paths.toml
@c:/cygwin64/home/Owner/HL-EDA/EDA/src/load/config.py
@c:/cygwin64/home/Owner/HL-EDA/EDA/src/load/schema.py
@c:/cygwin64/home/Owner/HL-EDA/EDA/run_report.slurm
@c:/cygwin64/home/Owner/HL-EDA/EDA/environment.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project config and source modules referencing HL-EDA assets</name>
  <files>
    config/paths.toml
    src/__init__.py
    src/load/__init__.py
    src/load/config.py
    src/load/schema.py
    datastructure.txt
    valuesets.csv
  </files>
  <action>
1. Create `config/paths.toml` by adapting `c:/cygwin64/home/Owner/HL-EDA/config/paths.toml`. Keep the same `[paths]` section (data_root, scratch_root, datastructure_path, valuesets_path). Add a new `[paths.output]` section with `parquet_dir = "hl-clean/parquet"` (relative to scratch_root) and `logs_dir = "hl-clean/logs"`.

2. Create `src/__init__.py` and `src/load/__init__.py` as empty files (package markers).

3. Create `src/load/config.py` by adapting `c:/cygwin64/home/Owner/HL-EDA/EDA/src/load/config.py`:
   - Keep the `tomllib`/`tomli` import pattern and `_project_root()` helper.
   - Extend the `Paths` dataclass to add `parquet_dir: Path` field.
   - In `load_config()`, resolve `parquet_dir` from `raw["paths"]["output"]["parquet_dir"]` as `scratch_root / parquet_dir`. Make the output section optional (default parquet_dir to `scratch_root / "hl-clean" / "parquet"` if output section missing).
   - Keep the same `resolve()` helper for relative paths.

4. Create `src/load/schema.py` — copy directly from `c:/cygwin64/home/Owner/HL-EDA/EDA/src/load/schema.py` without modification. The `parse_datastructure()` and `verify_files_exist()` functions work as-is.

5. Copy `datastructure.txt` from `c:/cygwin64/home/Owner/HL-EDA/EDA/datastructure.txt` into the project root.

6. Copy `valuesets.csv` from `c:/cygwin64/home/Owner/HL-EDA/EDA/valuesets.csv` into the project root.
  </action>
  <verify>
Run `python -c "from src.load.config import load_config, Paths; print('config OK')"` from project root.
Run `python -c "from src.load.schema import parse_datastructure; p = parse_datastructure(__import__('pathlib').Path('datastructure.txt')); print(f'{len(p[1])} tables')"` — should print "22 tables".
Verify `config/paths.toml` contains `[paths.output]` section with `parquet_dir`.
Verify `valuesets.csv` exists and has >15000 lines.
  </verify>
  <done>
config/paths.toml has data_root, scratch_root, and parquet_dir. src/load/config.py loads it into a Paths dataclass with parquet_dir. src/load/schema.py parses datastructure.txt yielding 22 table filenames. datastructure.txt and valuesets.csv are present in project root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create environment spec, SLURM template, and smoke test script</name>
  <files>
    environment.yml
    submit_job.sh
    scripts/smoke_test.py
  </files>
  <action>
1. Create `environment.yml` as a draft spec extending HL-EDA's environment. Read `c:/cygwin64/home/Owner/HL-EDA/EDA/environment.yml` for the base packages. Create a new environment.yml with `name: hl-eda` (extending existing env) that includes all existing packages plus `polars` and `duckdb` from conda-forge. Do NOT pin exact versions for polars/duckdb — let the solver pick compatible versions. Include a comment noting this is a draft; the real export will be generated on HPC after installation via `mamba env export --no-builds`.

2. Create `submit_job.sh` by adapting `c:/cygwin64/home/Owner/HL-EDA/EDA/run_report.slurm`:
   - `--job-name=hl-clean`
   - `--account=erin.mobley-hl.bcu`
   - `--qos=erin.mobley-hl.bcu`
   - `--mem=64gb`
   - `--time=2:00:00`
   - `--cpus-per-task=4` (up from 2 — Polars auto-parallelizes)
   - `--output=logs/hl-clean_%j.log`
   - Keep the `module load conda` and `conda activate hl-eda` pattern.
   - `cd /blue/erin.mobley-hl.bcu/hl-clean`
   - Default command: `python scripts/smoke_test.py` (users will edit for later phases).

3. Create `scripts/smoke_test.py` implementing the full end-to-end validation:
   - Import `load_config` from `src.load.config` and `parse_datastructure` from `src.load.schema`.
   - Accept optional config path as CLI argument (default: `config/paths.toml`).
   - Step 1: Load config, verify data_root exists and is a directory.
   - Step 2: Parse datastructure.txt to get table list. Print table count.
   - Step 3: Verify at least one CSV exists (DEMOGRAPHIC_Mailhot_V1.csv).
   - Step 4: Load DEMOGRAPHIC CSV with `pl.read_csv()`. Print row/column counts.
   - Step 5: Parse BIRTH_DATE column: `pl.col("BIRTH_DATE").str.to_date("%d%b%Y", strict=False)`. Print dtype and null count before/after.
   - Step 6: Create parquet_dir (`paths.parquet_dir`), write `DEMOGRAPHIC.parquet` with `write_parquet()`.
   - Step 7: Read back with `pl.read_parquet()`. Assert shape matches. Assert BIRTH_DATE dtype is `pl.Date`.
   - Step 8: Verify DuckDB can read the Parquet: `import duckdb; con = duckdb.connect(); result = con.sql("SELECT COUNT(*) FROM read_parquet('...')").fetchone()`. Assert count matches.
   - Step 9: Print summary (CSV size, Parquet size, compression ratio).
   - Print `=== SMOKE TEST PASSED ===` on success, `=== SMOKE TEST FAILED ===` with error details on failure.
   - Use `sys.exit(0)` on success, `sys.exit(1)` on failure.
   - Wrap main logic in try/except to catch and report errors clearly.
  </action>
  <verify>
Run `python -c "import ast; ast.parse(open('scripts/smoke_test.py').read()); print('syntax OK')"` to verify Python syntax.
Run `python -c "import ast; ast.parse(open('submit_job.sh').read() if False else ''); print('skip')"` — just verify submit_job.sh exists.
Verify `environment.yml` contains both `polars` and `duckdb` in dependencies.
Verify `submit_job.sh` contains `--cpus-per-task=4` and `erin.mobley-hl.bcu`.
Verify `scripts/smoke_test.py` imports from `src.load.config` and `src.load.schema`.
  </verify>
  <done>
environment.yml lists polars and duckdb alongside HL-EDA base packages. submit_job.sh is a valid SLURM script with correct account, 64GB mem, 4 CPUs, pointing to /blue/erin.mobley-hl.bcu/hl-clean. scripts/smoke_test.py implements CSV load → SAS date parse → Parquet write → read-back verify → DuckDB verify pipeline.
  </done>
</task>

</tasks>

<verification>
1. All 10 files exist in the project tree with correct directory structure.
2. `python -c "from src.load.config import load_config"` succeeds.
3. `python -c "from src.load.schema import parse_datastructure"` succeeds.
4. `scripts/smoke_test.py` has valid Python syntax.
5. `config/paths.toml` is valid TOML with data_root, scratch_root, and parquet_dir.
6. `submit_job.sh` has all required SBATCH directives.
7. `environment.yml` has polars and duckdb in the dependency list.
</verification>

<success_criteria>
Complete project scaffold exists locally with all config, source modules, environment spec, SLURM template, and smoke test script. All Python files have valid syntax. Config loader correctly extends HL-EDA's Paths dataclass with parquet_dir. Schema parser handles the 22-table manifest. Ready for HPC deployment in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-extension-data-staging/01-01-SUMMARY.md`
</output>
