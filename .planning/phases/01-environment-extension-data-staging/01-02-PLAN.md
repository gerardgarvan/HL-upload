---
phase: 01-environment-extension-data-staging
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/setup_hpc.sh
  - environment.yml
autonomous: false
requirements:
  - REQ-01
  - REQ-04
  - REQ-05
must_haves:
  truths:
    - "polars and duckdb are importable in the conda env on HiPerGator"
    - "DEMOGRAPHIC CSV loads from /orange with Polars in under 5 seconds"
    - "SAS DATE9. BIRTH_DATE strings parse to Polars Date type without excess nulls"
    - "Parquet file written to /blue/erin.mobley-hl.bcu/hl-clean/parquet/"
    - "Round-trip read preserves row count and Date dtype"
    - "DuckDB reads the Parquet file and returns correct row count"
    - "Updated environment.yml exported with actual installed versions"
  artifacts:
    - path: "scripts/setup_hpc.sh"
      provides: "HPC deployment and environment setup automation"
      min_lines: 40
    - path: "environment.yml"
      provides: "Exported conda env with actual installed polars+duckdb versions"
      contains: "polars"
  key_links:
    - from: "scripts/setup_hpc.sh"
      to: "scripts/smoke_test.py"
      via: "python scripts/smoke_test.py"
      pattern: "smoke_test"
    - from: "scripts/setup_hpc.sh"
      to: "environment.yml"
      via: "mamba env export --no-builds"
      pattern: "env export"
---

<objective>
Create an HPC setup script that automates deployment to HiPerGator, then verify the full environment works via user-run checkpoint: conda env extension, smoke test execution, and environment export.

Purpose: Prove the entire Phase 1 pipeline works end-to-end on HPC with real data — Polars loads CSVs from /orange, parses SAS dates, writes Parquet to /blue, DuckDB reads it back. This is the gate before Phase 2 (full 22-table conversion).
Output: Working HPC environment with polars+duckdb; verified smoke test; exported environment.yml with actual versions.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-environment-extension-data-staging/01-RESEARCH.md
@.planning/phases/01-environment-extension-data-staging/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HPC deployment and setup script</name>
  <files>scripts/setup_hpc.sh</files>
  <action>
Create `scripts/setup_hpc.sh` — a bash script the user runs on HiPerGator after SSH-ing in. The script automates everything that can be automated on HPC:

1. **Header and safety:** `#!/bin/bash`, `set -euo pipefail`. Print banner with timestamp.

2. **Create project directories on /blue:**
   ```
   mkdir -p /blue/erin.mobley-hl.bcu/hl-clean/parquet
   mkdir -p /blue/erin.mobley-hl.bcu/hl-clean/logs
   mkdir -p /blue/erin.mobley-hl.bcu/hl-clean/scripts
   mkdir -p /blue/erin.mobley-hl.bcu/hl-clean/config
   mkdir -p /blue/erin.mobley-hl.bcu/hl-clean/src/load
   ```

3. **Verify source data accessible:**
   Check that `/orange/erin.mobley-hl.bcu/Mailhot_V1_20250915` exists and contains `DEMOGRAPHIC_Mailhot_V1.csv`. Print file size. If not found, print clear error and exit 1.

4. **Verify /blue has space:**
   Run `blue_quota 2>/dev/null || lfs quota -u $(whoami) /blue 2>/dev/null || echo "Could not check quota — verify manually"`.

5. **Conda environment setup:**
   ```
   module load conda
   source $(conda info --base)/etc/profile.d/conda.sh
   conda activate hl-eda
   ```
   Then attempt: `mamba install -y polars duckdb -c conda-forge`. If mamba fails, print fallback instructions for creating a new `hl-clean` env (from the research doc's fallback command).

6. **Verify installations:**
   ```
   python -c "import polars as pl; print(f'  Polars {pl.__version__}')"
   python -c "import duckdb; print(f'  DuckDB {duckdb.__version__}')"
   python -c "import pyarrow; print(f'  PyArrow {pyarrow.__version__}')"
   ```

7. **Copy project files to /blue working directory:**
   Print instructions for the user to copy/rsync project files from local workspace to `/blue/erin.mobley-hl.bcu/hl-clean/`. Include example rsync command (user fills in their local path):
   ```
   # From your local machine:
   # rsync -av --exclude='.planning' . hpg:/blue/erin.mobley-hl.bcu/hl-clean/
   ```
   Note: The script should also handle the case where files are already present (idempotent).

8. **Run smoke test (if project files present):**
   Check if `scripts/smoke_test.py` exists in `/blue/erin.mobley-hl.bcu/hl-clean/`. If yes, run it. If not, print "Copy project files first, then run: python scripts/smoke_test.py".

9. **Export environment:**
   `mamba env export --no-builds > environment.yml` (write to project dir on /blue).
   Print success message with instructions to copy back to local workspace.

10. **Print summary** of what was done and next steps.

Make the script idempotent — safe to run multiple times. Use color output (green for success, red for failure) via ANSI escape codes.
  </action>
  <verify>
Run `bash -n scripts/setup_hpc.sh` (syntax check — will work on Cygwin/Git Bash) to verify valid bash syntax.
Verify the script contains: `mkdir -p`, `mamba install`, `conda activate`, `smoke_test.py`, `env export`.
  </verify>
  <done>
scripts/setup_hpc.sh is a valid bash script that automates HPC directory creation, conda env extension with polars+duckdb, installation verification, smoke test execution, and environment export. Script is idempotent and has clear error messages.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify HPC environment and smoke test</name>
  <files>environment.yml</files>
  <action>
User deploys project to HiPerGator and runs the setup script. Steps:
1. SSH to HiPerGator: `ssh username@hpg.rc.ufl.edu`
2. Copy project files to HPC: `rsync -av --exclude='.planning' --exclude='.git' local_project_path/ hpg:/blue/erin.mobley-hl.bcu/hl-clean/`
3. Run the setup script: `cd /blue/erin.mobley-hl.bcu/hl-clean && bash scripts/setup_hpc.sh`
4. Expected output: Polars+DuckDB version prints, smoke test PASSED, environment.yml exported.
5. Copy exported environment.yml back: `scp hpg:/blue/erin.mobley-hl.bcu/hl-clean/environment.yml .`
6. Alternative: submit via SLURM: `sbatch submit_job.sh` and check `logs/hl-clean_*.log`.
  </action>
  <verify>
Smoke test output log shows: "[OK]" for each step, BIRTH_DATE dtype=Date, row count matches, Parquet file created on /blue, DuckDB query returns correct count, "SMOKE TEST PASSED" at end.
  </verify>
  <done>
User confirms: polars and duckdb installed, smoke test passed (DEMOGRAPHIC CSV loaded, SAS dates parsed, Parquet written and verified, DuckDB reads Parquet), environment.yml exported with actual versions.
  </done>
</task>

</tasks>

<verification>
1. `scripts/setup_hpc.sh` has valid bash syntax.
2. Smoke test passes on HiPerGator: DEMOGRAPHIC CSV loads, BIRTH_DATE parses to Date type, Parquet writes to /blue, round-trip verifies.
3. DuckDB reads the Parquet file and returns correct row count.
4. `environment.yml` is exported with actual polars+duckdb versions.
5. SLURM template successfully submits and runs (or can be used for future phases).
</verification>

<success_criteria>
User confirms: (1) polars and duckdb installed in conda env, (2) smoke test passed — DEMOGRAPHIC CSV loaded, SAS dates parsed, Parquet written and verified, DuckDB reads Parquet, (3) environment.yml exported with actual versions. The HPC environment is ready for Phase 2 (full 22-table CSV-to-Parquet conversion).
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-extension-data-staging/01-02-SUMMARY.md`
</output>
