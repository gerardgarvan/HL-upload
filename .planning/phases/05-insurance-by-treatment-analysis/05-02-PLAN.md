---
phase: 05-insurance-by-treatment-analysis
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/build_insurance_by_treatment.py
  - reports/insurance_by_treatment/chemotherapy_table.png
  - reports/insurance_by_treatment/radiation_table.png
  - reports/insurance_by_treatment/sct_table.png
  - reports/insurance_by_treatment/overview_table.png
  - reports/insurance_by_treatment/chemotherapy_table.html
  - reports/insurance_by_treatment/radiation_table.html
  - reports/insurance_by_treatment/sct_table.html
  - reports/insurance_by_treatment/overview_table.html
autonomous: false
requirements: []
must_haves:
  truths:
    - "PNG images render as color-coded tables with payer category background colors"
    - "HTML files are self-contained with inline CSS and open correctly in a browser"
    - "All 4 tables exist in all 3 formats (PNG, CSV, HTML) plus markdown"
    - "Colors are consistent across all tables (same payer = same color)"
    - "Tables are presentation-ready: readable fonts, clean alignment, cohort size in title"
  artifacts:
    - path: "reports/insurance_by_treatment/chemotherapy_table.png"
      provides: "Color-coded PNG table for chemo cohort"
    - path: "reports/insurance_by_treatment/chemotherapy_table.html"
      provides: "Styled HTML table for chemo cohort"
      contains: "<table"
    - path: "scripts/build_insurance_by_treatment.py"
      provides: "Complete script with all 3 output formats"
      min_lines: 300
  key_links:
    - from: "scripts/build_insurance_by_treatment.py"
      to: "matplotlib.pyplot"
      via: "import matplotlib"
      pattern: "import matplotlib"
    - from: "scripts/build_insurance_by_treatment.py"
      to: "seaborn"
      via: "color palette"
      pattern: "sns\\.color_palette"
---

<objective>
Add PNG image rendering and styled HTML output to the insurance-by-treatment script, completing all three required output formats.

Purpose: The tables must be presentation-ready for pasting into slides. PNG images with color-coded payer categories provide instant visual clarity. HTML tables enable screenshot or direct paste into documents. Together with CSV/markdown from Plan 01, all three output formats are covered.

Output: 4 PNG images + 4 HTML files added to reports/insurance_by_treatment/, plus visual verification checkpoint.
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-insurance-by-treatment-analysis/05-CONTEXT.md
@.planning/phases/05-insurance-by-treatment-analysis/05-RESEARCH.md
@.planning/phases/05-insurance-by-treatment-analysis/05-01-SUMMARY.md
@scripts/build_insurance_by_treatment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matplotlib PNG table rendering with payer category colors</name>
  <files>scripts/build_insurance_by_treatment.py</files>
  <action>
Add PNG table rendering to the existing script from Plan 01. Use matplotlib (already installed, zero new dependencies) and seaborn for the color palette.

**Color palette setup:**
- Use `seaborn.color_palette("Pastel1", n_colors=9)` for payer category colors
- Map each of the 9 payer categories to a consistent color:
  - Medicare, Medicaid, Dual eligible, Private, Other government, Self-pay, Other, Unavailable, Unknown
- Convert RGB tuples to hex via `matplotlib.colors.to_hex()`
- Define as module-level dict PAYER_COLORS after palette generation
- Header row color: dark blue (#2C5AA0) with white text

**PNG rendering function `_render_png(table_data, title, output_path)`:**
- Parameters: table_data (list of dicts from aggregation), title string (e.g. "Chemotherapy Cohort (N=192)"), output Path
- Create figure: `fig, ax = plt.subplots(figsize=(12, 7))` — wide enough for 4 columns (Payer Category + 3 data columns)
- Turn off axes: `ax.axis('off')`
- Build cellText: 2D list of strings, rows = 9 payer categories, cols = [Payer Category, Primary Insurance, First Treatment, Last Treatment]
- Build cellColours: 2D list matching shape. Header row = dark blue, data rows = PAYER_COLORS[category] repeated across all columns in that row
- Create table: `ax.table(cellText=..., colLabels=[...], cellColours=..., loc='center', cellLoc='center')`
- Set Payer Category column cells to left-align (cellLoc='left' for first column)
- Font: `table.auto_set_font_size(False)`, `table.set_fontsize(11)`
- Scale: `table.scale(1.2, 2.0)` for readability
- Header row styling: Set header cells to bold font, white text color
- Title: `fig.suptitle(title, fontsize=14, fontweight='bold', y=0.95)`
- Save: `plt.savefig(output_path, dpi=150, bbox_inches='tight', facecolor='white')` — white background
- Close: `plt.close(fig)` to free memory
- Handle matplotlib import gracefully: wrap in try/except ImportError, print "matplotlib not available; skipping PNG output" and continue

**Integration into main():**
- After building each table's data, call _render_png() for all 4 tables
- Output files: chemotherapy_table.png, radiation_table.png, sct_table.png, overview_table.png
- Print progress: "  {filename}.png" for each file written

**Pitfall avoidance (from RESEARCH.md):**
- cellColours array must be (data_rows x cols) — do NOT include header row in cellColours (matplotlib handles header separately when using colLabels)
- Actually verify: matplotlib.table() with colLabels + cellColours — cellColours should match cellText shape (data only, NOT including header). The colLabels get their own color via cell object styling after creation.
- Style header cells after table creation by iterating `table.get_celld()` and setting facecolor/textprops for row 0 cells
  </action>
  <verify>
1. Run `python -c "import ast; ast.parse(open('scripts/build_insurance_by_treatment.py').read()); print('Syntax OK')"`
2. Verify script imports matplotlib and seaborn
3. If data available, run script and verify 4 PNG files exist in reports/insurance_by_treatment/
4. Open a PNG to confirm: color-coded rows, readable text, title with cohort size, 4 columns
  </verify>
  <done>
Script generates 4 color-coded PNG table images using matplotlib. Payer categories have consistent Pastel1 colors across all tables. Headers are dark blue with white text. Title shows cohort name and size. PNG files saved at 150 DPI with white background.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add styled HTML table output with inline CSS</name>
  <files>scripts/build_insurance_by_treatment.py</files>
  <action>
Add HTML table generation to the script. Use simple string formatting with inline CSS — no Jinja2 or external templating.

**HTML rendering function `_render_html(table_data, title, output_path, payer_colors)`:**
- Parameters: table_data (list of dicts), title string, output Path, payer_colors dict
- Build complete HTML document with:
  - DOCTYPE html declaration
  - UTF-8 charset meta tag
  - Inline `<style>` block with:
    - Table: border-collapse, Arial font, centered margin, box-shadow for depth
    - th: dark blue (#2C5AA0) background, white text, 12px/16px padding, left-align, 1px solid border
    - td: 10px/16px padding, 1px solid #ddd border
    - CSS class per payer category: `.payer-medicare { background-color: #hex; }` etc.
    - CSS class names: lowercase, spaces replaced with hyphens (e.g., "dual-eligible", "other-government", "self-pay")
    - Hover effect on tbody rows: `tbody tr:hover { filter: brightness(0.95); }` (subtle darken, works with colored rows)
  - `<h2>` title centered with cohort size
  - `<table>` with thead (4 column headers) and tbody
  - Each `<tr>` gets CSS class `payer-{normalized-name}` for row coloring
  - Cell text escaped with `html.escape()` to prevent XSS (data may contain special characters)
  - Footer note: "Source: encounter_payer_summary.parquet | Generated: {timestamp}"

**Integration into main():**
- After building each table's data, call _render_html() for all 4 tables
- Output files: chemotherapy_table.html, radiation_table.html, sct_table.html, overview_table.html
- Print progress: "  {filename}.html" for each file written
- Import `html` module (stdlib) at top of file
- Import `datetime` for generation timestamp

**Quality checks:**
- HTML must be valid (proper open/close tags)
- Colors must match PNG output (same PAYER_COLORS dict)
- Tables should be self-contained (no external CSS/JS references)
- File should open and render correctly in any modern browser
  </action>
  <verify>
1. Verify script syntax: `python -c "import ast; ast.parse(open('scripts/build_insurance_by_treatment.py').read()); print('Syntax OK')"`
2. If data available, run script and verify 4 HTML files exist
3. Open an HTML file in browser — confirm colored rows, readable layout, title with cohort size
4. Verify HTML contains `<style>` block (inline CSS) and `html.escape` is used
  </verify>
  <done>
Script generates 4 styled HTML table files with inline CSS. Payer category rows are color-coded matching the PNG output. HTML is self-contained with no external dependencies. Tables open correctly in any browser with proper styling.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify presentation-ready visual output</name>
  <what-built>Complete insurance-by-treatment analysis script generating 4 tables in 3 formats:
- 4 PNG images (color-coded tables) in reports/insurance_by_treatment/
- 4 HTML files (styled tables) in reports/insurance_by_treatment/
- 4 CSV files + 1 markdown README in reports/insurance_by_treatment/

Total: 13 output files from a single script run.</what-built>
  <how-to-verify>
1. Run: `python scripts/build_insurance_by_treatment.py`
2. Open `reports/insurance_by_treatment/chemotherapy_table.png` — verify:
   - Title shows "Chemotherapy Cohort (N=XXX)"
   - 9 payer category rows with pastel background colors
   - 3 data columns: Primary Insurance, First Treatment, Last Treatment
   - Cell values are "N (XX.X%)" format
   - No suppressed/masked values (no "-" or special symbols)
3. Open `reports/insurance_by_treatment/chemotherapy_table.html` in browser — verify:
   - Same table structure and colors as PNG
   - Clean, presentation-appropriate styling
4. Open `reports/insurance_by_treatment/README.md` — verify 4 markdown tables present
5. Check all 4 treatment types have consistent visual style
  </how-to-verify>
  <resume-signal>Type "approved" if tables look presentation-ready, or describe specific issues (colors, alignment, readability, missing data)</resume-signal>
</task>

</tasks>

<verification>
1. `scripts/build_insurance_by_treatment.py` generates all 3 output formats (PNG, HTML, CSV+markdown)
2. PNG files are color-coded with seaborn Pastel1 palette, 150 DPI, white background
3. HTML files are self-contained with inline CSS, valid markup, proper escaping
4. Colors are consistent between PNG and HTML (same PAYER_COLORS dict)
5. All 4 tables (chemo, radiation, SCT, overview) exist in all formats
6. Visual output is presentation-ready (readable fonts, clean alignment, cohort sizes in titles)
</verification>

<success_criteria>
- 4 PNG images render as clean, color-coded tables pasteable into presentation slides
- 4 HTML files open in browser with styled, color-coded tables
- Color scheme is consistent across all tables and formats
- All output is in reports/insurance_by_treatment/ directory
- Script handles missing matplotlib gracefully (skips PNG, continues with other formats)
</success_criteria>

<output>
After completion, create `.planning/phases/05-insurance-by-treatment-analysis/05-02-SUMMARY.md`
</output>
