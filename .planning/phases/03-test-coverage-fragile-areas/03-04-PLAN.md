---
phase: 03-test-coverage-fragile-areas
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_validate/test_checkpoint.py
  - tests/test_clean/test_harmonize.py
  - tests/test_clean/test_outcomes_flags.py
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "Checkpoint row-count validation tested (strict and tolerance modes)"
    - "Checkpoint schema validation tested (missing columns, wrong dtypes)"
    - "validate_no_vanish tested for catastrophic data loss detection"
    - "Partner flag logic tested with SOURCE column values"
    - "Outcomes code lookup tested with schema validation"
  artifacts:
    - path: "tests/test_validate/test_checkpoint.py"
      provides: "Comprehensive checkpoint validation tests"
      min_lines: 150
    - path: "tests/test_clean/test_harmonize.py"
      provides: "Partner flag logic tests"
      min_lines: 80
    - path: "tests/test_clean/test_outcomes_flags.py"
      provides: "Outcomes code lookup with schema validation"
      min_lines: 60
  key_links:
    - from: "tests/test_validate/test_checkpoint.py"
      to: "src/validate/checkpoint.py"
      via: "import CheckpointError, validate_row_count, validate_schema, validate_no_vanish"
      pattern: "from src\\.validate\\.checkpoint import"
    - from: "tests/test_clean/test_harmonize.py"
      to: "src/clean/harmonize.py"
      via: "import add_partner_flags, PARTNER_FLAGS"
      pattern: "from src\\.clean\\.harmonize import"
---

<objective>
Comprehensive test coverage for checkpoint validation logic (row-count, schema, no-vanish), partner flag assignment, and outcomes code lookup with schema validation.

Purpose: Lock in correctness of Phase 2 checkpoint infrastructure and resolve TODO(audit) items for partner abbreviations (AUDIT-013) and Outcomes.csv schema (AUDIT-014).

Output: Tests validating checkpoints catch data loss and schema drift, partner flags work correctly
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-test-coverage-fragile-areas/03-CONTEXT.md
@.planning/phases/03-test-coverage-fragile-areas/03-RESEARCH.md
@docs/AUDIT_LOG.md

# Phase 2 Outputs (Checkpoint Module)
@.planning/phases/02-validation-suppression-hardening/02-01-SUMMARY.md

# Source Code Under Test
@src/validate/checkpoint.py
@src/clean/harmonize.py
@src/clean/outcomes_flags.py
</context>

<tasks>

<task type="auto">
  <name>Create checkpoint validation tests</name>
  <files>tests/test_validate/test_checkpoint.py</files>
  <action>
Create comprehensive tests for src/validate/checkpoint.py validation functions.

**Create directory:**
```bash
mkdir -p tests/test_validate
touch tests/test_validate/__init__.py
```

**Test file structure:**
```python
import pytest
import polars as pl
from src.validate.checkpoint import (
    CheckpointError,
    CheckpointResult,
    validate_row_count,
    validate_no_vanish,
    validate_schema
)
```

**Tests to implement:**

**1. test_validate_row_count_exact_match_pass:**
- Create DataFrame with 3 rows
- Call validate_row_count(df, "load", "DIAGNOSIS", expected=3, tolerance=0.0)
- Verify: result.passed=True, result.actual=3, result.expected=3

**2. test_validate_row_count_exact_match_fail:**
- Create DataFrame with 2 rows
- Call validate_row_count(df, "load", "DIAGNOSIS", expected=3, tolerance=0.0)
- Verify: raises CheckpointError with "expected=3" and "got=2" in message

**3. test_validate_row_count_tolerance_pass:**
- Create DataFrame with 98 rows
- Allow 2% loss: 100 * 0.02 = 2 rows
- Call validate_row_count(df, "dedup", "DIAGNOSIS", expected=100, tolerance=0.02)
- Verify: result.passed=True (delta=-2, within tolerance)

**4. test_validate_row_count_tolerance_fail:**
- Create DataFrame with 95 rows
- Allow 2% loss: 100 * 0.02 = 2 rows
- Call validate_row_count(df, "dedup", "DIAGNOSIS", expected=100, tolerance=0.02)
- Verify: raises CheckpointError (delta=-5, exceeds tolerance)

**5. test_validate_no_vanish_pass:**
- Create DataFrame with 100 rows
- Call validate_no_vanish(df, "filter", "DIAGNOSIS", min_rows=50)
- Verify: result.passed=True

**6. test_validate_no_vanish_fail:**
- Create DataFrame with 10 rows
- Call validate_no_vanish(df, "filter", "DIAGNOSIS", min_rows=50)
- Verify: raises CheckpointError with "expected_min=50" and "got=10"

**7. test_validate_schema_all_columns_present_pass:**
- Create DataFrame: {"ID": ["PT001"], "DX": ["C81.10"], "DX_TYPE": ["10"]}
- Expected schema: {"ID": pl.Utf8, "DX": pl.Utf8, "DX_TYPE": pl.Utf8}
- Call validate_schema(df, "typing", "DIAGNOSIS", expected_schema)
- Verify: result.passed=True

**8. test_validate_schema_missing_column_fail:**
- Create DataFrame: {"ID": ["PT001"], "DX": ["C81.10"]} (missing DX_TYPE)
- Expected schema: {"ID": pl.Utf8, "DX": pl.Utf8, "DX_TYPE": pl.Utf8}
- Call validate_schema(df, "typing", "DIAGNOSIS", expected_schema)
- Verify: raises CheckpointError with "missing_columns" and "DX_TYPE" in message

**9. test_validate_schema_wrong_dtype_fail:**
- Create DataFrame: {"ID": ["PT001"], "DX_DATE": ["2025-01-01"]} (String, not Date)
- Expected schema: {"ID": pl.Utf8, "DX_DATE": pl.Date}
- Call validate_schema(df, "typing", "DIAGNOSIS", expected_schema)
- Verify: raises CheckpointError with "dtype_mismatch" in message

**10. test_validate_schema_dtype_flexibility:**
- Create DataFrame: {"ID": ["PT001"], "DX_DATE": ["2025-01-01"]} (date detection failed, kept as String)
- Expected schema: {"ID": pl.Utf8, "DX_DATE": (pl.Date, pl.Utf8)} (accept either)
- Call validate_schema(df, "typing", "DIAGNOSIS", expected_schema)
- Verify: result.passed=True (String is in allowed tuple)

**Use research examples from 03-RESEARCH.md "Testing Checkpoint Validation"**
  </action>
  <verify>
Run: `pytest tests/test_validate/test_checkpoint.py -v`

Check: All checkpoint modes tested (strict, tolerance, no-vanish, schema), edge cases covered
  </verify>
  <done>
tests/test_validate/test_checkpoint.py exists with comprehensive checkpoint tests covering row-count, schema, and no-vanish validation; all tests passing
  </done>
</task>

<task type="auto">
  <name>Create partner flag and outcomes tests</name>
  <files>tests/test_clean/test_harmonize.py, tests/test_clean/test_outcomes_flags.py</files>
  <action>
Create tests for partner flag assignment and outcomes code lookup.

**Test file 1: tests/test_clean/test_harmonize.py**

```python
import pytest
import polars as pl
from polars.testing import assert_frame_equal
from src.clean.harmonize import add_partner_flags, PARTNER_FLAGS
```

**Tests for partner flags (AUDIT-013):**

**1. test_add_partner_flags_ams:**
- Create test data: SOURCE=["AMS", "FLM", "UMI"]
- Call add_partner_flags(df, partner_col="SOURCE")
- Verify: AMS and UMI have ICD_MAPPED=1, FLM has CLAIMS_ONLY=1
- Use assert_frame_equal to verify flag columns

**2. test_add_partner_flags_unrecognized_source:**
- Create test data: SOURCE=["AMC", "XYZ"] (unrecognized abbreviations)
- Call add_partner_flags(df, partner_col="SOURCE")
- Verify: All flags are 0 (no match)
- Add comment: "AUDIT-013: Verify partner abbreviations (AMS, UMI, FLM, VRT) match current data sources. Unrecognized SOURCE values get no flags."

**3. test_partner_flags_constants:**
- Verify PARTNER_FLAGS dict has expected structure:
  - "ICD_MAPPED": {"AMS", "UMI"}
  - "CLAIMS_ONLY": {"FLM"}
  - "DEATH_ONLY": {"VRT"}
- Document: "AUDIT-013: Partner abbreviations hardcoded - validate against actual SOURCE column values in production data"

---

**Test file 2: tests/test_clean/test_outcomes_flags.py**

**Reorganize existing test_load_outcomes_code_lookup.py:**
- Move existing tests/test_load_outcomes_code_lookup.py to tests/test_clean/test_outcomes_flags.py
- Preserve existing test behavior

**Enhance with schema validation (AUDIT-014):**

**1. test_load_outcomes_code_lookup_valid_schema:**
- Create minimal mock Outcomes.csv data:
  - Columns: ["Modality", "Code system", "Code"]
  - Rows: [["Chemotherapy", "J-code", "J9035"], ["Radiation", "CPT", "77401"]]
- Write to temp CSV, load with load_outcomes_code_lookup()
- Verify: returns dict with (modality, code_system, code) → modality mappings

**2. test_load_outcomes_code_lookup_missing_column_fail:**
- Create mock CSV with wrong columns: ["Treatment", "System", "Code"] (missing "Modality")
- Attempt to load
- Verify: raises ValueError with "expected columns" message
- Add docstring: "AUDIT-014: Schema validation - load_outcomes_code_lookup() should fail fast if Outcomes.csv columns renamed or reordered"

**3. test_load_outcomes_code_lookup_empty_file:**
- Create empty CSV (header only, no data rows)
- Load and verify: returns empty dict (no codes to map)

**If load_outcomes_code_lookup() doesn't validate schema, ADD schema check:**
```python
expected_cols = ["Modality", "Code system", "Code"]
if not all(col in df.columns for col in expected_cols):
    raise ValueError(f"Outcomes.csv missing expected columns. Expected: {expected_cols}, Got: {df.columns}")
```

**Systematically resolve AUDIT-013 and AUDIT-014:**
- AUDIT-013: Document partner abbreviations in test, add TODO for production validation
- AUDIT-014: Add schema validation to load_outcomes_code_lookup() if missing, test failure mode
  </action>
  <verify>
Run: `pytest tests/test_clean/test_harmonize.py tests/test_clean/test_outcomes_flags.py -v`

Check: Partner flags tested with known abbreviations, outcomes schema validation added and tested
  </verify>
  <done>
tests/test_clean/test_harmonize.py and tests/test_clean/test_outcomes_flags.py exist with partner flag tests and outcomes schema validation; AUDIT-013 and AUDIT-014 resolved; all tests passing
  </done>
</task>

</tasks>

<verification>
- pytest runs successfully: `pytest tests/test_validate/ tests/test_clean/ -v`
- Checkpoint validation tested (row-count strict/tolerance, schema, no-vanish)
- Partner flags tested with SOURCE column values
- Outcomes code lookup tested with schema validation
- AUDIT-013 (partner abbreviations) and AUDIT-014 (Outcomes schema) resolved
</verification>

<success_criteria>
- Checkpoint validation has comprehensive coverage
- Row-count validation tested in strict and tolerance modes
- Schema validation tested for missing columns and wrong dtypes
- validate_no_vanish tested for catastrophic data loss
- Partner flag logic tested with known abbreviations
- Outcomes code lookup tested with schema validation
- All tests pass
- AUDIT-013 and AUDIT-014 resolved
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage-fragile-areas/03-04-SUMMARY.md`
</output>
