---
phase: 03-test-coverage-fragile-areas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_report/test_suppression.py
  - tests/test_report/test_quality_report.py
  - tests/test_clean/test_dedup.py
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "Suppression tested at boundary values (0, 1, 10, 11)"
    - "Report structure validated (expected columns present)"
    - "Aggregation correctness spot-checked with known inputs"
    - "Dedup logic tested with null keys and composite keys"
  artifacts:
    - path: "tests/test_report/test_suppression.py"
      provides: "Boundary value tests for HIPAA suppression"
      min_lines: 80
    - path: "tests/test_report/test_quality_report.py"
      provides: "Report structure and aggregation tests"
      min_lines: 100
    - path: "tests/test_clean/test_dedup.py"
      provides: "Deduplication logic tests including null key handling"
      min_lines: 120
  key_links:
    - from: "tests/test_report/test_suppression.py"
      to: "src/report/suppression.py"
      via: "import suppress, flag_small_cell"
      pattern: "from src\\.report\\.suppression import"
    - from: "tests/test_clean/test_dedup.py"
      to: "src/clean/dedup.py"
      via: "import flag_duplicates, DEDUP_KEYS"
      pattern: "from src\\.clean\\.dedup import"
---

<objective>
Comprehensive test coverage for report generation (structure, suppression, aggregation) and deduplication logic (null key handling, composite keys).

Purpose: Lock in correctness of HIPAA suppression (centralized in Phase 2) and dedup logic which has 2 MEDIUM TODO(audit) items (AUDIT-004: null keys, AUDIT-007: suppression consistency, AUDIT-010: VITAL dedup key).

Output: Tests validating report outputs apply suppression correctly and dedup handles edge cases
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-test-coverage-fragile-areas/03-CONTEXT.md
@.planning/phases/03-test-coverage-fragile-areas/03-RESEARCH.md
@docs/AUDIT_LOG.md

# Phase 2 Outputs
@.planning/phases/02-validation-suppression-hardening/02-02-SUMMARY.md

# Source Code Under Test
@src/report/suppression.py
@src/report/quality_report.py
@src/clean/dedup.py
</context>

<tasks>

<task type="auto">
  <name>Reorganize and enhance existing suppression tests</name>
  <files>tests/test_report/test_suppression.py</files>
  <action>
Move existing tests/test_suppress.py and tests/test_flag_small_cell.py to tests/test_report/test_suppression.py to mirror src/ structure.

**Create directory:**
```bash
mkdir -p tests/test_report
touch tests/test_report/__init__.py
```

**Reorganize existing tests:**
- Read existing tests/test_suppress.py (has basic suppress() tests)
- Read existing tests/test_flag_small_cell.py (has basic flag_small_cell() tests)
- Merge into tests/test_report/test_suppression.py
- Preserve existing test behavior (don't break passing tests)

**Enhance with exhaustive boundary value tests (use pytest.mark.parametrize):**

**1. test_suppress_boundary_values (EXPANDED):**
- Parametrize: value x expected
- Cases:
  - (0, "0") - Zero is safe to display (no PHI)
  - (1, "-") - Suppress small cell
  - (5, "-") - Suppress mid-range small cell
  - (10, "-") - Suppress at threshold (inclusive)
  - (11, "11") - Above threshold, safe to display
  - (100, "100") - Large value, safe
  - (None, "-") - Null handled gracefully (edge case)
  - (-1, "-1") - Negative (invalid but shouldn't crash)
- Use ids for clarity: ids=["zero", "one", "mid_small", "threshold", ...]

**2. test_flag_small_cell_boundary_values (EXPANDED):**
- Similar parametrize for flag_small_cell()
- Expected outputs: "0", "1 ⚠", "10 ⚠", "11" (no flag)

**3. test_suppression_consistency (AUDIT-007):**
- Verify suppress() and flag_small_cell() use same threshold
- Test that both check SMALL_CELL_THRESHOLD=10
- Document difference: suppress() hides value ("-"), flag_small_cell() shows with warning ("N ⚠")
- Add docstring: "AUDIT-007: Suppression strategy - suppress() for CSV (PHI protection), flag_small_cell() for markdown (internal review)"

**Update imports:**
```python
import pytest
from src.report.suppression import suppress, flag_small_cell, SMALL_CELL_THRESHOLD
```
  </action>
  <verify>
Run: `pytest tests/test_report/test_suppression.py -v`

Check: Existing tests still pass, new boundary value tests added, parametrize cases visible
  </verify>
  <done>
tests/test_report/test_suppression.py exists with reorganized and enhanced suppression tests, boundary values covered, AUDIT-007 documented, all tests passing
  </done>
</task>

<task type="auto">
  <name>Create report structure and aggregation tests</name>
  <files>tests/test_report/test_quality_report.py</files>
  <action>
Create structural + spot-check tests for quality report generation.

**Test file structure:**
```python
import pytest
import polars as pl
from polars.testing import assert_frame_equal
from src.report.quality_report import (
    # Import relevant functions - check quality_report.py exports
)
```

**Testing strategy (from 03-CONTEXT.md "Claude's Discretion"):**
- Structural tests: Verify columns present, dtypes correct, no crashes
- Spot-check tests: Verify suppression at boundary values, aggregation correctness on known inputs
- NOT exact value matching (too brittle)

**Tests to implement:**

**1. test_quality_metrics_structure:**
- Create minimal test DataFrame with quality metrics
- Verify expected columns exist:
  - table, completeness_pct, conformance_pct, plausibility_issues (adjust based on actual quality_report.py output)
- Use pl.DataFrame schema inspection: df.columns, df.dtypes

**2. test_suppression_applied_in_reports:**
- Create test data with small cell counts (1, 5, 10, 11, 100)
- Generate report aggregation (group by, count)
- Apply suppression using suppress()
- Verify small cells become "-", large cells display actual count
- Use research example from 03-RESEARCH.md "Testing Report Generation with Suppression"

**3. test_aggregation_correctness_spot_check:**
- Create known input: 3 records with DX="C81.10", 5 records with DX="C81.11"
- Group by DX, count
- Verify counts: C81.10 → 3 (suppressed to "-"), C81.11 → 5 (suppressed to "-")
- Spot-check: aggregation math is correct before suppression applied

**4. test_report_no_crashes_empty_input:**
- Edge case: empty DataFrame input
- Verify report generation doesn't crash, returns empty or placeholder output

**Use conftest.py fixtures if helpful (make_diagnosis_df for test data)**

**Note:** Quality report implementation may vary - adapt tests to actual functions exported by quality_report.py. Focus on structure + spot-checks, not exact value matching.
  </action>
  <verify>
Run: `pytest tests/test_report/test_quality_report.py -v`

Check: Structure tests pass, suppression spot-checks work, aggregation correctness verified
  </verify>
  <done>
tests/test_report/test_quality_report.py exists with structural and spot-check tests for report generation, suppression verified, all tests passing
  </done>
</task>

<task type="auto">
  <name>Create deduplication tests with null key handling</name>
  <files>tests/test_clean/test_dedup.py</files>
  <action>
Create comprehensive tests for src/clean/dedup.py including null key handling and composite key edge cases.

**Create directory:**
```bash
mkdir -p tests/test_clean
touch tests/test_clean/__init__.py
```

**Test file structure:**
```python
import pytest
import polars as pl
from polars.testing import assert_frame_equal
from src.clean.dedup import flag_duplicates, DEDUP_KEYS
```

**Tests to implement:**

**1. test_flag_duplicates_composite_key:**
- Test DIAGNOSIS dedup key: ["ID", "DX_DATE", "DX"]
- Create test data:
  - Row 1: ID="PT001", DX_DATE=2020-01-01, DX="C81.10"
  - Row 2: ID="PT001", DX_DATE=2020-01-01, DX="C81.10" (exact duplicate)
  - Row 3: ID="PT001", DX_DATE=2020-01-02, DX="C81.10" (different date, not duplicate)
- Verify: Rows 1 and 2 flagged IS_DUPLICATE=1, Row 3 has IS_DUPLICATE=0

**2. test_flag_duplicates_null_keys_not_matched (AUDIT-004):**
- Test Polars is_duplicated() behavior: null != null
- Create test data:
  - Row 1: ID="PT001", DX_DATE=None, DX="C81.10"
  - Row 2: ID="PT001", DX_DATE=None, DX="C81.10" (null dates)
- Verify: Rows 1 and 2 are NOT flagged as duplicates (nulls don't match)
- Add docstring: "AUDIT-004: Null key behavior - rows with null keys are NOT flagged as duplicates (null != null in Polars)"

**3. test_flag_duplicates_all_occurrences_flagged:**
- Verify ALL occurrences of duplicate key are flagged (not just subsequent)
- Create test data:
  - Row 1: ID="PT001", DX="C81.10" (first occurrence)
  - Row 2: ID="PT001", DX="C81.10" (second occurrence)
- Verify: Both rows have IS_DUPLICATE=1

**4. test_flag_duplicates_vital_key (AUDIT-010):**
- Test VITAL dedup key: ["ID", "MEASURE_DATE"]
- Create test data simulating same-day multi-vital scenario:
  - Row 1: ID="PT001", MEASURE_DATE=2020-01-01, HT=170, WT=70
  - Row 2: ID="PT001", MEASURE_DATE=2020-01-01, HT=None, WT=70 (same date, different vital type)
- Verify current behavior: Both flagged as duplicates (current key doesn't include vital type)
- Add comment: "AUDIT-010: VITAL dedup key missing vital-type discriminator - same-day vitals of different types incorrectly flagged as duplicates. Fix requires schema check for VITAL_SOURCE column."
- If test reveals bug, document as TODO for follow-up (requires schema investigation)

**5. test_dedup_keys_table_coverage:**
- Verify all expected tables have dedup keys defined
- Check DEDUP_KEYS.keys() includes: DIAGNOSIS, PROCEDURES, LAB_RESULT_CM, VITAL, PRESCRIBING
- Document which tables are NOT in DEDUP_KEYS (intentionally, because single-row-per-patient tables)

**Systematically resolve AUDIT-004 and AUDIT-010:**
- AUDIT-004: Test confirms null behavior, document in test docstring
- AUDIT-010: Test exposes issue, document as needing schema investigation (not fixable without checking VITAL columns)
  </action>
  <verify>
Run: `pytest tests/test_clean/test_dedup.py -v`

Check: Composite key tests pass, null key behavior verified, VITAL edge case documented
  </verify>
  <done>
tests/test_clean/test_dedup.py exists with comprehensive dedup tests including null key handling, composite keys, and VITAL edge case; AUDIT-004 and AUDIT-010 documented; all tests passing
  </done>
</task>

</tasks>

<verification>
- pytest runs successfully: `pytest tests/test_report/ tests/test_clean/ -v`
- Suppression boundary values tested (0, 1, 10, 11)
- Report structure validated (columns exist, types correct)
- Dedup null key behavior verified (nulls don't match)
- AUDIT-004, AUDIT-007, AUDIT-010 resolved or documented
</verification>

<success_criteria>
- Report suppression has boundary value coverage
- Report structure validated with spot-checks
- Dedup logic tested with null keys and composite keys
- Existing tests reorganized into mirrored structure
- All tests pass
- AUDIT-004, AUDIT-007, AUDIT-010 resolved
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage-fragile-areas/03-03-SUMMARY.md`
</output>
