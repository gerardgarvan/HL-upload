---
phase: 03-test-coverage-fragile-areas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_report/test_payer_logic.py
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Payer fallback logic tested for all sentinel combinations (NI/UN/OT/99/9999)"
    - "Dual-eligible detection tested with all code combinations (14/141/142)"
    - "Payer fallback chain tested when primary is sentinel, secondary is valid"
    - "INCLUDE_99_AS_SENTINEL flag tested in both True/False modes"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Factory fixtures for ENCOUNTER DataFrames with payer columns"
      min_lines: 50
    - path: "tests/test_report/test_payer_logic.py"
      provides: "Exhaustive payer logic edge case tests"
      min_lines: 200
  key_links:
    - from: "tests/test_report/test_payer_logic.py"
      to: "src/report/encounter_payer_summary.py"
      via: "import _collapse_payer_category, _effective_payer_and_dual_exprs"
      pattern: "from src\\.report\\.encounter_payer_summary import"
---

<objective>
Comprehensive test coverage for payer logic edge cases including effective payer derivation, dual-eligible detection, fallback chains, and sentinel value handling.

Purpose: Lock in correctness of the most complex logic in the pipeline (payer categorization and dual-eligible detection) which has 5 HIGH/MEDIUM TODO(audit) items flagged in Phase 1.

Output: Exhaustive parametrized tests for payer logic with PCORnet-realistic test data
</objective>

<execution_context>
@C:/Users/Owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage-fragile-areas/03-CONTEXT.md
@.planning/phases/03-test-coverage-fragile-areas/03-RESEARCH.md
@docs/AUDIT_LOG.md

# Phase 2 Outputs (Suppression Module)
@.planning/phases/02-validation-suppression-hardening/02-02-SUMMARY.md

# Source Code Under Test
@src/report/encounter_payer_summary.py
</context>

<tasks>

<task type="auto">
  <name>Create conftest.py with ENCOUNTER factory fixture</name>
  <files>tests/conftest.py</files>
  <action>
Create shared pytest fixtures for building test DataFrames with PCORnet-realistic data.

**Factory fixture for ENCOUNTER table:**
- Function name: `make_encounter_df()`
- Returns function that accepts kwargs for customization
- Default parameters: n_rows=3, patid_col="ID", payer_primary=None, payer_secondary=None, admit_dates=None, enc_types=None
- Default payer codes: payer_primary=["11"] * n_rows (Medicare FFS), payer_secondary=["21"] * n_rows (Medicaid FFS)
- Default dates: [date(2025, 1, i+1) for i in range(n_rows)]
- Default enc_types: ["IP"] * n_rows (inpatient)
- Returns pl.DataFrame with columns: ID, ENCOUNTERID, PAYER_TYPE_PRIMARY, PAYER_TYPE_SECONDARY, ADMIT_DATE, ENC_TYPE

**Use research pattern from 03-RESEARCH.md section "Pattern 1: Factory Functions in conftest.py"**

**PCORnet code realism:**
- Use actual PCORnet payer codes: "11" (Medicare FFS), "21" (Medicaid FFS), "14"/"141"/"142" (dual-eligible), "51" (Private)
- Use sentinel values: "NI" (no information), "UN" (unknown), "OT" (other), "99"/"9999" (unavailable)
- Do NOT use simple integers (1, 2, 3) or generic strings ("payer1", "payer2")

Import structure:
```python
import polars as pl
from datetime import date
import pytest
```
  </action>
  <verify>
Run: `pytest tests/conftest.py --collect-only` (should show fixtures)

Check: conftest.py contains @pytest.fixture decorator and make_encounter_df function that returns a factory
  </verify>
  <done>
conftest.py exists with make_encounter_df factory fixture returning function that builds ENCOUNTER DataFrames with PCORnet-realistic payer codes
  </done>
</task>

<task type="auto">
  <name>Create exhaustive payer logic tests</name>
  <files>tests/test_report/test_payer_logic.py</files>
  <action>
Create comprehensive test coverage for src/report/encounter_payer_summary.py payer logic.

**Test file structure:**
```python
import pytest
import polars as pl
from polars.testing import assert_series_equal
from src.report.encounter_payer_summary import (
    _collapse_payer_category,
    INVALID_PAYER,
    DUAL_ELIGIBLE_CODES,
    INCLUDE_99_AS_SENTINEL
)
```

**Tests to implement (use pytest.mark.parametrize for ALL edge case tests):**

**1. test_collapse_payer_category_all_pcornet_codes:**
- Parametrize: all PCORnet payer codes + sentinel values
- Codes to test:
  - Valid: "11" (Medicare FFS) → "Medicare", "121" (Medicare Advantage) → "Medicare"
  - Valid: "21" (Medicaid FFS) → "Medicaid", "221" (Medicaid managed) → "Medicaid"
  - Valid: "51" (Commercial) → "Private", "61" (Self-insured) → "Private"
  - Valid: "31" (VA) → "Other government", "41" (Corrections - NOT dual) → "Other government"
  - Valid: "81" (No payment) → "No payment / Self-pay"
  - Sentinels: "NI" → "Unknown", "UN" → "Unknown", "OT" → "Unknown"
  - Sentinels: "99" → "Unavailable", "9999" → "Unavailable"
  - Edge: None → "Unknown", "" → "Unknown", "   " (whitespace) → "Unknown"
  - Edge: "ZZ" (unrecognized) → "Other"
- Use parametrize with ids for readability: ids=["medicare_ffs", "medicare_advantage", ...]

**2. test_dual_eligible_code_detection:**
- Parametrize: all dual-eligible codes ("14", "141", "142")
- Verify each is in DUAL_ELIGIBLE_CODES set
- Verify "41" (Corrections) is NOT in DUAL_ELIGIBLE_CODES (common confusion)

**3. test_payer_fallback_to_secondary:**
- Test primary is sentinel → use secondary
- Cases:
  - ("NI", "21") → effective payer "21" (Medicaid)
  - ("UN", "11") → effective payer "11" (Medicare)
  - ("OT", "51") → effective payer "51" (Private)
  - (None, "21") → effective payer "21" (null primary fallback)
  - ("", "21") → effective payer "21" (empty string fallback)

**4. test_payer_fallback_when_secondary_also_invalid:**
- Test both primary and secondary are sentinels
- Cases:
  - ("NI", "UN") → both invalid, effective payer is what? (document current behavior)
  - (None, None) → both null
  - ("", "") → both empty
- Add comment in test: "TODO(audit) AUDIT-005: Check if fallback should continue or stop at secondary"

**5. test_dual_eligible_detection_with_null_secondary (AUDIT-005):**
- Test primary is 14/141/142, secondary is null
- Expected behavior per AUDIT-005: dual_eligible should be 1 based on primary alone
- If current code sets dual_eligible=0, FIX the code (per user constraint: "If current code is wrong, FIX the code in this phase")
- Cases:
  - ("14", None) → dual_eligible=1
  - ("141", None) → dual_eligible=1
  - ("142", None) → dual_eligible=1

**6. test_include_99_as_sentinel_flag (AUDIT-001):**
- Test INCLUDE_99_AS_SENTINEL in both True and False modes
- When False (current default): "99" → "Unavailable" category, no fallback
- When True: "99" treated like "NI", triggers fallback to secondary
- Use monkeypatch or mock to test both flag states:
```python
def test_99_sentinel_false(monkeypatch):
    monkeypatch.setattr("src.report.encounter_payer_summary.INCLUDE_99_AS_SENTINEL", False)
    # Test 99 maps to Unavailable
```

**All tests use PCORnet codes from 03-RESEARCH.md section "Pitfall 4: Testing with Unrealistic Data"**

**Systematically resolve AUDIT-001 and AUDIT-005:**
- Read current behavior from encounter_payer_summary.py
- If dual-eligible detection is wrong (ignores 14/141/142 when secondary is null), FIX it
- Document INCLUDE_99_AS_SENTINEL decision in test docstrings
  </action>
  <verify>
Run: `pytest tests/test_report/test_payer_logic.py -v`

Check: All tests pass, parametrize tables show multiple cases per test, edge cases covered
  </verify>
  <done>
tests/test_report/test_payer_logic.py exists with exhaustive payer edge case coverage using parametrize, all tests passing, AUDIT-001 and AUDIT-005 resolved with fixes or documentation
  </done>
</task>

</tasks>

<verification>
- pytest runs successfully: `pytest tests/test_report/test_payer_logic.py -v`
- All parametrize cases execute (check test count: should be 30+ test cases from parametrize expansion)
- PCORnet codes used in test data (not integers or generic strings)
- AUDIT-001 (99/9999 semantics) and AUDIT-005 (dual-eligible with null secondary) addressed
</verification>

<success_criteria>
- Payer logic has comprehensive edge case coverage
- All PCORnet payer codes + sentinels tested
- Dual-eligible detection tested with all code combinations
- Payer fallback chain tested for all sentinel scenarios
- INCLUDE_99_AS_SENTINEL flag behavior tested
- Tests use factory fixtures from conftest.py
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage-fragile-areas/03-01-SUMMARY.md`
</output>
