---
phase: 17-dual-eligible-as-payer-level
plan: 01
type: execute
wave: 2
depends_on: [16]
files_modified:
  - src/report/encounter_payer_summary.py
  - scripts/build_insurance_summary.py
  - docs/CODEBOOK.md
  - docs/PAYER_VARIABLES_AND_CATEGORIES.md
autonomous: true
requirements: [REQ-03, REQ-06]
must_haves:
  truths:
    - '"Dual eligible" is a category level of each payer variable (Medicare, Medicaid, Dual eligible, Private, ...)'
    - "When an encounter is dual-eligible, its payer category is 'Dual eligible' instead of Medicare/Medicaid"
    - "PAYER_CATEGORY_PRIMARY, AT_FIRST_DX, AT_FIRST_CHEMO, AT_LAST_CHEMO, MOST_FREQUENT_AT_CHEMO can all take value 'Dual eligible'"
  artifacts:
    - path: src/report/encounter_payer_summary.py
      provides: "Category = Dual eligible when dual_eligible; _payer_at_date returns _dual_eligible; enc_chemo category uses dual_eligible"
    - path: scripts/build_insurance_summary.py
      provides: "PAYER_CATEGORY_ORDER includes 'Dual eligible'"
    - path: docs/CODEBOOK.md
      provides: "Dual eligible listed as payer category level"
  key_links:
    - from: encounter_payer_summary.py
      to: "valid_enc, _payer_at_date, enc_chemo"
      via: "Assign 'Dual eligible' when dual_eligible else _collapse_payer_category(effective_payer)"
---

<objective>
Make "Dual eligible" a level of each payer variable. When an encounter is dual-eligible, assign payer category "Dual eligible" instead of Medicare or Medicaid. Apply to PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, AT_FIRST_CHEMO, AT_LAST_CHEMO, PAYER_CATEGORY_MOST_FREQUENT_AT_CHEMO. Add "Dual eligible" to report category order and docs.
</objective>

<context>
@.planning/phases/17-dual-eligible-as-payer-level/17-RESEARCH.md
@src/report/encounter_payer_summary.py
@scripts/build_insurance_summary.py
@docs/PAYER_VARIABLES_AND_CATEGORIES.md
</context>

<prerequisites>
- Phase 16 complete (effective payer, dual_eligible per encounter, DUAL_ELIGIBLE column in parquet).
- Encounter-level dual_eligible and effective_payer already computed in encounter_payer_summary.py.
</prerequisites>

<tasks>

<task type="auto">
  <name>Task 1: Category = "Dual eligible" when dual_eligible in encounter_payer_summary.py</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- Add helper or inline rule: for each encounter row, payer category = "Dual eligible" when dual_eligible == 1, else _collapse_payer_category(effective_payer). Use string "Dual eligible" (capital D, lowercase rest).
- valid_enc: currently selects PATID_COL, effective_payer and maps to PAYER_CATEGORY via _collapse_payer_category. Change to: valid_enc must include dual_eligible from the enc scan (add dual_eligible to the select so we have it per row). Then compute PAYER_CATEGORY per row as "Dual eligible" if dual_eligible else _collapse_payer_category(effective_payer). Use map_batches or a single expression so all valid_enc rows get the correct category.
- All downstream uses of valid_enc (PAYER_CATEGORY_PRIMARY, N_DISTINCT_PAYER_CATEGORIES, PAYER_TRANSITION) then automatically use the new category level.
  </action>
  <verify>Grep for "Dual eligible" in encounter_payer_summary.py; run build_encounter_payer_summary with test data and confirm PAYER_CATEGORY_PRIMARY can be "Dual eligible".</verify>
  <done>valid_enc PAYER_CATEGORY uses "Dual eligible" when dual_eligible; primary/mode and transition use it.</done>
</task>

<task type="auto">
  <name>Task 2: _payer_at_date return _dual_eligible; use for AT_FIRST_DX, AT_FIRST_CHEMO, AT_LAST_CHEMO</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- _payer_at_date: currently returns PATID + _raw_payer (effective_payer). Change to: in the ENCOUNTER scan for _payer_at_date, keep dual_eligible (add dual_expr from _effective_payer_and_dual_exprs). Select PATID_COL, ADMIT_DATE, effective_payer, dual_eligible. After choosing closest encounter per patient, return PATID_COL, _raw_payer (= effective_payer), _dual_eligible (= dual_eligible of closest row).
- Callers (first_dx, first_chemo, last_chemo): when building PAYER_CATEGORY_AT_FIRST_DX etc., use: "Dual eligible" if _dual_eligible == 1 else _collapse_payer_category(_raw_payer). Apply in the with_columns that create PAYER_CATEGORY_AT_FIRST_DX, PAYER_CATEGORY_AT_FIRST_CHEMO, PAYER_CATEGORY_AT_LAST_CHEMO.
  </action>
  <verify>_payer_at_date returns _dual_eligible; PAYER_CATEGORY_AT_FIRST_DX/AT_FIRST_CHEMO/AT_LAST_CHEMO can be "Dual eligible".</verify>
  <done>At-date payer variables show "Dual eligible" when the closest encounter is dual-eligible.</done>
</task>

<task type="auto">
  <name>Task 3: enc_chemo block — category "Dual eligible" when dual_eligible</name>
  <files>src/report/encounter_payer_summary.py</files>
  <action>
- In the enc_chemo scan, add dual_eligible (dual_expr from _effective_payer_and_dual_exprs) so each row has effective_payer and dual_eligible. When building PAYER_CATEGORY for each encounter in the chemo window, set category = "Dual eligible" when dual_eligible else _collapse_payer_category(effective_payer). The rest (group_by, mode, join to base) unchanged.
  </action>
  <verify>PAYER_CATEGORY_MOST_FREQUENT_AT_CHEMO can be "Dual eligible".</verify>
  <done>Most frequent at chemo uses "Dual eligible" level.</done>
</task>

<task type="auto">
  <name>Task 4: build_insurance_summary and docs — add "Dual eligible" to category order and docs</name>
  <files>scripts/build_insurance_summary.py, docs/CODEBOOK.md, docs/PAYER_VARIABLES_AND_CATEGORIES.md</files>
  <action>
- build_insurance_summary.py: Add "Dual eligible" to PAYER_CATEGORY_ORDER (e.g. after "Medicaid" so order is Medicare, Medicaid, Dual eligible, Private, ...).
- CODEBOOK.md (Section 6a) and PAYER_VARIABLES_AND_CATEGORIES.md: In the payer category list, add "Dual eligible" as a level. State that when an encounter is dual-eligible (Medicare+Medicaid or code 14/141/142), the payer category for that encounter is "Dual eligible" instead of Medicare or Medicaid.
  </action>
  <verify>Reports and docs list "Dual eligible" as a category; tables sort/display it correctly.</verify>
  <done>PAYER_CATEGORY_ORDER and docs updated.</done>
</task>

</tasks>

<verification>
- Run assemble_clean (or build_encounter_payer_summary) and build_insurance_summary; confirm parquet and reports can show "Dual eligible" in payer category columns.
- Optional: On data with dual-eligible encounters, spot-check that PAYER_CATEGORY_PRIMARY or AT_FIRST_DX is "Dual eligible" where expected.
</verification>

<success_criteria>
- "Dual eligible" is a possible value of PAYER_CATEGORY_PRIMARY, PAYER_CATEGORY_AT_FIRST_DX, PAYER_CATEGORY_AT_FIRST_CHEMO, PAYER_CATEGORY_AT_LAST_CHEMO, PAYER_CATEGORY_MOST_FREQUENT_AT_CHEMO.
- When encounter is dual-eligible, category is "Dual eligible" (not Medicare or Medicaid).
- Reports and docs include "Dual eligible" in the category list and order.
</success_criteria>

<output>
After completion, create .planning/phases/17-dual-eligible-as-payer-level/17-01-SUMMARY.md with what was implemented.
</output>
